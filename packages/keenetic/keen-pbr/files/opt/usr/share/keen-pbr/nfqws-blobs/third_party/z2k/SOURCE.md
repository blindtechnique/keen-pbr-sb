# z2k source snapshot

- Repository: `https://github.com/Necronicle/z2k.git`
- Branch at review time: `z2k-enhanced`
- Pinned commit: `ee2d04a5554dea26bbded45416e13e590bb71c6c`
- Source directory: `files/fake/`
- License: MIT; the exact license text is in `LICENSE.MIT`.

The following 20 packaged blobs are byte-identical to files at that commit:

| Packaged name | z2k source name |
|---|---|
| `http_iana_org.bin` | `http_iana_org.bin` |
| `quic_initial_4.bin` | `quic_4.bin` |
| `quic_initial_5.bin` | `quic_5.bin` |
| `quic_initial_6.bin` | `quic_6.bin` |
| `quic_initial_dbankcloud_ru.bin` | same name |
| `quic_initial_ozon_ru.bin` | same name |
| `quic_initial_rutracker_org.bin` | same name |
| `quic_initial_www_google_com.bin` | same name |
| `stun.bin` | same name |
| `syn_packet.bin` | same name |
| `tls_clienthello_14.bin` | same name |
| `tls_clienthello_4pda_to.bin` | same name |
| `tls_clienthello_activated.bin` | same name |
| `tls_clienthello_gosuslugi_ru.bin` | same name |
| `tls_clienthello_max_ru.bin` | same name |
| `tls_clienthello_t2.bin` | `t2.bin` |
| `tls_clienthello_vk_com.bin` | same name |
| `tls_clienthello_www_google_com.bin` | same name |
| `tls_clienthello_www_onetrust_com.bin` | same name |
| `zero_256.bin` | same name |

`quic_initial_steamcommunity_com.bin` is the pre-existing local keen-pbr-sb
asset. It is not claimed as z2k content; its project-maintained bytes are
pinned separately in `../../ORIGIN_SHA256SUMS`.
