# Блобы, на которые ссылаются наши стратегии, но которые мы не поставляем.
#
# Roadmap: "P0. Ввести единый invariant gate ... наличие каждой внешней
# зависимости в `required-blobs`".
#
# Эти файлы приходят вместе с установленным пакетом nfqws2 и лежат в его
# собственном каталоге /opt/etc/nfqws2/blobs, а не в нашем nfqws-blobs.
# Список нужен, чтобы гейт отличал внешнюю зависимость от опечатки в имени.
quic_initial.bin
tls_clienthello.bin
