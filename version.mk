KEEN_PBR_VERSION=3.2.1
KEEN_PBR_RELEASE=12
# The one sing-box release this build installs and tests against. install.sh
# and the daemon both read it from here; a gate fails if they drift.
SING_BOX_PINNED_VERSION=1.13.14
