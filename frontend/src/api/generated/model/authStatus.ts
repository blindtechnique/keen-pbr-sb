/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { AuthStatusKeeneticEndpointSource } from './authStatusKeeneticEndpointSource';

export interface AuthStatus {
  /** Authentication is required at all. */
  enabled: boolean;
  /** Which verifier decides, e.g. `local` or the router account. */
  provider: string;
  authenticated: boolean;
  /** This connection is trusted to carry a credential. False is the safe answer and the default: the panel must not collect a password over a path that cannot protect it.
   */
  trusted_local_connection: boolean;
  /** Present only while trusted. A string rather than a number so a browser cannot truncate it into a different generation.
   */
  trusted_local_connection_generation?: string;
  /** Present only while trusted. Lets the browser expire the capability itself when the next status refresh fails, instead of assuming it still holds.
   */
  trusted_local_connection_valid_for_seconds?: number;
  /** Present only when authentication is disabled. Names the scope the API is reachable from - `loopback_only` - so "no password" is never read as "open to the network".
   */
  no_auth_scope?: string;
  /** Present only when authentication is disabled: whether this caller is outside that scope.
   */
  network_api_blocked?: boolean;
  /** `auth_misconfigured` or `auth_endpoint_unavailable`. Reported on a 200 because the caller's own state is still described accurately; the error says the verifier cannot be consulted.
   */
  error?: string;
  /** Present only for an authenticated router-account session. */
  keenetic_endpoint?: string;
  keenetic_endpoint_mode?: string;
  /** `ndms` when the firmware named the endpoint, `fallback` when this build guessed - the difference matters when login fails.
   */
  keenetic_endpoint_source?: AuthStatusKeeneticEndpointSource;
}
