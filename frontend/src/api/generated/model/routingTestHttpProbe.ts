/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { RoutingTestHttpProbeMethod } from './routingTestHttpProbeMethod';
import type { RoutingTestHttpProbeReason } from './routingTestHttpProbeReason';
import type { RoutingTestHttpProbeScope } from './routingTestHttpProbeScope';
import type { RoutingTestHttpProbeStatus } from './routingTestHttpProbeStatus';

/**
 * One explicitly requested router-originated HTTPS HEAD probe to port 443 and path /. Uses the freshly evaluated IP, full mark and interface, preserves hostname TLS verification, and does not follow redirects. Any HTTP response is evidence of a response, not site usability or client PREROUTING/nfqws coverage. No retries or configuration changes.

 */
export interface RoutingTestHttpProbe {
  status: RoutingTestHttpProbeStatus;
  reason: RoutingTestHttpProbeReason;
  /** Requested destination; a changed DNS answer does not silently select a different address. */
  ip: string;
  /** HTTPS root URL used, or empty when the target was not usable. */
  url: string;
  method: RoutingTestHttpProbeMethod;
  scope: RoutingTestHttpProbeScope;
  /** Fresh FIB interface requested for socket binding; empty when unavailable. */
  interface: string;
  /** Probe attempt time in Unix seconds; zero if no transfer was attempted. */
  attempted_at: number;
  /**
     * @minimum 0
     * @maximum 4294967295
     */
  fwmark?: number;
  /**
     * @minimum 0
     * @maximum 4294967295
     */
  table?: number;
  /**
     * @minimum 100
     * @maximum 599
     */
  http_status?: number;
  /**
     * Total transfer time in milliseconds, when measured.
     * @minimum 0
     */
  elapsed_ms?: number;
  /**
     * Time from transfer start until the transport connection was established, not a separate phase duration.
     * @minimum 0
     */
  connect_ms?: number;
  /**
     * Time from transfer start until TLS completed, not the standalone TLS handshake duration.
     * @minimum 0
     */
  tls_ms?: number;
  /** Peer address observed by the HTTP transport when available. */
  connected_ip?: string;
}
