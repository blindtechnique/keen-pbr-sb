/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { RegistryCheckResponseReason } from './registryCheckResponseReason';

/**
 * Whether the target is on Russia's blocking registry, according to cheburcheck.ru. This is a fact about the registry, not about this router: whether the site actually works from here is what the routing test and the reachability probes answer, and the panel keeps the two apart on purpose.

 */
export interface RegistryCheckResponse {
  /** false when no lookup happened. Read it before `blocked`: a lookup that did not run must never be shown as a clean bill of health.
   */
  checked: boolean;
  /** The service credited for the verdict. */
  service: string;
  /** Present when `checked` is false. */
  reason?: RegistryCheckResponseReason;
  /** Present with reason `lookup_failed`. */
  error?: string;
  /** true when the verdict came from the hour-long cache rather than a fresh request. The registry moves in days, and the service publishes neither terms nor a rate limit.
   */
  cached?: boolean;
  target?: string;
  /** The domain itself is on the registry. */
  blocked?: boolean;
  /** The registry entry that covers it, when there is one. */
  rkn_domain?: string;
  /** Blocked prefixes covering the target's addresses. A site with no registry entry of its own can still be unreachable through one of these.
   */
  blocked_subnets?: string[];
  /** Addresses the service resolved, which may differ from ours. */
  ips?: string[];
  /** CDNs the addresses belong to. A blocked prefix on a shared CDN explains an outage that the domain's own registry status does not.
   */
  cdn_providers?: string[];
  /** Owner of the address range, when known. */
  organisation?: string;
}
