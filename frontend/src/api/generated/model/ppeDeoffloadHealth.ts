/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { PpeDeoffloadCapability } from './ppeDeoffloadCapability';
import type { PpeDeoffloadCounter } from './ppeDeoffloadCounter';
import type { PpeDeoffloadHealthState } from './ppeDeoffloadHealthState';
import type { PpeDeoffloadMode } from './ppeDeoffloadMode';
import type { PpeDeoffloadProtocolHealth } from './ppeDeoffloadProtocolHealth';

export interface PpeDeoffloadHealth {
  /** Configured desired mode; this alone never means active. */
  mode: PpeDeoffloadMode;
  /** Whether the complete live PPE contract was proven. */
  capability: PpeDeoffloadCapability;
  /** Reconciled runtime state. `admissible` means only that all preconditions currently hold; it must never be presented as `active`. `active` requires semantic verification of the live owned graph.
   */
  state: PpeDeoffloadHealthState;
  /** Stable machine-readable reason for a non-active state. */
  reason?: string;
  /** Optional operator-facing diagnostic detail. */
  detail?: string;
  /**
     * Per-flow packet window retained on the CPU, when known.
     * @minimum 1
     */
  connskip_packets?: number;
  tcp: PpeDeoffloadProtocolHealth;
  quic: PpeDeoffloadProtocolHealth;
  prerouting?: PpeDeoffloadCounter;
  forward?: PpeDeoffloadCounter;
  /** Unix timestamp of the last completed reconcile attempt. */
  last_reconcile_ts?: number;
  /** Unix timestamp when the live graph and counters were observed. */
  observed_at?: number;
}
