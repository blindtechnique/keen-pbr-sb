/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */

/**
 * The automation that finds hosts nfqws2 keeps failing on and routes them through a tunnel instead.

Each pass reads what nfqws2 recorded in its auto-hostlist debug log, picks hosts that are not already covered, and measures each one twice: once pinned to the provider's own device and once pinned to the tunnel's. Only a host that answers through the tunnel and not directly is a candidate; every other verdict leaves traffic exactly where it is.

Moving a host is not reversible in practice. nfqws2's rules are bound to the provider interface, so once a host is routed through a tunnel it disappears from nfqws2's view and stops producing the evidence that put it there. That is why this is off by default, why the registry check is on by default, and why confirmed hosts are appended to a file this automation owns rather than written into the operator's configuration.

 */
export interface TunnelProbeConfig {
  /** Whether the periodic pass runs. Off by default: a pass sends real requests to real hosts and can change what a list routes.
   */
  enabled?: boolean;
  /**
     * The tunnel to measure against, and the one confirmed hosts end up using. It must name an outbound that has an interface: each probe leg is pinned to a device, and a mark alone would measure whatever routing happened to pick.

     * @minLength 1
     * @maxLength 128
     */
  outbound?: string;
  /**
     * The list that receives confirmed hosts. It must be a list with a `file`, because that file is what this automation appends to. A list without one is refused rather than silently ignored.

  Create the file before naming it in the list. A list whose `file` does not exist makes list streaming throw, and that surfaces inside the firewall apply - so a missing file does not disable one list, it stops routing from being applied at all. An empty file is enough; a list may carry `file` and inline `domains` at the same time, and all of its sources are read together.

     * @minLength 1
     * @maxLength 128
     */
  list?: string;
  /**
     * How often to look. The trigger is nfqws2 itself: a host enters the queue when nfqws2 records that it failed on it, so a minute is the longest anything waits.

  Looking is cheap. A pass reads only what was appended to nfqws2's log since the last one, and when nothing was appended and the queue is empty it ends there, having probed nothing. What a pass costs is bounded by `max_probes_per_pass`, and only while there is a backlog to work through; once the queue is drained the cost falls to reading a few small files.

     * @minimum 60000
     * @maximum 86400000
     */
  interval_ms?: number;
  /**
     * How many hosts one pass measures. Two requests each, so this bounds the traffic a pass generates; the rest of the queue waits for the next one.

     * @minimum 1
     * @maximum 64
     */
  max_probes_per_pass?: number;
  /** Whether a host must also be listed as blocked by the registry check before it is acted on. A differential probe proves that this connection failed here and worked through the tunnel; it cannot tell deliberate blocking from a host that simply refuses this network. On real traffic this gate is what keeps advertising endpoints out of the result.
   */
  require_registry_confirmation?: boolean;
}
