/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { RoutingTestNfqwsMatch } from './routingTestNfqwsMatch';

/**
 * One saved startup profile, in --new order. Only list predicates are evaluated, separately for each resolved IP. This is not a claim that the profile wins for a live packet: protocol, port, visible hostname, earlier profiles, queue admission and running state also matter.

 */
export interface RoutingTestNfqwsProfile {
  /** @minimum 1 */
  index: number;
  name: string;
  /** Saved --filter-* selectors, shown as conditions rather than observed traffic. */
  filters: string[];
  /** Whether the profile contains a strategy action; false for a pass-through profile. */
  has_actions: boolean;
  /** Nonempty domain filters or an auto hostlist require a hostname visible to nfqws. */
  hostname_required: boolean;
  auto_hostlist: boolean;
  /**
     * Result of the list predicates only. Host and IP includes intersect; exclusions are local to this profile. mixed means different IPs have different results. Empty static includes are unrestricted, while an empty auto hostlist still waits for learning.

     * @pattern ^(matched|excluded|unmatched|unrestricted|mixed|unknown|hostname_required|ip_required|auto_pending)$
     */
  list_result: string;
  matches: RoutingTestNfqwsMatch[];
}
