/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { SubscriptionApplyResultOutcome } from './subscriptionApplyResultOutcome';

export interface SubscriptionApplyResult {
  line: number;
  /** What happened to this entry. `already_imported` means an earlier apply of the same preview already created it - a preview is a one-shot import, so re-applying a line cannot duplicate the transport it produced. It is deliberately not `failed`: nothing went wrong and there is nothing for the operator to fix, and presenting it as an error would teach them to distrust the report.
   */
  outcome: SubscriptionApplyResultOutcome;
  /** The tag the transport was created with. */
  tag?: string;
  /** The derived kernel interface name, produced by the same rule the manual creation dialog uses, so an imported transport is indistinguishable from a hand-made one.
   */
  interface?: string;
  /** Why this entry failed. Present only with `outcome: failed`, and never contains the link - a transport manager error that echoes the share link is replaced before it reaches here.
   */
  error?: string;
}
