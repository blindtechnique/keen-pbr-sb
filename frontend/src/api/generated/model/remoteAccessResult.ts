/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { RemoteAccessSettings } from './remoteAccessSettings';

/**
 * One shape for accepted, queued and refused outcomes; all arrive as HTTP 200.

 */
export interface RemoteAccessResult {
  /** Absent on every refusal. */
  ok?: boolean;
  /** `auth_state_unavailable`, `login_disabled`, `keenetic_auth_plaintext_wan`, `listen_loopback`, `custom_port_not_supported_safely`, a port-range message, `cannot write remote-access.json`, or - when reconciliation is degraded and nothing owns its recovery - `cannot apply remote-access firewall rules`.
   */
  error?: string;
  /** Why the refusal, or what the queued reconciliation is waiting for.
   */
  detail?: string;
  /** The configured bind address, returned with `listen_loopback` so the caller can see what made it unreachable.
   */
  listen?: string;
  /** Returned with `custom_port_not_supported_safely`. */
  supported_port?: number;
  settings?: RemoteAccessSettings;
  durable?: boolean;
  /** Present only when `durable` is false. */
  warning?: string;
  /** The firewall state is queued, not applied. No firewall command runs on this request.
   */
  pending?: boolean;
  generation?: number;
  phase?: string;
  degraded?: boolean;
  retry_scheduled?: boolean;
  recovery_owned?: boolean;
  maintenance?: boolean;
  /** Present only when a retry is scheduled. */
  retry_after_ms?: number;
}
