/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { NdmsNativeDeletePhase } from './ndmsNativeDeletePhase';
import type { NdmsNativeDeleteStatus } from './ndmsNativeDeleteStatus';
import type { NdmsNativeDeleteStop } from './ndmsNativeDeleteStop';
import type { NdmsNativeDeleteTransportOutcome } from './ndmsNativeDeleteTransportOutcome';
import type { NdmsNativeDirectObservationFailure } from './ndmsNativeDirectObservationFailure';
import type { NdmsNativeManagedInterfaceName } from './ndmsNativeManagedInterfaceName';
import type { NdmsNativeMutationKind } from './ndmsNativeMutationKind';

/**
 * Phase, interface_name and kind are an all-or-none durable identity projection. A blocked or recovery_required response may omit all three when the WAL cannot be reread; clients must not invent a target. `save_acknowledged_unverified` is coherent only with stop `none`, phase `cleanup`, both original acknowledgements, a durable tombstone and a freshly reread rollback snapshot. It remains amber rather than proof of startup persistence. No response contains an internal transaction id, ownership revision, marker, snapshot contents or raw RCI response.

 */
export interface NdmsNativeDeleteResponse {
  status: NdmsNativeDeleteStatus;
  stop: NdmsNativeDeleteStop;
  /** Always false. The owner acknowledges the remaining race; the cooperative lease cannot exclude firmware or third-party writers.
   */
  external_writer_race_excluded: boolean;
  external_writer_race_accepted: boolean;
  global_save_scope_acknowledged: boolean;
  /** Current-invocation transport fact only. */
  delete_perform_started: boolean;
  /** Current-invocation transport fact only. */
  save_perform_started: boolean;
  /** Current-invocation transport fact only. */
  request_may_have_been_dispatched: boolean;
  /** The running save request was acknowledged; this is not proof of startup/saved configuration.
   */
  system_configuration_save_acknowledged: boolean;
  ownership_tombstone_durable: boolean;
  /** True only after this invocation reread the exact encrypted full snapshot at the response boundary; false also means unknown.
   */
  rollback_snapshot_retained: boolean;
  phase?: NdmsNativeDeletePhase;
  interface_name?: NdmsNativeManagedInterfaceName;
  kind?: NdmsNativeMutationKind;
  transport_outcome?: NdmsNativeDeleteTransportOutcome;
  observation_failure?: NdmsNativeDirectObservationFailure;
}
