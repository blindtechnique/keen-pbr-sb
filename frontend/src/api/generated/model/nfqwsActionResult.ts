/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */

/**
 * The union of what the actions return; each populates the subset that applies to it.

 */
export interface NfqwsActionResult {
  /** For `service` and the package actions this is the command's exit status being zero - not a claim about what the service is now doing. For `check_url` it means the check ran, not that the site answered.
   */
  ok?: boolean;
  /** `read_file`. For a log the lines are newest first, not file order.
   */
  content?: string;
  /** Command output, capped. */
  output?: string;
  /** Exit code of the command that ran. */
  status?: number;
  durable?: boolean;
  /** Present when `durable` is false. */
  warning?: string;
  /** The daemon was asked to reconcile the firewall because this action could change the live queue or the process argv. No firewall command runs on the HTTP worker.
   */
  firewall_reconcile_pending?: boolean;
  /** `check_url`. */
  reachable?: boolean;
  /** `check_url`: why it was not reachable. The reason is kept because an unresolved name, a refused connection and a cut TLS handshake are three different problems.
   */
  error?: string;
  captured?: number;
  restored?: number;
  failed?: number;
  files_restored?: boolean;
  runtime_verified?: boolean;
  journal_retained?: boolean;
  /** After `restore_component`, false means the panel can no longer tell which version is installed - which is what blocks further updates until the package is repaired by hand.
   */
  package_metadata_verified?: boolean;
  restore_point?: string;
  /** Always false. A file restore is not a package rollback, and the field exists so nothing has to infer that from silence.
   */
  exact_package_state?: boolean;
  installed_blobs?: number;
  preserved_blobs?: number;
  [key: string]: unknown;
 }
