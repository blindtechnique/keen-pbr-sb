/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { SystemUpdateLocalStatus } from './systemUpdateLocalStatus';

/**
 * Local update state plus the release comparison. Every release field is empty when no release is known; an empty `latest` is the absence of an answer, not a claim that the router is current.

 */
export type SystemUpdateStatus = SystemUpdateLocalStatus & {
  /** Selected channel; empty if the setting is invalid. */
  channel?: string;
  /** Package-owned channel, independent of the preference. */
  installed_channel?: string;
  /** Immutable tag to confirm when starting an update. */
  release_tag?: string;
  source?: string;
  channel_change?: boolean;
  /** Known release, no check error, not older than the current package, and either newer or an explicit channel switch.
   */
  installable?: boolean;
  /** Version this daemon reports, as `v<version>-<build timestamp>`. */
  current: string;
  /** Latest release tag, or empty when none is known. */
  latest: string;
  available: boolean;
  /** This build is newer than the latest published release, which happens on a locally built package.
   */
  current_ahead: boolean;
  release_name: string;
  /** Truncated to 64 KiB with a trailing ellipsis. */
  release_notes: string;
  release_url: string;
  /** Empty unless the tag passed validation, so an unexpected tag cannot be turned into a link.
   */
  changelog_url: string;
  /** The release half was served from the local cache. */
  cached: boolean;
  /** Why the remote check failed while a cached release was still returned. Empty when the check succeeded.
   */
  check_error: string;
};
