/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */

/**
 * The most recent pass of the nfqws2-to-tunnel automation.

 */
export interface TunnelProbeStateResponse {
  /** Whether a pass has completed since this daemon started. False also covers the ordinary case of the automation being switched off, and `refusal` then says so.
   */
  ever_ran: boolean;
  /** Why the last attempt did nothing, in one sentence. Absent when the pass ran.
   */
  refusal?: string;
  /** The same sentence the daemon logs: what was read, probed, routed and held back.
   */
  summary?: string;
  /**
     * Hosts measured in that pass. Two requests each.
     * @minimum 0
     */
  probed?: number;
  /**
     * Hosts left in the queue for the next pass. Zero means the automation has worked through everything nfqws2 has complained about so far.

     * @minimum 0
     */
  remaining?: number;
  /** Hosts that pass added to the list, and so moved into the tunnel. Not reversible in practice: nfqws2's rules are bound to the provider interface, so a routed host stops producing the evidence that put it there.
   */
  routed?: string[];
  /** Hosts a tunnel would fix that the registry check did not confirm. The probe proves the connection failed here and worked through the tunnel; it cannot tell deliberate blocking from a host that refuses this network for its own reasons. These are the ones worth a person deciding about.
   */
  held_back?: string[];
  /**
     * When that pass finished, in milliseconds since the epoch.
     * @minimum 0
     */
  finished_at_unix_ms?: number;
}
