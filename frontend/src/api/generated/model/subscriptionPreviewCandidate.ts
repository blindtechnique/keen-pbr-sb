/**
 * Generated by orval v8.6.2 🍺
 * Do not edit manually.
 * keen-pbr API
 * REST API for the keen-pbr policy-based routing daemon.
 * OpenAPI spec version: 3.0.0
 */
import type { SubscriptionPreviewCandidateDisposition } from './subscriptionPreviewCandidateDisposition';

/**
 * One entry of the fetched subscription. Deliberately has no field that can hold the share link: the link carries the credential, and the preview is built so that forgetting to redact is structurally impossible rather than merely avoided.

 */
export interface SubscriptionPreviewCandidate {
  /** 1-based line in the decoded document as the provider serves it, counting blanks and comments. This is the selection key for apply.
   */
  line: number;
  /** Lowercased link scheme, e.g. "vless". Empty when malformed. */
  scheme?: string;
  /** Host and port with any userinfo removed. Empty for vmess and legacy shadowsocks, whose endpoint lives inside a base64 payload that also carries the credential and is therefore not opened.
   */
  endpoint?: string;
  /** The provider's label (URI fragment), percent-decoded, with C0/C1 controls and bidirectional override characters removed so the label cannot misrender the entry it describes.
   */
  remark?: string;
  /** Tag derived from the remark, matching the transport tag pattern and unique within this plan. Empty when the candidate is not importable.
   */
  suggested_tag?: string;
  disposition: SubscriptionPreviewCandidateDisposition;
  /** For duplicate_in_document - the line of the occurrence that was offered instead.
   */
  duplicate_of?: number;
}
