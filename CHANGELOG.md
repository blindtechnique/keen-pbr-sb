# Журнал изменений keen-pbr-sb

Здесь перечислены изменения именно форка `keen-pbr-sb`. История оригинального keen-pbr остаётся в upstream-репозитории [maksimkurb/keen-pbr](https://github.com/maksimkurb/keen-pbr).

Формат основан на Keep a Changelog. Установленные сборки обозначаются как `3.3.0-ГГГГММДДччммсс`: первая часть — базовая версия keen-pbr, последние 14 цифр — время сборки форка в UTC.

## [3.3.0] — в разработке

Ниже собраны все изменения после последнего редактирования этого файла 17 августа 2026 года, включая синхронизацию с новой базой upstream и последующие исправления на реальном Keenetic.

Локальная поставка `3.3.0-20260908141642` установлена на `192.168.1.1:222`
08.09.2026: добавлено сохранение Lua, стратегии и настроек при поддерживаемой
миграции конфигурации nfqws через панель. Backend собран для Keenetic aarch64
GCC 8.4 / glibc 2.27; WebUI имеет тот же timestamp. Неизменившийся companion
Go 1.25.12 переиспользован после сравнения всех 44 исходных файлов двух снимков;
его происхождение в IPK подтверждено побайтно после Entware strip.
IPK: 7 867 849 байт,
SHA256 `97f68f22b1223758b1eca244c2b48de763d94a825ee6c2840a171b74c16bfcda`.
Состав пакета, ELF/ABI, установочные файлы, чистый seed, WebUI и 84 gzip-копии
проверены. После первого штатного postinst маршрутизация — `Overall: OK`,
transport-manager health — `ok`; DNS разрешает youtube.com и 2ip.io.
HTTP index и активный `assets/index-DumbrT9Q.js` совпадают с пакетом;
config/transports/auth не изменились. nfqws2 сохранил PID и starttime.
Резерв: `/opt/var/backups/keen-pbr/release-20260908141642`; штатный rescue
сохраняет предыдущий пакет, без pending/unknown. Повторный restart/apply,
обновление пакета nfqws, перезагрузка роутера, commit и push не выполнялись.
Миграция при реальном обновлении внешнего nfqws этим проходом не проверялась:
установлено исправление обновляющего кода, а не новая версия самого nfqws.

Предыдущая объединённая локальная поставка `3.3.0-20260908125228` установлена на
`192.168.1.1:222` 08.09.2026. В один свежий снимок вошли накопленные
срезы P0–P1/P2 и все исправления повторного ревью FR1–FR11. Backend собран
для Keenetic aarch64 GCC 8.4 / glibc 2.27, transport-manager — Go 1.25.12;
WebUI имеет тот же timestamp. Проверены состав IPK, ELF/ABI, lifecycle scripts,
чистый стартовый seed, весь WebUI и 84 gzip-копии. Происхождение companion
подтверждено побайтно после штатного Entware strip.
IPK: 7 864 238 байт,
SHA256 `67fa68243cc46fe2ff1577b90d4749485b32024e7df27db728ce7a573950a1a0`.
Первый штатный postinst поднял службы и маршрутизацию (`Overall: OK`),
transport-manager health — `ok`; DNS разрешает youtube.com и 2ip.io.
Ранее наблюдавшееся предупреждение неавторитетного inventory после установки
отсутствует; причина прежнего состояния этим не установлена. HTTP index
и активный `assets/index-Di2dm0I4.js` совпадают с пакетом; config/transports/auth
не изменились. nfqws2 сохранил PID и starttime, поэтому новая Lua-логика,
уже доставленная на диск, начнёт работать при следующем обычном его перезапуске.
Публичный ключ и verifier установлены, OpenSSL 3.5.5 добавлен зависимостью;
первый подписанный CI-выпуск и сквозное подписанное обновление ещё не проверены.
Полный Linux Go 1.25 test/vet — PASS; frontend gate ранее дал 1829/1829,
релизная WebUI-сборка и cross-build GCC 8 — PASS. Новый C++ HTTP proxy-тест
скомпилирован ранее, но отдельный запуск не завершён и не засчитан.
Резерв конфигурации/WebUI: `/opt/var/backups/keen-pbr/release-20260908125228`;
штатный rescue имеет текущий и предыдущий пакет, без pending/unknown.
Не было второго restart/apply, перезагрузки роутера, commit или push.
Это ограниченная проверка установки, не полная клиентская, cold-boot или
fault-injection приёмка. Исторические пометки «локально/не установлено» ниже
для вошедших изменений описывают прежние этапы, а не текущую поставку.

Предыдущая объединённая локальная поставка `3.3.0-20260906231955` установлена на
`192.168.1.1:222` 07.09.2026. В пакет вошли все 32 исправления ревью
R1–R12 / S1–S4 / N1–N4 / L1–L7 / C1–C5 и накопленные изменения WebUI.
Оба бинарника и WebUI собраны из одного свежего снимка текущего дерева:
Keenetic aarch64 GCC 8.4 / glibc 2.27 и новый transport-manager Go 1.25.
Состав реального IPK, lifecycle scripts, чистый стартовый seed и WebUI/gzip
сверены со снимком; происхождение transport-manager подтверждено побайтно
после штатного Entware strip. IPK: 7 627 233 байта,
SHA256 `8caa98a1a6b4e1fd75c9a8e1991eaae4b7b5ed9faa5e34b2f1dcd9d6047d374a`.
После первого штатного postinst службы и маршрутизация работают (`Overall: OK`),
health transport-manager — `ok`; DNS роутера разрешает youtube.com и 2ip.io.
HTTP index и активный JS совпадают с пакетом. Config/transports/auth и
OpenConnect hook сохранены; nfqws2 не перезапускался. Повторного restart/apply,
перезагрузки роутера, commit или push не было. Конфигурация, прежний WebUI
и штатный откат сохранены. Это ограниченная живая проверка установленной
поставки, не закрытие всех клиентских, cold-boot и fault-injection сценариев.

Предыдущая объединённая локальная поставка `3.3.0-20260906110559` установлена на Keenetic
06.09.2026. В неё вошли все накопленные срезы подписок/backup, журналов,
первого запуска/мастера, действий после применения, ленты событий,
локализации и подписей интерфейсов. Backend собран для Keenetic aarch64 GCC 8;
WebUI и пакет имеют один timestamp, 328 изменённых/новых файлов сверены со
снимком. IPK: SHA256 `e4959bdbeda3349bd48296863c246112e732ce428f009d324c7d822812d1dc4d`,
7 585 375 байт. Подготовленный transport-manager и OpenConnect hook сохранены.
Сервисы и маршрутизация поднялись после первого штатного postinst без второго
restart/apply (`Overall: OK`); config/transports/auth не изменились,
nfqws2 не перезапускался. Новый WebUI подтверждён HTTP 200 и SHA256 entry JS.
Есть резерв предыдущего пакета; commit/push не выполнялись.
Точечные повторные проверки: backend backup 45/45, refresh 61/61,
пакетные/restart Python 31/31. Последний полный frontend gate — 1503/1503;
release typecheck/WebUI build и проверка реального IPK PASS. Живая UI-приёмка
остаётся открытой: инструмент Chrome завершился при подключении. Исторические
записи о срезах, завершённых до этой поставки, сохранены как этапы разработки;
их пометки «ещё не установлен» заменены этой записью. Более поздние локальные
изменения отмечены отдельно и пока в установленный пакет не входят.

Предыдущая объединённая локальная поставка `3.3.0-20260905193037` установлена на Keenetic
05.09.2026: backend и WebUI имеют один номер сборки; подготовленный
transport-manager и принятое исправление OpenConnect сохранены. Сервисы,
группы и маршрутизация поднялись после первого штатного postinst, без второго
restart/apply; пользовательская конфигурация не изменилась. Поставка не
означает завершения контролируемой приёмки новых failure/shrink сценариев.

### Исправления проверок alpha CI — 08.09

- Проверка shell больше не принимает функции AWK в verifier за bashism:
  кавычки аргументов `-F`/`-v` отделены от самой программы; комментарии и
  соседние shell-команды остаются видимыми для проверки. Добавлены регрессии.
- Проверка текстов учитывает точную внутреннюю ошибку фоновой диагностики:
  UI уже перехватывает её и показывает существующий перевод RU/EN.
- Изолированный тест удаления запускается обычным пользователем CI:
  UID имитируется только во временной копии entry point, дочерние helpers
  видят настоящего владельца файлов. Реальная проверка root не изменена;
  отрицательный сценарий проверяет отказ до каких-либо изменений. Все 62
  сценария `test-package-dns` прошли от непривилегированного пользователя.
- Все шесть оставшихся тестовых заглушек обновления списков приведены к
  текущей сигнатуре `const api::ListRefreshRequest&` вместо `optional<string>`.
  Затронутые C++-файлы отдельно скомпилированы GCC 13 в режиме C++17;
  production API и логика сервиса не менялись, полный прогон остаётся в CI.
- Пройдены 10 тестов сканера, 38 shell-скриптов, OpenAPI/nfqws/Lua checks,
  literals check и 12 тестов фоновой диагностики. Это исправления CI,
  без изменений кода маршрутизации, установленных пакетов и служб роутера.

### Lua при миграции nfqws через панель — установлено 08.09 в 20260908141642

- Исправлена миграция поддерживаемого формата `nfqws2.conf` при обновлении
  nfqws из панели: прежняя стратегия, параметры и подключение Lua сохраняются
  до запуска нового движка, вместо безусловного отката из-за замены конфига.
  Новое числовое `CONFIG_VERSION` и отсутствующие известные defaults берутся
  из нового пакета; остальные прежние значения и текст не перезаписываются.
  Используется существующий разбор shell-присваиваний без выполнения shell;
  неизвестные поля/форматы и изменившиеся зависимости не угадываются.
- Перед запуском перенос проверяется существующим validator и dry-run нового
  nfqws. При несовместимости действует прежний откат; обычные обновления без
  миграции и пользовательские Save/Apply не получают новых проверок.
- Техническая версия конфига сама по себе больше не сбрасывает название
  активного пресета. Присваивания VERSION внутри кавычек не удаляются,
  а реальные зависимости параметров от VERSION не считаются метаданными.
- Заводской конфиг нового пакета сохраняется отдельной стратегией; ошибка
  записи этой необязательной копии даёт предупреждение, не откат рабочего
  обновления. Scripted update имеет один штатный start после переноса;
  остановленная до обновления служба не запускается. Plain fallback при
  уже состоявшемся package start перезапускает его с сохранённым профилем.
- Проверено: 58/58 тестов `keen-pbr-api-operation-errors-tests`, 732 assertions,
  включая 9 новых регрессий; production handler скомпилирован C++17.
  Это локальная правка для подтверждённого владельцем обновления через нашу
  панель, не hook для внешнего opkg и не обещание совместимости с любым
  будущим форматом nfqws. Исправление включено в IPK `3.3.0-20260908141642`
  и установлено; ограниченная проверка первого запуска прошла. Сам пакет
  nfqws не обновлялся и не перезапускался, реальная миграция его новой версии
  на роутере пока не проверена. Commit/push не выполнялись.

### Исправления полного повторного ревью FR1–FR11 — 08.09, включены в 20260908125228

- Проверка подписей совместима с сокращённым `od` Keenetic: конечный LF
  проверяется без его неподдерживаемых параметров. Встроенный verifier
  автоматически перегенерирован из единого исходника.
- Консольная установка sing-box проверяет кандидат и wrapper до замены
  рабочих файлов; ошибки публикации возвращают прежнюю пару. Восстановление
  HTTPS сохраняет старый nfqws feed при ошибках и обрыве SSH и выполняется
  до bootstrap OpenSSL, если это первичная установка, а не update-only.
- Восстановление backup и rollback обновляют config/связанные кеши по ответу
  REST, без зависимости от SSE. При открытой форме восстановленные значения
  не заменяются старой базой; отдельно сохраняются реальные локальные правки.
- Таблица обучения nfqws ограничена 64 записями непосредственно в памяти,
  даже после отказа телеметрии; интервал дисковых записей не увеличен.
- Shared sing-box сохраняет crash watcher после переименования и повторного Up.
  Импортируемые VPN получают свободные TUN /30 с сохранением назначения;
  совпавшие хеши имён больше не отклоняют корректные серверы подписки.
- Запоздалый native GeoIP меняет только страну текущего интерфейса в auto,
  не возвращает старое имя и не отменяет ручной выбор; runtime не перезапускает.
- Поля журнала согласованно недоступны во время Save. Ошибка API соединений
  больше не выглядит пустым списком: добавлены RU/EN сообщение, повторение
  загрузки, сохранение прежних строк и состояние ошибки следующей страницы.
- Регрессии установщика и пропущенная цель фоновой диагностики подключены
  к обычному `make test`. Новых общих блокировок Save/Apply или дополнительных
  фоновых процессов нет. Frontend: 1829 тестов, lint/i18n/typecheck/build,
  known-fields и повторная генерация PASS; целевые Python/Go/Lua проверки PASS.
  C++ handler и proxy-тест скомпилированы в C++17; сам HTTP proxy-сценарий
  остаётся запуском общего backend suite, без заявления роутерной приёмки.
  **IPK не собирался, роутер/commit/push не затронуты.**

### Подписанные обновления Keenetic — локально 08.09, ещё не установлено

- Добавлены собственный публичный ключ форка, RSA-3072/SHA-256 signer и
  standalone BusyBox verifier. Manifest связывает repository/channel/tag,
  source/build и SHA256 установщика/IPK с именем, версией и архитектурой/ABI.
- Self-update проверяет скачанный install.sh до исполнения; установщик
  проверяет IPK до распаковки rescue helpers и opkg. Ключ не загружается рядом
  с обновлением: его доставляет доверенный bootstrap-пакет/установщик.
  Встроенные verifier/key генерируются из единых исходников и сверяются CI.
- Full/headless включают verifier, публичный ключ и зависимость openssl-util;
  validator проверяет их присутствие. Подпись не добавляет постоянно работающих
  процессов, перезапусков nfqws или блокировок обычных пользовательских действий.
- CI подготовлен для stable-релизов и alpha/next-артефактов; PR не требуют
  секрета. Временный ключ очищается при успехе и ошибке, подпись предшествует
  изменению опубликованных release assets. Исторический установщик без
  проверки подписей не подписывается без соответствующего backport.
- По явному разрешению владельца отдельный ключ подключён в Actions Secrets;
  его проверенная зашифрованная DPAPI-копия хранится вне Git. Она зависит от
  Windows-профиля; приватный ключ не входит в репозиторий или пакеты.
- 80 точечных offline-тестов проверяют подписи, реальный порядок shell-вызовов,
  состав IPK и CI. Локальное восстановление, включая legacy snapshots, не меняется
  и не требует сети. Первый доверенный пакет и первый подписанный stable-релиз
  требуют согласованной поставки: старые unsigned-релизы новый updater не примет.
  **IPK не собирался, роутер не изменён; commit/push и реального CI-запуска не было.**
  Подробности: `docs/keenetic-release-signing.md`.

### Один выпуск на весь проход обновления — локально 08.09, ещё не установлено

- Self-update передаёт выбранный тег загруженному установщику. Установщик
  обращается к этому выпуску, не выбирает новый latest посреди операции.
  Несовпадение ответа обнаруживается до загрузки/распаковки пакета;
  сообщение объясняет состояние и предлагает повторить проверку обновлений.
- Одноразовый параметр удаляется из окружения до opkg/postinst, чтобы не
  попасть в перезапущенные службы. Первая установка без указанного тега
  сохраняет выбор latest; проверка SHA256 и локальный откат не изменены.
- Шесть целевых offline-тестов проходят, регрессия добавлена в ранний CI.
  **Этот подготовительный срез сам по себе не является подписью.** Доверенный
  ключ и signer добавлены отдельным срезом выше. IPK/роутер/commit/push не трогались.

### Отключаемый сигнал Secure DNS — локально 08.09, ещё не установлено

- Рядом с DNS клиентов в основных настройках добавлена галочка «Отключать
  автоматический Secure DNS в Firefox». Обычный локальный черновик и Save/Apply;
  возврат к исходному значению снимает признак изменений. RU/EN-подписи и
  раскрываемое пояснение не обещают управление всеми браузерами.
- Новый необязательный `dns.firefox_doh_canary`: отсутствие/null/true сохраняют
  прежний сигнал `use-application-dns.net`; false убирает только эту строку.
  Значение участвует в DNS-хеше и обычном применении; явное false сохраняется
  в конфигурации и backup, неизвестные соседние поля не теряются.
- Ручной DoH Firefox, Chrome, Edge и Яндекс.Браузер не перенастраиваются.
  Нет блоклиста HTTPS-резолверов, изменений firewall/nfqws, DNS-провайдера
  роутера или новых проверок пользовательских действий.
- Документация DNS и CLI дополнена; оба API-клиента и known-fields
  сгенерированы из схемы. Backend: 59 DNS-generator + 255 config-validation
  случаев, суммарно 314 / 3444 assertions, PASS. Frontend: 1814 / 11378,
  lint/i18n/typecheck/WebUI build/known-fields PASS; прежнее предупреждение
  TanStack Virtual (0 ошибок). Production config/parser и DNS generator
  скомпилированы локально в C++17. IPK/роутер/commit/push не трогались;
  это не живая приёмка браузеров или Keenetic GCC 8.

### Диагностика фоновых задач — локально 08.09, ещё не установлено

- В настройки → журнал добавлен раскрываемый раздел «Фоновые задачи»:
  последний результат и длительность, ошибки подряд, число выполняющихся
  проходов и следующий плановый вызов. Подписи доступны на русском и английском;
  неизвестное расписание не выдаётся за отключение задачи или обещание повтора.
- API использует прежний ограниченный реестр и реальные таймеры Scheduler.
  Просмотр не читает срабатывания timerfd и не меняет расписание, блокировки,
  маршрутизацию или перезапуски. Success/noop сбрасывают серию ошибок;
  skipped/abandoned сохраняют. Новые поля необязательны для старых клиентов.
- Данные запрашиваются только при раскрытии раздела или нажатии «Обновить».
  Постоянного опроса, нового SSE, карточки на дашборде и новых проверок действий нет.
- Локально PASS: 31 backend case / 489 assertions; 1806 frontend tests /
  11281 assertions, lint/i18n/typecheck/WebUI build/known-fields.
  В lint осталось прежнее предупреждение TanStack Virtual (0 ошибок).
  Узкая backend-цель проверяет timerfd и API-проекцию, не HTTP/authentication.
  Production handler, daemon_api, Scheduler и реестр скомпилированы локально
  в C++17; это не приёмка Keenetic GCC 8 или установленного роутера.
  IPK, установка на роутер, commit и push не выполнялись.

### WAN/hotspot по событиям — локально 08.09, ещё не установлено

- WAN и hotspot-кеши адресно помечаются устаревшими через существующий monitor:
  смена default route/адреса обновляет WAN, значимые ARP/NDP-события — клиентов,
  topology/gap/reconnect — оба наблюдения. Кеш версии не инвалидируется.
- Счётчик клиентов по-прежнему приходит из Keenetic, не из таблицы соседей.
  Необязательная NEIGH-подписка не ломает прежний monitor на старом ядре;
  tracker ограничен 2048 записями и игнорирует обычный шум NUD-состояний.
  Клиентские события не вызывают firewall/routing rebuild или проверку VPN.
- Инвалидация не делает RCI в daemon loop. Сохраняются single-flight,
  последний корректный результат, pending-событие во время запроса,
  пятисекундный cooldown и прежняя задержка после ошибки.
- Существующий SSE передаёт лёгкое событие router_info. WebUI объединяет
  повторы и перечитывает только активную карточку metadata, сохраняя один
  отложенный запрос после текущего. Постоянные интервалы metadata 60 секунд
  и локальных metrics 15 секунд не изменены; новых потоков/worker нет.
  Подробности и ограничения: docs/router-overview-events.md.
- Локально PASS: 66 backend cases / 2749 assertions, 1794 frontend tests /
  11173 assertions; production API handler и daemon_core скомпилированы.
  lint/i18n/typecheck/WebUI build также PASS; прежнее предупреждение TanStack
  Virtual сохранено, ошибок нет.
  IPK, установка на роутер, commit и push не выполнялись.

### JSON/YAML списки целиком — локально 08.09, ещё не установлено

- В редакторе списков добавлен импорт файла/вставленного текста с явным выбором
  text, JSON-массива строк или YAML payload. Импорт добавляет весь результат
  в текущий локальный черновик, не сохраняет автоматически и не выбирает имя.
  Ошибки показывают исходную строку; частичный список не добавляется.
- Формат URL и файла на роутере сохраняется в конфигурации. Общий parser
  используется при preview, импорте и последующих обновлениях; IP/CIDR и
  домены нормализуются существующим ListParser. Полные Clash/sing-box config
  не интерпретируются; поддерживаемые структуры описаны в docs/list-source-formats.md.
- Кеш учитывает URL, формат и версию decoder. Структурированный список
  публикуется после полного разбора, ошибка оставляет прежний кеш. Прежние
  text/SRS и правила shrink сохраняются; новых условий для обычного Save/Apply нет.
- Ограничение preview в 50 строк не распространяется на импорт: до 50000
  записей, 2 MiB входа/результата и 4096 байт на значение. RU/EN-подсказки
  объясняют формат, действие над черновиком и причину ошибки.
- Локально PASS: 180 backend cases / 2159 assertions, 1786 frontend tests /
  11088 assertions; lint/i18n/typecheck/WebUI build и production API handler
  object. Лимит структурированного URL действует уже при загрузке; обычный
  text/SRS получает прежний лимит. IPK, установка, commit и push не выполнялись.

### Общая база opkg после прерванного обновления — локально 08.09, ещё не установлено

- После отдельного подтверждения владельцем реализовано восстановление
  потерянного, пустого или обрезанного общего status перед переустановкой
  прежнего IPK nfqws. Обнаруживается и корректный на вид обрыв между пакетами,
  в том числе вместе с повреждённой записью обновляемого пакета.
- Проверенный v4-снимок сопоставляется с текущими control/list. Здоровые чужие
  записи сохраняются побайтно, включая новые версии, составные флаги и
  многострочные поля. Если запись потеряна, она возвращается только при
  совпадении версии и сохранившихся данных пакета. Неоднозначная/новая версия,
  утраченные info или неизвестные фрагменты не заменяются предположениями.
- База публикуется атомарно под существующим lock opkg, который освобождается
  до запуска самого opkg. Права/владелец существующего status сохраняются;
  отсутствующего — возвращаются из проверенного снимка. Данные list не
  загружаются целиком, обход ограничен каталогом opkg/info и пределами объёма.
- Это аварийное восстановление в существующем откате, не новый допуск к
  обычным изменениям. Ручной restore и legacy v2/v3 не менялись. Структурное
  восстановление не гарантирует актуальности потерянных последних флагов,
  Conffiles и внешних изменений той же версии; это отмечается в диагностике.
- PASS с первого объединённого прогона: 163 сценария / 6425 assertions,
  включая 12 новых групп тестов; обработчик nfqws скомпилирован. IPK не
  собирался, роутер/службы/commit/push не затронуты. Подписи релиза остаются
  открытыми до согласования доверенного ключа и подключения CI-signer.

### Файлы установщика nfqws при откате — локально 08.09, ещё не установлено

- Снимок перед обновлением дополнен четырьмя известными файлами вне обычного
  payload: активный `nfqws2.conf`, прежние `nfqws2.conf-old` и `user.list-old`,
  служебный `nfqws2_install_type`. После переустановки прежнего IPK возвращаются
  исходные байты/права либо исходное отсутствие файла. Старый backup не теряется
  при повторной миграции; созданный установщиком маркер не остаётся после отката.
- Это дополнительный v4/v3-снимок, а не новое условие допуска к обновлению.
  Если расширение недоступно, остаётся прежний снимок файлов. Ручной restore,
  восстановление временно убранного init и обслуживание runtime/PID не изменены.
  Каталоги и посторонние соседние файлы не удаляются.
- Список основан на сохранённых скриптах nfqws2-keenetic 1.2.3; произвольные
  изменения будущих сторонних scripts не объявляются автоматически покрытыми.
- PASS: 151 сценарий / 4864 assertions в целевой component-recovery suite,
  компиляция обработчика nfqws, проверка включения тестов в CMake 6/6.
  Это локальные C++17-проверки, не испытание отключения питания на устройстве.
  IPK, роутер и публикация релиза этим изменением не затронуты.
- На этом этапе реконструкция общей базы opkg была отложена до отдельного
  подтверждения риска. После подтверждения реализована записью выше;
  прежний откат одной записи opkg сохранён.

### Завершение кодов ошибок конфигурации — локально 08.09, ещё не установлено

- Все 123 оставшиеся ветки проверки конфигурации получили явные коды:
  интерфейсы/VPN, списки и источники загрузки, группы URLTEST/conntrack,
  fwmark/таблицы, DNS и отдельные enum/cron. Добавлены 46 кодов; остальные
  причины переиспользуют существующие. Полное покрытие config/DNS/JSON —
  93/93, дополнительно сохранены 34 кода nfqws предыдущего среза.
- RU/EN пояснения помогают исправить настройку; исходные подробности и
  legacy-ответы сохранены. Добавлено отображение причины необходимости
  миграции в существующем окне ошибок, без изменения приоритетов recovery.
- Сравнение исходного и итогового кода подтвердило неизменность условий,
  сообщений, путей, порядка и допустимых значений. Нет новых проверок,
  блокировок, повторов или изменений runtime. Новые params содержат только
  фиксированные пределы; пропуск code у private helper выявляется при компиляции.
- PASS: 404 backend cases / 3830 assertions, 1772 frontend tests / 10959
  assertions, lint/i18n/typecheck/WebUI build, CMake coverage 6/6.
  IPK не собирался, сервисы и роутер не затронуты.

### Понятные ошибки JSON — локально 08.09, ещё не установлено

- Синтаксис JSON, переполнение числа, неверный тип, отсутствующее поле,
  корневой объект и прочие ошибки чтения значения получили шесть стабильных
  кодов. Они определяются по типу/id исключения, не по английскому сообщению.
- Коды передаются из конфигурации и JSON-ошибок запросов настройки списков,
  каталога и backup. UI объясняет причину и действие на RU/EN; исходные
  подробности и приоритет сообщений восстановления сохранены. Backup
  по-прежнему не раскрывает дополнительные токены входного документа.
- HTTP status, прежние error/path/reason, catch fallback, комментарии в
  конфигурации, миграция, допустимые значения и порядок действий не менялись.
  Новых проверок, запросов, повторов, params и изменений runtime нет.
- Локальные проверки: 388 cases / 2638 assertions в config target,
  49 cases / 640 assertions в API-errors target (общие serializer cases
  пересекаются); три production handler object скомпилированы. Frontend:
  1724 tests / 9023 assertions, lint/i18n/typecheck/WebUI build; после уточнения
  текста повторены профильные тесты, i18n и build. IPK и роутер не обновлялись.

### Понятные ошибки конфигурации nfqws — локально 08.09, ещё не установлено

- 33 существующие причины проверки nfqws и отказ nfqws2 --dry-run передаются
  стабильными кодами. UI показывает RU/EN пояснения, что исправить; исходная
  диагностика остаётся в подробностях. Новых условий принятия настроек нет.
- Исправлена передача ошибок в окнах применения стратегии и сохранения файлов:
  структура больше не теряется при превращении ошибки в технический текст.
  Настройки используют те же пояснения; отдельное сообщение о сохранённых
  изменениях при неудачном перезапуске сохранено.
- Разбор конфигурации, допустимые значения, сообщения/пути/порядок ошибок,
  аргументы запуска, PPE и работа сервиса не менялись. Старые ответы без кодов
  совместимы; новые коды не передают пользовательские данные в params.
- PASS: 47 backend cases / 608 assertions и компиляция production handler;
  1711 frontend tests / 8618 assertions, lint/i18n/typecheck/WebUI build;
  CMake target coverage 6/6. IPK не собирался, роутер и сервисы не затронуты.

### Коды ошибок портов, адресов, DNS и имён — локально 08.09, ещё не установлено

- Ещё 22 причины валидации получили стабильные коды: порты и диапазоны,
  адреса, Unicode-имена, DNS адреса/порты/скобки, доменные привязки и шаблоны,
  URL схемы и отдельные причины резервного маршрута. UI использует существующие
  переводы RU/EN независимо от диагностического текста; старые ответы совместимы.
- Код DNS-ошибки проходит из парсера через конфигурацию в общий сериализатор.
  Прежние сообщения what(), пути, порядок и допустимые значения сохранены.
  Для ограничения имени передаётся только max=80, без пользовательских значений.
  Условия сохранения и применения, runtime и API schema не менялись.
- PASS: 379 backend cases / 2421 assertions; 1668 frontend tests / 7832 assertions,
  lint/i18n/typecheck/WebUI build; CMake target coverage 6/6. Проверка producer/DTO
  включена в config target; узкий API-errors target не получил новых зависимостей.
  IPK и роутер не обновлялись. Оставшиеся legacy-ветки, nfqws и ошибки JSON
  этим срезом не объявляются полностью перенесёнными.

### Починка записи opkg перед откатом nfqws — локально 08.09, ещё не установлено

- Исправлен порядок восстановления после прерванного обновления: оборванная
  однозначно определённая запись nfqws в status восстанавливается из существующего
  v4 снимка до запуска прежнего IPK. Это устраняет ситуацию, когда повреждённая
  запись мешает переустановке, а починка metadata ждёт её успешного завершения.
- Чинится только запись нужного пакета; новые записи и изменения остальных
  пакетов, форматирование, права и владелец текущей базы сохраняются. Исправная
  база не переписывается. Штатный lock отпускается до запуска scripts/opkg.
- При утрате общей базы или повреждении чужих записей сохраняется возврат
  файлов и службы без ложного сообщения о полном восстановлении opkg.
  v2/v3, ручное восстановление, обычное обновление и UI не изменены;
  новых подтверждений или runtime-блокировок не добавлено.
- PASS: 146 component recovery cases / 4625 assertions, включая 11 новых.
  Обработчик nfqws скомпилирован отдельно; полный HTTP набор и настоящий opkg
  не запускались. IPK и роутер не обновлялись, commit/push не выполнялись.

### Предпросмотр списков из URL и каталога — локально 08.09, ещё не установлено

- В редактор URL/шаблона и настройку каталога добавлена необязательная проверка
  источника: уникальные IPv4/IPv6/домены, повторы, нормализованные примеры и
  ошибки с номером строки. Сохранение и применение не требуют этой проверки;
  конфигурация, кеш списков и действующая маршрутизация не меняются.
- Общий C++ parser используется и в preview; ошибки и результаты переведены
  на RU/EN. Изменение источника, маршрута или ревизии сбрасывает результат,
  а поздний ответ не подменяет текущую форму. URL загружается по действующему
  маршруту обновления списков либо явно выбранному VPN, без скрытого direct fallback.
- Анализ ограничен 2 MiB и 50 000 строками; показываются до 50 примеров и ошибок.
  У всех попыток скачивания общий лимит 10 секунд. Частичный результат явно
  обозначен; слишком длинная строка останавливает проверку, как runtime reader.
  SRS и JSON/YAML не принимаются за обычный текстовый список; их поддержка
  этим срезом не объявляется. Обычный refresh источников не изменён.
- PASS: профильный C++17 target 32/32, включая 21 новый тест; frontend 1666/1666,
  lint/i18n/typecheck/WebUI build; CMake target coverage 6/6.
  IPK и роутер не обновлялись, commit/push не выполнялись.

### Восстановление записей opkg внешнего пакета — локально 07.09, ещё не установлено

- Снимок nfqws перед обновлением включает запись пакета в status и стандартные
  info-файлы, включая `.list`, `.control`, conffiles и maintainer scripts.
  Новый manifest v4 хранит их в том же поколении, что и прежние файлы пакета.
- Откат возвращает служебные файлы и только запись нужного пакета в текущей
  базе opkg, сохраняя изменения остальных пакетов и многострочные поля.
  Новые записанные info-файлы этого пакета удаляются, если прежде отсутствовали.
  Используется штатная POSIX-блокировка opkg; его общий lock-файл не удаляется.
- Ручное восстановление metadata не меняет. Старые снимки совместимы;
  недоступность дополнительного снимка не блокирует обычное обновление.
  Ошибка metadata не препятствует восстановлению прежних файлов и runtime.
  Результат переустановки IPK больше не называется доказанным возвратом metadata
  на основании одной строки версии.
- Полная утрата/повреждение общей базы opkg, нестандартные info-файлы,
  неперечисленные эффекты maintainer scripts и подписи этим не закрыты.
  IPK и роутер не обновлялись.
- PASS: 135 component recovery cases / 3301 assertions и 6 тестов покрытия
  CMake targets. Обработчик и API tests скомпилированы отдельно;
  полный API executable не запускался.

### Откат файлов внешнего пакета без уцелевшего opkg list — локально 07.09, ещё не установлено

- Перед preinst/unpack nfqws список файлов проверенного нового IPK добавляется
  в существующий снимок восстановления. Manifest v3 хранит и прежнее содержимое,
  и отсутствие новых путей; пользовательский файл на совпавшем пути сохраняется.
- При откате после переустановки точного старого IPK сначала возвращаются прежние
  файлы, затем удаляются только записанные отсутствовавшие файлы нового payload.
  Потеря нового `.list` этому не мешает; соседние файлы и каталоги не удаляются.
  Старые v2 снимки совместимы, ручное восстановление остаётся без удаления.
- Неудачный разбор payload не блокирует обычное обновление: остаётся прежний
  файловый снимок. Неопределённое завершение команды использует существующий
  статус операции; дополнительного журнала или фонового процесса нет.
- PASS: 116 component recovery cases / 2393 assertions, 6 тестов покрытия
  CMake targets. Обработчик и API regression скомпилированы отдельно;
  полный API executable здесь не запускался. IPK/роутер не обновлялись.
  Полное восстановление/доказательство opkg metadata, пути только из maintainer
  scripts и подписи пакетов остаются отдельным незавершённым объёмом.

### Сохранение дополнительных полей конфигурации — локально 07.09, ещё не установлено

- Обычное typed load/edit/save сохраняет неизвестные поля на каждом объекте
  конфигурации, включая null, пустые объекты/массивы и вложенные значения.
  Изменённые известные настройки имеют приоритет; очистка поля или удаление
  VPN/правила не возвращает старые данные. Нет слияния массивов по индексам.
- Исправлены формы DNS, маршрутов, VPN/групп, списков и внутренних VPN:
  расширения остаются у своей записи при изменениях и перестановке. Метаданные
  известных ключей генерируются из OpenAPI. Смена типа/источника сохраняет
  прежнюю очистку несовместимых подразделов.
- При удалении VPN связанная политика загрузки списков очищается без потери
  остальных полей list_refresh. Дополнительные данные хранятся только в самом
  объекте; новых файлов состояния, служб или runtime-проверок нет.
- Снятые с поддержки неизвестные настройки сохраняются как непрозрачные данные,
  но не получают прежнюю runtime-семантику. Это не возвращает старый WhatsApp
  TCP-reset. DTO авторизации и других запросов вне графа ConfigObject не менялись.
- Локальный функциональный P0 миграций завершён вместе с предыдущим срезом.
  IPK, установка, restart, commit и push не выполнялись.
- Проверки: focused backend C++17/GCC13 с Keenetic API — 307 cases / 2385
  assertions; frontend — 1652 / 7598; генераторы — 19 / 49. Lint (одно прежнее
  предупреждение), i18n/literals, TypeScript, WebUI build и воспроизводимость
  codegen — PASS. Это не GCC8/router-приёмка.

### Миграции старых настроек и резервных копий — локально 07.09, ещё не установлены

- В v1→v2 добавлены проверенные по истории форматы: числовые uint32-метки
  `fwmark.start/mask` → hex; строка `dns.fallback` → массив;
  `dns.test_server` → `dns.dns_test_server`. Современное поле имеет приоритет,
  неверные типы не исправляются догадками. Три синтетических fixture сохраняют
  маршруты, DNS и метки при повторной загрузке; происхождение документировано.
- Частичные копии мигрируются по версии источника до слияния с текущими
  настройками. Необязательный `config_schema_version` сохраняется даже в
  DNS-only архиве; старые копии совместимы, версия backup остаётся 1.
  WebUI больше не отбрасывает версию источника при чтении и отправке файла.
- Backup/restore сохраняет неизвестные вложенные поля, null и пустые
  объекты/массивы. Выбранные разделы заменяются точно, удалённые элементы
  не возвращаются; при ошибке применения восстанавливаются исходные байты.
- Обычное сохранение неизвестных полей через typed Config/редактор на этом этапе
  оставалось следующим слоем; теперь завершено отдельным срезом выше.
  Новый runtime-контур или подтверждения не добавлены.
  IPK, установка, restart, commit и push не выполнялись.
- Проверки: focused host C++17/GCC13 с Keenetic API-веткой — 291 cases /
  1991 assertions; frontend — 1638 tests / 7543 assertions. Lint (одно прежнее
  предупреждение), i18n/literals, TypeScript, WebUI build и воспроизводимость
  API codegen (404 файла) — PASS. Это не router/GCC8-приёмка.

### Ручная HTTPS-проверка маршрута — локально 07.09, ещё не установлена

- В закрытых подробностях routing-test добавлена кнопка HTTPS-проверки одного
  IP. Свежий активный маршрут задаёт полную метку и интерфейс; выбранный IP
  закреплён, а исходное имя сохраняется для Host/SNI и проверки сертификата.
- Один HEAD к корню сайта на порту 443: без редиректов, загрузки тела,
  повторов и фоновых задач. Максимум 5 секунд в общем дедлайне диагностики;
  16 КиБ заголовков. Обычные загрузки и URLTester сохраняют прежнее поведение.
- Отдельные code/reason и RU/EN для HTTP-ответа, TLS, connect, timeout,
  изменившегося DNS и отсутствующего пути. HTTP 403/405/3xx — полученный ответ,
  не подтверждение работы сайта; запрос с роутера не доказывает путь клиента
  через PREROUTING/nfqws. Показаны свежие route-поля и измеренные времена.
- Основная диагностика не исчезает во время запроса; поздние ответы не
  попадают в новый снимок. Учтены эквивалентные записи IPv6 и старый API.
  Схема/C++/TypeScript обновлены без новых блокировок пользовательских действий.
- Focused C++17/GCC13: 53 cases / 929 assertions — PASS; внешний network-case
  исключён, transfer tests используют только локальный loopback. Это локальная
  проверка, не подтверждение новой версии на Keenetic GCC8/роутере.
- Frontend: 1634 tests / 7513 assertions — PASS; lint (одно прежнее
  предупреждение TanStack Virtual), i18n (3081 RU/EN-ключ), literals,
  TypeScript и production WebUI build — PASS. API/daemon integration —
  syntax-only, не runtime-приёмка handler; генераторы API воспроизводимы.
- IPK, установка, перезапуски, commit и push не выполнялись.

### Счётчики правил маршрутизации — локально 07.09, ещё не установлены

- В закрытых подробностях routing-test — реальные накопленные пакеты/байты
  выбранного физического PREROUTING-классификатора, его таблица, цепочка,
  позиция, семейство и время. Это трафик всего правила, не конкретного сайта.
- Ограниченный read-only снимок iptables/ip6tables или nftables использует
  captured realized rule/backend/RAW/mask; не смешивает неактивное поколение,
  другой rule_index с той же меткой и соседние служебные счётчики.
- RU/EN, optional API и точные uint64 десятичные строки. Недоступность и
  неоднозначность не подменяются нулём или статусом «VPN не работает».
  Никаких новых блокировок действий, фонового опроса и сброса счётчиков.
  Обычная проверка DNS/FIB не теряется при неудаче чтения счётчиков.
- Изменения локальные, CLI/IPC не расширялись, IPK не собирался.
- Проверены API projection/connections history (27 тестов) и frontend
  (1621 тест); RU/EN, lint, literals, TypeScript и WebUI build прошли.
  API воспроизводимо сгенерирован. Daemon API/core и HTTP handler с прежними
  HTTP-тестами прошли syntax-only; это не router/GCC8-приёмка.
- Focused firewall gate: 390 тестов / 1937 проверок — PASS, включая 9 новых
  counter cases и прежние verifier/iptables/nftables builder regressions.

### Правила выбора таблицы маршрутизации — локально 07.09, ещё не установлены

- Закрытые подробности routing-test теперь включают policy rules ядра:
  семейство адресов, приоритет, таблицу, метку/маску и время снимка. Это
  кандидаты, а не обещание выбранного пути или доступности сайта.
- Переиспользован raw RTM_GETRULE codec: отдельное read-only чтение без
  shell-команд и writer lease. Один снимок на нужное семейство, до 4096 строк
  и секунды на чтение; до 32 кандидатов на IP. Недоступность/обрыв чтения
  отделены от пустой таблицы. Условия изменения маршрутов не менялись.
- Полная маска правила учитывает сторонние биты mark; неполно представленные
  selectors/actions/inversion не выдаются за несовпадение. Порядок приоритетов
  не подменяет FIB: поиск внутри таблицы может продолжиться по другим правилам.
- RU/EN и необязательное поле OpenAPI, штатная генерация моделей, без новых
  запросов/опроса UI. CLI/IPC этим срезом не расширены. Счётчики правил и
  HTTP-фаза конкретного пути остаются отдельным остатком. IPK не собирался.
- Локальная проверка: kernel routing 63 теста, API projection/connections
  history 21 тест, frontend 1615 тестов — PASS; генерация API воспроизводима,
  RU/EN, lint, literals, TypeScript и сборка WebUI прошли. HTTP handler и
  прежние HTTP-тесты проверены syntax-only. Router/GCC8-приёмки не было.

### Подробная цепочка маршрута — локально 07.09, ещё не установлена

- В существующий routing-test добавлены источник DNS/явный резолвер,
  точные индексы совпавших правил и маска активной конфигурации. Прежние
  маршруты, правила, условия совпадений и значение `ok` не менялись.
- Текущие соединения к проверяемым адресам берутся из прежнего ограниченного
  `/proc`-кеша: не более 64 строк, точное IPv4/IPv6-сопоставление, без выдачи
  исчезнувшей истории за свежее наблюдение. Усечённый/недоступный снимок
  обозначается явно; DNS-журнал, NDMS и внешние команды не вызываются.
- В прежнем результате добавлены закрытые RU/EN подробности: DNS, список,
  правило, метка, таблица/интерфейс и исходные соединения. Сравнение учитывает
  только биты маски keen-pbr-sb. Нет записи — не значит VPN не работает;
  локальный FIB и соединение не выдаются за доказанный ответ сайта.
  Старой карточки пути, новых WebUI-запросов и фонового опроса нет.
- Поля OpenAPI необязательны, API-модели обновлены генераторами. Счётчики
  меток, отдельное свидетельство policy rule и привязанная HTTP-фаза остаются
  следующим расширением диагностики. IPK и установленная версия не менялись.

Проверки: 32 профильных C++17 cases / 728 assertions и 1610 frontend tests /
7120 assertions PASS; lint/i18n/literals/typecheck/WebUI build PASS.
Генерация API воспроизводима. HTTP-набор не запускался; его handler и
регрессия прошли syntax-only C++17. Локальный WebUI:
`build/ui-routing-evidence-20260907`, не пакет для установки.

### Структурированные ошибки полей API — локально 07.09, ещё не установлены

- К `validation_errors` добавлены необязательные code/params. 19 основных
  причин конфигурации получают код непосредственно в валидаторе: IP/CIDR,
  версия формата, тип/диапазон значения, техническое имя и отсутствующая ссылка
  на список, VPN или DNS-сервер. Старые path/message, статусы и условия отказа
  не меняются; строковые числовые параметры сохраняют точность вплоть до uint64.
- Общий сериализатор сохраняет эти поля в ответах конфигурации, backup,
  каталога/мастера и nfqws. Клиенты форм и nfqws больше не отбрасывают metadata.
  Известный код определяет RU/EN-текст независимо от формулировки диагностики;
  неизвестный код показывает общее сообщение с подробностями. Для старого
  сервера без кода сохранён прежний перевод. Номер строки IP/CIDR и текст
  пользователя остаются на месте. Новых подтверждений или runtime-блокировок нет.
- OpenAPI, C++ и TypeScript API-модели обновлены генераторами с проверкой
  воспроизводимости. Использованы прежние переводы, новых зависимостей нет.
  Специализированные port/address/DNS/name/nfqws и parser-причины ещё частично
  остаются legacy; полная миграция всех ошибок этим слоем не объявляется.

Два профильных C++17 набора: 357 cases / 1890 assertions PASS. Frontend:
1598 tests / 7021 assertions, lint/i18n/literals/typecheck/WebUI build PASS;
остались прежние предупреждения TanStack Virtual и старого range-loop теста.
Четыре HTTP handlers и расширенная API-регрессия прошли syntax-only;
монолитный HTTP test target не запускался. IPK и роутер не изменялись.

### Импорт IP/CIDR — локально 07.09, ещё не установлен

- Для ручных, файловых и кешированных URL-списков используется одна
  нормализация IPv4/IPv6: адрес сети вместо host bits, компактный IPv6,
  `/32` и `/128` как отдельные адреса. `/0` и частные сети остаются допустимыми.
- При сохранении нового или изменённого ручного списка канонические повторы
  удаляются с сохранением порядка. Ошибочные значения получают RU/EN-причину
  с номером исходной строки, выделением и прокруткой к ней; введённый текст
  сохраняется. Поздний ответ не перемещает курсор поверх новых правок.
  Подробный исходный ответ доступен в закрытых подробностях.
- Неизменённый старый IP-список не мешает редактировать другие настройки.
  Некорректный новый ввод не создаёт черновик и не запускает изменение runtime.
  Неоднозначные IPv4 с ведущими нулями теперь отклоняются; в потоковых источниках
  они пропускаются как некорректные, даже если старый конфиг сохранён без изменения.
- Подсчёт записей URL-списка остаётся по вхождениям: обновление не вызывает
  ложного shrink rejection только из-за канонизации. Предварительный UI-разбор
  ошибок удалённого источника этим срезом не добавлен. Новых блокировок,
  журналов восстановления, владельцев runtime или зависимостей нет.

Профильные C++17 tests: 455 cases / 2628 assertions PASS. Frontend: 1588 tests /
6889 assertions; lint, RU/EN, literals, typecheck и локальная сборка WebUI PASS
(прежнее предупреждение TanStack Virtual). API handler и добавленная HTTP-регрессия
прошли syntax-only; монолитный API test target здесь не запускался. Это локальная
реализация, не установка: IPK, действующая конфигурация и службы роутера не менялись.

### Три следующих среза и перевод диагностики — локально 07.09, ещё не установлены

- Добавлен первый слой версионированной конфигурации: отсутствие
  `schema_version` трактуется как v1, текущий формат — v2; последовательная
  миграция сырого JSON выполняется до валидации. Сейчас переход добавляет
  только маркер и сохраняет прежние поля. Конфигурация из будущей версии
  получает отдельную ошибку, объяснённую в WebUI на RU/EN. Новые шаблоны
  содержат актуальный маркер, старые входные конфиги продолжают приниматься;
  CLI понимает ответ старого демона во время перехода между версиями.
  Схема и оба generated API обновлены штатно, defaults required scalar полей
  берутся из схемы. Исторические sb.N fixtures и полноценный lossless backup
  этим слоем ещё не закрыты; экспорт неизвестных потенциально секретных полей
  не расширялся.
- Восстановление прерванного обновления внешнего компонента использует
  сохранённый точный прежний IPK даже при ещё совпадающих версии и SHA
  бинарника. Это позволяет существующему installer восстановить metadata
  `opkg`, которые unpack мог изменить раньше бинарника. Обычное обновление,
  действующая операция и уже подтверждённый результат не меняются. Новых
  owners, журналов или подтверждений перед действиями нет. Полная очистка
  orphan new paths и подписи остаются отдельным остатком.
- Десять legacy-пулов nfqws (6 QUIC и 4 UDP) получили настоящую круговую
  ротацию двух различающихся вариантов. Исходное первое действие сохранено,
  второе взято из поставляемых профилей. Независимые ключи, границы наблюдения
  и действий проверены, включая поздний QUIC Initial: оба варианта не
  исполняются одновременно. Закрыты только соответствующие десять исключений;
  остальные восемь замечаний к assets не изменены.
- Ошибка `runtime routing inventory is not authoritative; waiting for
  reconciliation` переведена в понятное RU/EN объяснение на дашборде. Исходный
  текст доступен в закрытых технических подробностях; незнакомые ошибки тоже
  не выводятся как основной английский текст. Поддержаны и старый HTTP 200/error,
  и текущий HTTP 500. Это не скрывает оставшийся сбой маршрутизации.

Профильные проверки: config 222/907, recovery 10/76 (cases/assertions),
generator defaults 3/3, assets 23 Python tests и pinned Lua smoke PASS.
Общий frontend gate: 1581 тест / 6838 assertions, lint/i18n/literals/typecheck
и UI build PASS. Реальные IPK/opkg и роутер не затрагивались; дополнительная
сборка отложена пользователем. Правка планировщика остановленного VPN также
остаётся локальной и должна войти в будущую общую поставку.

### Исправление shared sing-box и белого экрана — поставка 07.09

IPK `3.3.0-20260907104009` установлен на Keenetic. Размер 7 657 447 байт,
SHA256 `29169b10908f79e5a1c440425863e482efba96bc6c6e6a006a94d6ad3f3eea8f`.
Подтверждены состав и свежие бинарники, WebUI/source-id/gzip, первый startup
`Overall: OK`, DNS youtube.com/2ip.io, неизменность config/transports/auth и nfqws.
В пакет также вошли описанные ниже три локальных P2-среза 07.09.
После возврата ранее остановленного MyPN #2 в down общий sing-box остаётся
стабильным более пяти минут при двух ошибающихся Hysteria2. Однако обнаружена
третья причина того же инцидента: down-интерфейс с `strict_enforcement=false`
оставляет пустое policy rule и блокирует восстановление маршрутов других VPN.
Правка этого дефекта планировщика готова локально: правила создаются только для
семейств с маршрутом в своей таблице, stable table IDs и strict kill-switch
сохранены. 29 C++17-сценариев / 176 утверждений PASS (host GCC13). В установленный
IPK она ещё не входит; дополнительная поставка отложена владельцем ради
накопления нескольких локальных срезов. До нового разрешения IPK не собирается.
Весь инцидент не объявлен закрытым.

- Недоступность внешнего сервера или ошибка проверки отдельного участника
  больше не перезапускают общий sing-box вместе с исправными VPN. Статус
  проблемного участника остаётся видимым; смерть процесса и исчезновение TUN
  по-прежнему запускают существующее восстановление, правила восстанавливаются
  без принудительной замены работающего процесса.
- Routing health с ошибкой возвращает HTTP 500 вместо успешного ответа без
  обязательных массивов. WebUI также понимает ошибку в HTTP 200 от прежнего
  сервера и показывает существующий блок диагностики, не обрушая дашборд.
- Регрессия WebUI использует точный ответ инцидента; общий frontend gate:
  1574 теста, 6761 утверждение, typecheck/i18n/build PASS. Профильные Go-тесты
  проверяют неисправный сервер рядом с исправным VLESS, повторное чтение
  статусов, ручное отключение и сохранение восстановления процесса/TUN.
- Это исправление двух подтверждённых дефектов, без новых mutation guards,
  новой системы восстановления, изменения DNS или конфигураций VPN.
  Проверка контракта backend: 13 C++17-сценариев / 171 утверждение PASS;
  production собран Keenetic GCC 8.4. Новых проверок перед действиями нет.

### Локальные метрики отдельно от KeeneticOS — локальный срез 07.09, ещё не установлен

- Новый read-only `GET /api/system/metrics` получает CPU, RAM, температуру,
  диск, uptime/load average и счётчики conntrack из локальных proc/sysfs/statvfs.
  В этом пути нет RCI, запуска shell-команд или сканирования таблицы соединений.
  Используется один секундный снимок для параллельно открытых панелей,
  без нового фонового потока и накопления истории в памяти.
- CPU считается по разности счётчиков; до первой пары замеров процент
  отсутствует. Ошибка/сброс счётчика не превращаются в ложные 0% или 100%.
  Отсутствующие датчики/поля не заменяются нулями; ошибка отдельного локального
  источника не скрывает остальные показатели.
- WAN и число клиентов получают независимые 60-секундные кэши и сохраняют
  последние успешные сведения при ошибке своего источника. Версия/модель
  используют прежний общий typed NDMS cache; `/show/system` больше не нужен.
  Отказ hotspot не замораживает свежие данные WAN и локальные CPU/RAM/uptime.
- Карточка остаётся одной: локальный запрос обновляется каждые 15 секунд,
  сведения KeeneticOS — раз в минуту. Локальные цифры появляются и обновляются
  независимо от медленного/неудачного NDMS-ответа; поздний ответ не откатывает
  свежие метрики. Старый `/api/system/router` сохраняет совместимый полный ответ
  для существующих клиентов API и диагностических инструментов.
- Срез не меняет маршрутизацию, сервисы, авторизацию или mutation owners.
  IPK, установка/перезапуск служб и commit/push не выполнялись. Событийное
  обновление новых кэшей через NDMS hooks остаётся отдельным пунктом roadmap.
- Локальная проверка: 20 C++17-сценариев / 170 утверждений PASS, отдельно
  скомпилирован production handler. При синтетическом опросе раз в 15 секунд
  подтверждены 5 RCI-запросов в минуту с существующим version TTL, включая
  холодный старт. Это модель вызовов, не измерение на Keenetic.
  Frontend: 1570 тестов / 6742 утверждения, typecheck, i18n и WebUI build PASS;
  обе генерации API воспроизводимы. Прежнее предупреждение TanStack Virtual
  сохраняется; новых ошибок lint нет.

### Большие таблицы списков — локальный срез 07.09, ещё не установлен

- Таблица списков получает краткие записи через `POST /api/lists/query`,
  страницами по 50. Поиск по именам, источникам и зависимостям выполняется по
  всему набору до сортировки и разбиения на страницы, включая русский текст.
  Удаление записей с последней страницы корректирует её границы.
- На компьютере и телефоне большие страницы отрисовывают только видимые строки
  и небольшой запас; маленькие страницы сохраняют обычное отображение.
  Выбор сохраняется по стабильным ID при смене страницы и сортировки;
  «выбрать все» действует на текущую страницу. Поиск очищает выбор, как раньше.
  Строка с клавиатурным фокусом не удаляется при прокрутке за пределы экрана.
- Зависимости запрашиваются для текущей страницы. Удаление, импорт, экспорт и
  редакторы по-прежнему используют полную конфигурацию; краткая страница не
  подменяет её в кэше. Общие блоки панели пока также загружают полный конфиг:
  этот срез сокращает работу таблицы/DOM, но не устраняет эту общую загрузку.
- Новый запрос только читает существующий снимок конфигурации/черновика:
  без runtime owner, повторного ввода пароля, журнала операций или новых
  блокировок сохранения. Другие таблицы с перестановкой строк не менялись.
- Проверены согласованность OpenAPI/генерации, 11 профильных C++17-сценариев
  (108 утверждений) и frontend gate: 1563 теста, typecheck, i18n и WebUI build.
  Изолированный браузерный прогон с 300 синтетическими списками прошёл для
  desktop/mobile: страницы, поиск, выбор, прокрутка с фокусом и смена ширины.
  Найденный цикл измерения скрытых строк при resize исправлен; повторный
  браузерный прогон, девять профильных тестов и итоговый WebUI build PASS.
  ESLint — без ошибок, одно предупреждение React Compiler о TanStack Virtual.
  IPK, установка/перезапуск служб и commit/push не выполнялись.

### Пакетная очистка ipset — локальный срез 07.09, ещё не установлен

- Очистка объединяет известные и найденные на роутере наборы без повторов:
  один `ipset restore` вместо отдельных `flush`/`destroy` для каждого набора.
  Inventory читает только имена, а не содержимое всех адресных списков.
- Если пакетная команда встретила ошибку, сохраняется прежнее поштучное
  продолжение. Ошибка чтения inventory не мешает очистить уже известные наборы;
  чужие наборы и сохраняемые динамические DNS-наборы не включаются в live sweep.
- Применение только правил (`RulesOnly`) больше не читает заголовки ipset дважды.
  Публикация A/B, наполнение наборов, NAT, откат и итоговая проверка остановки
  не менялись. Новых блокировок действий, API или журналов операций нет.
- При полном inventory ещё не созданные staged-наборы исключаются из очистки,
  чтобы отсутствующий набор не прерывал пакет. Необычные имена сохраняют
  поштучный argv-путь; прежние таймауты обычной и строгой очистки сохранены.
- Итоговый профильный C++17-прогон iptables/firewall runtime: 253 теста,
  1356 проверок, без ошибок; девять новых сценариев и обновлённые pure fixtures.
  Проверяется в том числе один restore для десятков наборов вместо 2×N команд.
- IPK не пересобирался, установка/перезапуск служб и commit/push не выполнялись.
  Реальное сокращение времени применения на Keenetic этим срезом не измерялось.

### Срез по ревью 06–07.09 — включён в 3.3.0-20260906231955

Ниже сохранена история локальных исправлений и их точечных проверок.
Пометки об отсутствии сборки/установки относятся к соответствующим этапам;
актуальный статус объединённой поставки указан выше.

- C5 (07.09): legacy nftables восстанавливает conntrack marks всех VPN,
  остающихся в текущей конфигурации, а не только выбранных прямо сейчас.
  После здорового переключения группы A -> B существующие соединения через A
  сохраняют прежний путь; новые классифицируются через B. Это действует в
  PREROUTING и OUTPUT, включая DNS. Набор пересоздаётся при каждом применении:
  прошлые поколения и удалённые outbounds в него не накапливаются. Маска и
  чужие биты marks сохранены; очистка удалённых VPN, iptables/RAW и современный
  nft RegisterMerge не менялись. Новых owners, журналов или API нет.
  Один локальный nftables/iptables/runtime gate: 359 cases / 1906 assertions,
  включая четыре новые регрессии. IPK и роутер этим срезом не изменены.
- Межслойное ревью C1–C4 (07.09): правила списков, правила по адресам/портам
  и DNS-detour выбирают рабочий leaf группы отдельно для IPv4 и IPv6.
  Сохраняются метки отдельных VPN и существующее закрепление соединений;
  при отсутствии семейного резерва остаётся прежний закрытый маршрут группы.
  Диагностика, сравнение опубликованного состояния и точечное восстановление
  учитывают обе семейные метки. Обработка входящих IKEv2/OpenConnect не менялась.
  Опоздавший ответ подписки не заменяет VPN/метаданные, уже обновлённые другой
  попыткой или восстановленные из backup того же URL; время проверки берётся
  до HTTP. Переименование подписки и смена интервала обновления не мешают
  текущей загрузке. Первый отложенный REST-ответ больше не затирает свежий
  полный SSE-снимок интерфейсов, VPN и служб; частичные события трафика и
  REST fallback при отключении SSE сохранены. Четыре готовых BusyBox-регрессии
  DNS/install/uninstall подключены к штатному `make test` и release backend CI.
  Новых глобальных блокировок, recovery-журналов или подтверждений в UI нет.
  Локальная проверка: routing/firewall/FIB — 624 cases / 3699 assertions,
  подписки — 71 / 783, frontend — 1551 / 6644 и полный frontend gate;
  BusyBox — 46 сценариев, CI coverage — 6 тестов. Пять production units
  прошли WITH_API/Keenetic API syntax-only. GCC 8/IPK/роутер ещё не проверены.
- Ревью установки/удаления/компонентов/мастера L1–L7 (07.09): uninstall
  передаёт существующую блокировку дочерним init/prerm и при удалении данных
  очищает завершённые собственные rescue/IPK/config-recovery снимки. Данные
  отдельно оставленного nfqws и чужие файлы общего каталога сохраняются.
  Перед удалением пакета managed DNS conf-script заменяется самостоятельным
  inline direct/manual fallback; full/headless postinst возвращают только
  свой блок при обновлении/force-reinstall. Пользовательский DNS не заменяется
  публичным, active ipset/nftset не переносятся в резерв.
  Отказ обновления sing-box восстанавливает все адресованные исходно
  работающие VPN, включая потерянный Down-ответ. Только установочный Down
  имеет короткий request-bound context и восстановление после его terminal;
  поздняя остановка не побеждает ранний компенсирующий Up. Нового общего
  owner/journal нет; обычные ручные действия и shutdown не изменены.
  Повторный мастер меняет первое общее правило VPN/DNS, сохраняя исключения
  и остальные списки; смешанные правила разделяются, повтор не создаёт
  дубли. Совпадение нового primary и прежнего reserve нормализуется в BLOCK,
  без выбора постороннего VPN. Source-only изменения применяются по revisions.
  Ошибка настройки DNS возвращает предыдущее состояние override/config/runtime,
  а не безусловно выключает ранее включённый override.
  DNS-конфиг публикуется после подготовки соседнего candidate; частичная
  запись, отказ chmod/rename не портят прежний файл, symlink сохраняется.
  PASS: C++17 host 80 cases / 948 assertions, Go API 5 tests / 12 сценариев,
  BusyBox uninstall 12, installer DNS rollback 14 сценариев, ndmc environment;
  handler_transports syntax-only WITH_API; WebUI 1544/6597 и полный UI gate.
  IPK, настоящий installer, роутер, restart, commit/push не запускались.

- Ревью nfqws/DNS/native VPN N1–N4 (07.09): автоматический DNS-резерв теперь
  обновляется из прямых глобальных DNS последней успешно активированной
  конфигурации, включая подготовленный Keenetic snapshot. В STOP/полный
  fallback не копируются detour и ipset/nftset; частичный stream сохраняет
  прежний активный LKG. Ручной fallback conffile не перезаписывается,
  мигрирует только точный прежний Google default либо маркированный auto-файл.
  Неизменный файл не переписывается; bootstrap до первой активации — Quad9
  из штатного примера. При отсутствии прямого DNS старые upstream не
  возвращаются: требуется прямой DNS или ручной резерв.
  Ошибка одного интерфейсного VPN больше не снимает проверенный bypass
  другого; свежие привязки приоритетнее сохранённых, конфликтующий bypass
  исключён. Сервисный IKEv2/OpenConnect resolver не менялся.
  Ожидания stop/start dnsmasq без рабочего usleep остаются 5/3 секунды.
  Просмотр большого nfqws log читает хвост до 2 МиБ, отмечает усечение,
  показывает ошибку и повтор загрузки вместо пустого редактора; разворот
  строк не создаёт массив из миллионов записей. Лимит обычных файлов и
  сохранение редакторского черновика оставлены прежними.
  PASS: host C++17 327/2457; BusyBox fallback/timing по 10 сценариев и
  прежний Keenetic LKG; handler_nfqws syntax-only WITH_API+USE_KEENETIC_API;
  WebUI 1542/6592, lint/i18n/typecheck/build, diff-check. Новых общих owners,
  блокировок операций или пользовательских подтверждений нет. Полная
  backend/Go/IPK сборка, установка, restart, commit/push не выполнялись;
  это локальная проверка, не приёмка целевого Keenetic GCC 8.

- Повторное ревью S1–S4: следующая URLTEST-проверка больше не обесценивает
  уже применяемое асинхронное переключение группы. Его существующий probe
  освобождается после результата применения/отката; ожидающие проверки
  объединяются в один следующий проход. Isolated Restart ждёт завершения
  старого процесса и очистки его правил в существующем operation context,
  без ложного успеха после Kill и без нового дополнительного таймаута.
  Подтверждённая routing-health деградация shared-процесса теперь запускает
  согласованный Restart вместо пустого same-config Reconcile; прежние
  порог ошибок/backoff и обработка unknown/unavailable сохранены. Verifier
  iptables/nft не требует отсутствующих по замыслу правил для именованного
  пустого списка; реальные selector-only правила остаются проверяемыми.
  Новых глобальных блокировок, owners или запросов пользователю нет.
  Host-проверки PASS: URLTEST 33/405, routing/list/firewall 278/2175;
  focused Go transport 91 tests, затем только профильные 2 + 2 tests после
  уточнений остановки. Добавлены 3 URLTEST cases (4 ветки), 2 verifier cases,
  6 Go cases (10 сценариев). daemon_core/runtime C++17 + USE_KEENETIC_API +
  WITH_API syntax-only, gofmt/diff-check PASS. C++ проверен host GCC 13, не
  Keenetic GCC 8. Полный backend/frontend/IPK не собирались; роутер,
  установка, restart, commit/push не затронуты. Все четыре замечания
  реализованы локально; объединённая поставка/живая приёмка ещё впереди.

- Shared shutdown (дополнение к R7): остановка manager отменяет уже начатое
  shared-переключение до ожидания существующего mutex. Закрытие браузера
  по-прежнему не обрывает commit/rollback; лимиты 30 секунд для обычного
  перехода и 8 секунд для shutdown не увеличены. Validation, readiness,
  forwarding, его повторы и crash-cleanup учитывают отмену; новые процессы
  и rollback после Close не запускаются. Ссылка на незавершённого кандидата
  сохраняется до Stop/reaping; финальная очистка включает фактически
  затронутые старый/новый интерфейсы даже при неизменном tag. Новых глобальных
  owners, admission, журналов восстановления и пользовательских проверок нет.
  Focused Go transport/API/config/cmd — PASS, включая 7 новых shared shutdown
  tests (9 сценариев) и 3 forwarding cancellation tests с подслучаями;
  обычный rollback, BusyBox/xtables и isolated lifecycle сохранены.
  Gofmt/diff-check PASS. Это локальные тесты, не приёмка Keenetic. C++/WebUI/IPK,
  установка, restart, commit/push не выполнялись.

- R4: ручной release_tag теперь выбирает исходники, а не только имя места
  публикации. Один resolve-source фиксирует commit для всех jobs; обычный
  PR сохраняет event merge SHA. Релиз из alpha/next получает полную матрицу
  aarch64/mips/mipsel и не маркируется как alpha/next artifact. Перед
  изменением release assets проверяются remote tag, frozen SHA и все три
  IPK; release action получает тот же target_commitish. Версия, timestamp
  и commit ELF сверяются одной точной строкой, чистый SHA не совпадает с
  SHA-dirty/другим commit. Новых проверок или блокировок на роутере нет.
  Локально PASS: 45 Python tests, YAML parse, actionlint 1.7.7 (без внешних
  shellcheck/pyflakes), diff-check. Новые source/wiring tests работают на
  checkout workflow до перехода к старому тегу. Сравнение tag перед публикацией
  не объявляется атомарным с GitHub API. Реальный Actions run, IPK, установка,
  commit/push не выполнялись. Все 12 исходных замечаний реализованы локально;
  поставка/приёмка остаются открытыми; дополнительный shared-mode shutdown
  реализован следующим локальным срезом, описанным выше.

- R7/R10/R11: isolated transport-manager учитывает deadline при ожидании
  lifecycle mutex и отменяет начатый запуск при остановке самого manager,
  сохраняя независимость от закрытия браузера. HTTP drain не задерживает
  начало остановки процессов; общий 8-секундный бюджет не увеличен, сокеты
  зависших запросов закрываются по deadline. При отмене Up во время forwarding
  останавливается только его дочерний процесс; после успешного Up обычная
  отмена API context не выключает работающий VPN. Прежний best-effort stop
  свободного процесса при исчерпанном бюджете сохранён. Native WG/AWG/nfqws
  этот путь не выключает; существующая shared transition policy не менялась.
  Её отдельный 30-секундный context ещё может превышать общий shutdown budget;
  этот остаток явно оставлен открытым в локальном roadmap.
  Повторный preview подписки после отмены работает: локальный token/reset и
  promise-finally не зависят от отсоединённых mutation callbacks, старый ответ
  не подменяет новую форму. Успешный импорт завершает окно без ожидания семи
  refetch, используя общий с SSE набор обновляемых данных. Ошибки фоновых
  GET не отменяют успешный POST; ошибки импорта/частичные результаты остаются
  видимыми. Новых глобальных owners/locks/recovery-журналов нет.
  Проверки PASS: frontend 1534 tests / 6563 assertions, lint/i18n/typecheck/build;
  focused Go 12 top-level / 19 leaf scenarios в transport/API/cmd, включая
  реальные тестовые процессы. Это локальные host-проверки, не live-приёмка;
  C++/IPK не собирались, router/restart/commit/push не выполнялись.
  Из ревью локально реализованы 11/12 замечаний; открыта R4 (release ref).

- R2/R8/R9: пустой или отсутствующий именованный список больше не расширяет
  правило firewall до всего выбранного устройства/порта. Без списка
  selector-only правила работают как прежде. DNS и firewall используют
  одинаковые источники списка: текущий URL, файл и inline-адреса. Старый
  URL-кэш не возвращается после удаления/замены источника, но offline-кэш
  того же URL остаётся доступен. URL закреплён в существующем snapshot и
  учитывается вместе с body digest в fingerprint: одинаковый ответ нового
  URL не оставляет ошибочно пустые sets. Проверка целостности тела прежняя.
  Доступность IPv4/IPv6 gateways разделена: недоступное/выключенное семейство
  не снимает рабочий маршрут другого, включая URLTEST fallback, старт и
  reconcile. Default route проверяется по своему семейству; CLI использует
  один общий dump маршрутов. Дублирующий gateway helper удалён, новых
  background probes, locks, recovery-журналов и блокировок UI нет.
  Локально PASS: один узкий backend target 221/221 cases, 1949 assertions;
  daemon_runtime/cmd/status C++17 + USE_KEENETIC_API syntax-only. Host GCC 13,
  не целевой GCC 8 и не live-приёмка. Target исключён из стандартной сборки
  и CTest, чтобы CI не компилировал уже включённые в основной suite тесты
  повторно. Frontend/full backend/IPK не собирались; установка, перезапуск,
  commit и push не выполнялись. Следующие открытые замечания: R4/R7/R10/R11.

- R3/R6/R12: корзина VPN больше не применяет общий посторонний черновик и
  не оставляет удаление маршрута на последующий Save. Sing-box DELETE
  выполняет удаление транспорта и связанных маршрутов через существующую
  config/transport транзакцию; manager использует тот же conditional revision
  контракт, что create/update. UI отправляет один запрос, без промежуточных
  stage/save/restore. WG/AWG очищает только маршруты точного интерфейса под
  уже удерживаемым native writer; при определённом отказе до удаления
  возвращает маршрут и исходный черновик. Членство в группе по-прежнему
  требует явного изменения группы. Active и draft преобразуются отдельно:
  остальные правки остаются неприменёнными, текущий draft сохраняет
  возможность обычного Save, ранее устаревший не объявляется новым.
  После подтверждённого удаления sing-box очищается связь с подпиской, но
  источник/лимиты/известные серверы сохраняются. Refresh также убирает
  подтверждённо отсутствующие tags, не восстанавливая удалённый VPN; сбой
  чтения менеджера не считается удалением. Ошибка cleanup метаданных не
  отменяет успешное удаление и не останавливает маршрутизацию.
  Новых recovery-журналов, независимых locks и запросов пароля нет.
  Проверки PASS: ConfigStore 23/202, общий config commit 4/451, linked DELETE
  1/85 (успех, CAS-отказ и rollback), подписки 68/637, planning 8/53,
  native writer 9/96 (cases/assertions); focused Go API/config. Frontend:
  1523/1523, 6450 assertions; lint/i18n/typecheck/build PASS. Изменённые
  daemon_api/daemon_core прошли C++17 syntax-only с USE_KEENETIC_API.
  Это локальная реализация: IPK/установка/перезапуск/push не выполнялись.

- Локальный срез по ревью 06.09 (R1/R5), ещё не установлен: отказ записи
  нового recovery-журнала до фиксации отклоняет сохранение с сохранением
  рабочего runtime и черновика. Прежний UNKNOWN и неоднозначная фиксация
  обрабатываются как раньше; новых журналов, locks и error codes нет.
  Фоновая загрузка списков больше не держит глобальный runtime lease на HTTP:
  короткая регистрация согласована с отменой при foreground-записи, а
  применение получает lease отдельно и при занятости повторяет только
  публикацию готового результата, без повторного скачивания. Foreground
  ожидает существующие Background-операции по типу, а не двум строковым
  labels. HTTP 304 проходит тот же cancellation/commit callback, что и 200.
  Проверено локально: API recovery 4/4 (401 assertions), admission 22/22
  (217), cache/list-refresh 102/102 (783); C++17 syntax-only daemon_runtime
  и daemon_api PASS. Полный backend suite, IPK,
  установка и push не выполнялись; перед сборкой требуется разрешение.

### Добавлено

- Двуязычные названия каталога и шаблонов 06.09: 17 названий, 4 подписи
  сопутствующих IP-списков и 8 названий шаблонов переключаются RU/EN вместе
  с интерфейсом, включая поиск, подсказки состава и подтверждение. Например,
  `Meta (все сервисы)` / `Meta (all services)`. Бренды и исходные name
  сохранены, технические ID/адреса/правила и пользовательские списки не
  переименовываются. Новое поле name_i18n необязательное: чужие/старые
  источники без перевода показывают исходное название. По разрешению
  WebUI и catalog.json установлены вместе, без IPK/backend/restart;
  версия v3.3.0-20260906110559, UI source-id `cksum-v1:38730662:63356`.
  1517 frontend tests / 6418 assertions, lint/i18n/literals/typecheck/build
  PASS. На роутере сверен entry JS, config/transports и PID служб не менялись.
  Резерв прежнего UI/каталога сохранён, временные файлы удалены. Визуальная
  проверка переключения пользователем остаётся открытой; commit/push не было.

- Локальный срез групп и английских описаний каталога 06.09 (после поставки
  `20260906110559`, WebUI установлен отдельно): в строках групп появилась корзинка
  того же вида и размера, что у VPN/подписок, рядом с замером и карандашом.
  Используются прежние окно подтверждения, анализ зависимостей и сохранение;
  новый путь передаёт только выбранную группу, её VPN-участники не удаляются.
  Кнопки доступны в desktop/mobile разметке. Для всех 22 непустых notice
  встроенного каталога и 9 описаний шаблонов добавлен английский перевод;
  русские тексты, имена, адреса, правила и catalog identity сохранены.
  Поиск учитывает выбранный язык и прежние исходные строки. После обновления
  внешнего каталога встроенное описание переносится вместе с переводом;
  устаревший перевод другого текста удаляется, пользовательский текст остаётся
  читаемым. API/schema и допуски операций не менялись. Frontend 1513/1513
  (6295 assertions), lint/i18n/literals/typecheck/UI build PASS;
  `build/ui-groups-catalog-20260906`. Production C++ merge-функция проверена
  отдельным коротким host-harness на paired/stale/custom/append/repeat/bundled
  сценариях; полный C++ suite и Keenetic IPK не пересобирались.
  По отдельному разрешению установлен только WebUI: корзинка групп и 9
  переводов шаблонов доступны на роутере. При UI-only загрузке 22 перевода
  основного каталога сначала не доставлялись: catalog.json/backend не менялись.
  Версия в шапке сохранена `v3.3.0-20260906110559`, UI source-id:
  `cksum-v1:3216197071:63356`. Отдаваемый entry JS проверен по SHA256;
  config/transports и PID keen-pbr/transport-manager/nfqws2 не изменились,
  службы не перезапускались. Предыдущий UI сохранён для отката, временная
  папка загрузки удалена. IPK/commit/push не выполнялись; функционального
  удаления пользовательских групп при технической сверке не было.
  Позже 06.09 недостающий catalog.json отдельно установлен без рестарта/IPK:
  все 22 английских notice теперь есть в действующем bundled каталоге.
  Кроме notice_i18n содержимое 102 записей полностью совпадает с прежним;
  адреса/правила/ID/RU-тексты, config/transports и PID служб сохранены.
  Файл сверен по SHA256, предыдущий JSON сохранён для отката. Визуальная
  проверка не выполнена из-за повторного сбоя Chrome-инструмента.
  Новый backend overlay для внешних источников остаётся до полной поставки.

- Сверка апстрима 06.09: после `c27a0f63` найдены `ca963620`, `2d5075ae`
  и `4b0a09b8`. Это Debian ARM64, Ubuntu APT aliases, native ARM runner для
  Debian CI и счётчик релиза. Изменений runtime/Keenetic нет, поэтому перенос
  не выполнялся и базовая версия остаётся 3.3.0; номер upstream 3.3.1 не
  присваивается форку без полезного переноса. Подробное решение — в локальном
  roadmap. В этом срезе IPK, установка и commit/push не выполнялись.

- Локальный срез понятных подписей выбора 06.09 (ещё не установлен): из
  обычных селекторов VPN/интерфейсов и подписей участников группы убраны
  дублирующие технические теги и имена ядра. Они остаются в `title`, значениях
  выбора и поисковых данных; исходные keys, onChange и ссылки конфигурации
  не меняются. Совпадающие имена в выборе различаются короткими номерами,
  вычисляемыми по стабильным ID для полного набора вариантов, а не по
  отфильтрованной выдаче. Эти подписи не сохраняются как псевдонимы.
  Без понятного имени единственный технический fallback остаётся видимым.
  Статусы, адреса и протоколы сохранены. У строк VPN без связанного маршрута
  также убрана лишняя строка с именем ядра; отличный человеческий псевдоним
  интерфейса в таблице остаётся полезным контекстом. Активные runtime-деревья
  уже использовали псевдонимы и технические title — их логика не менялась.
  Frontend 1503/1503 (6057 assertions), lint/i18n/literals/typecheck/UI build
  PASS; `build/ui-selector-labels-20260906`. Backend/C++, IPK, роутер,
  commit/push не затронуты.

- Локальный срез общих ошибок редакторов 06.09 (ещё не установлен): ошибки
  сохранения основных настроек, DNS-правил, списков, VPN и правил маршрутизации
  используют существующие RU/EN объяснения и исходные подробности. Также
  локализована общая ошибка удаления списка; прежняя отдельная обработка 409
  с обновлением данных сохранена. Коды занятости/входа и сведения о результате
  отката передаются целиком; неизвестная причина не подменяется известной.
  Исправлена потеря ошибок при нормализации TanStack: библиотека сохраняет
  только значение form, отбрасывая соседние метаданные. Общая ошибка теперь
  хранится в поддерживаемом значении, а ошибки без подходящего поля не исчезают.
  Это уточнение предыдущего полевого среза: теперь проверена не только
  отрисовка, но и прохождение сообщения через реальный FormApi. Во время
  повторного сохранения прежняя причина остаётся локализованной, затем
  заменяется или очищается штатно. Локальная валидация, привязка к полям,
  запросы, маршрутизация и допуски API не менялись. Проверены обычная очистка,
  редактирование после отказа и доступность повторного сохранения.
  Frontend 1476/1476 (5948 assertions), lint/i18n/literals/typecheck/UI build
  PASS; `build/ui-editor-errors-20260906`. C++, IPK, роутер, commit/push
  не затронуты.

- Локальный срез серверных ошибок форм 06.09 (ещё не установлен): ошибки под
  полями DNS, DNS-правил, основных настроек, списков, VPN и правил маршрутизации
  показываются на языке панели. Известные причины объясняют конкретное
  ограничение (порты, адреса, диапазоны, названия и ссылки на другие записи);
  неизвестные получают нейтральное сообщение. Исходные `path/message` доступны
  в закрытых подробностях, в том числе для ошибок без подходящего поля.
  Перевод выполняется после прежней привязки к полю и обновляется при смене
  языка без запроса к серверу. Локальные проверки, очистка ошибок и правила
  сохранения не менялись; фокус резервного маршрута не перескакивает при
  перерисовке сообщения. Использован существующий API, без новых ограничений
  или повторов операций. Frontend 1456/1456 (5810 assertions),
  lint/i18n/literals/typecheck/UI build PASS; `build/ui-server-validation-20260906`.
  C++, IPK, роутер, commit и push не затронуты.

- Локальный срез локализации 06.09 (ещё не установлен): общие ошибки служб,
  VPN, каталога/мастера, загрузки журнала и DNS-редактора показываются на языке
  панели, с точной исходной причиной в подробностях. Неподтверждённая готовность
  после перезапуска не объявляется доказанным отказом. Ошибки обновления списков
  и неизвестные предупреждения каталога используют существующие подсказки для
  технических деталей; подтверждение предупреждений не менялось. Уточнены
  обычные DNS/VPN формулировки; degraded — «Есть проблемы», не «Не работает».
  В диагностике переведены типы маршрутов, неизвестные статусы и ещё восемь
  видов причин, без изменения оценки здоровья. Производители Error.message,
  авторизация, блокировки, запросы и повторные попытки сохранены. Проверка
  литералов: 41 документированное техническое исключение, 0 миграционного долга.
  Серверные сообщения под конкретными полями завершены последующим срезом выше.
  Frontend 1373/1373 (4892 assertions), lint/i18n/literals/typecheck/UI build
  PASS; `build/ui-localization-20260906`. C++, IPK, установка, commit/push
  не выполнялись.

- Локальный срез ленты событий 06.09 (ещё не установлен): на дашборде появилась
  карточка «Переключения и восстановления». Показывает пять последних событий,
  раскрывается до 20: подтверждённый выбор другого VPN в группе, неполадки и
  восстановление маршрутов, изменения работы служб/процессов VPN и DNS.
  Используются существующие снимки SSE и транспорта; новые запросы и таймеры
  не добавлены. Первое наблюдение, переподключение, изменение задержки или
  счётчика попыток не выдаются за событие. DNS подтверждается живым состоянием,
  а не ожидаемым конфигом; состояние процесса VPN не обещает доступность сайтов.
  Краткая история хранится только в памяти открытой панели, очищается при
  обновлении страницы или выходе и не восстанавливает события за время разрыва
  связи. Время — момент наблюдения по часам устройства. Псевдонимы, RU/EN
  подписи, переходы к соответствующей диагностике и журналу; состояния
  загрузки/пустой истории/устаревших данных различаются. Системный колокольчик,
  flash, backend и допуски операций не менялись. Проверки: frontend 1333/1333
  (4635 assertions), lint/i18n/literals/typecheck/UI build PASS. C++, IPK,
  установка, commit и push не выполнялись.

- Локальный срез действий после применения 06.09 (ещё не установлен): после
  подтверждённого сервером применения конфигурации появляется «Изменения
  применены». Для известных изменений маршрутизации/DNS предлагается
  «Проверить сайт» с фокусом на адресе, без автоматического запроса. Изменения
  имён и журналов не вызывают этот совет; при неизвестной исходной конфигурации
  панель не угадывает, что изменилось. Медленное обновление данных дашборда
  больше не задерживает подтверждение завершённого apply. Ошибки дают переход
  к соответствующей диагностике DNS, маршрутизации или состоянию служб.
  Для завершённой проверки того же адреса кнопка называется «Проверить снова»;
  подсказки различают ошибку DNS, недоступность сервиса и неоднозначный результат
  браузерной проверки, не предлагая без оснований сбрасывать кэш или
  перезапускать приложения. Тексты RU/EN; без новых API, фоновых проверок,
  сохраняемых в браузере копий конфигурации или изменений допуска операций.
  Проверки: frontend 1280/1280 (4346 assertions),
  lint/i18n/literals/typecheck/UI build PASS. C++, IPK, установка и push
  не выполнялись.

- Локальный срез первого запуска 06.09 (ещё не установлен): Keenetic full и
  headless поставляются без демонстрационного `local_list/example.com` и
  блокирующего примера; необязательный `local.lst` пуст. Существующие config
  и списки при обновлении сохраняются; DNS, br0 и технические ID не менялись.
  Стандартные `wan`/`block` без пользовательских имён показываются как
  «Обычный интернет»/«Блокировать» с переводом при смене языка панели.
  На действительно пустом дашборде доступен мастер или восстановление backup;
  после появления пользовательских VPN/списков/правил приглашение исчезает.
  Ошибки загрузки и черновики не считаются пустой установкой. Повторный вход
  в мастер — «Ещё» → «Справка» на ПК, «Справка» в мобильном боковом меню.
  В существующем мастере пояснён автоматический DNS, добавлен переход к обычному
  импорту WG/AWG/подписок и к проверке сайта с фокусом на адресе; неверное имя VPN
  получает понятную ошибку и фокус. Открытие мастера и переход к проверке не
  запускают запись или сетевую проверку сами. Без новой модели состояния,
  browser storage, polling, API или backend-изменений. Проверки: frontend
  1244/1244 (4224 assertions), lint/i18n/literals/typecheck/UI build PASS;
  пакетные Python 26/26, postinst full/headless в BusyBox 1.37 сохраняют
  пользовательские config/list побайтно. C++, IPK, установка и push не выполнялись.

- Локальный срез 06.09 (ещё не установлен): общая резервная копия VPN теперь
  включает источники подписок, их URL, расписания, лимиты и привязки к VPN.
  При восстановлении привязки сверяются с восстановленными подключениями;
  повторного скачивания подписок и автоматического создания VPN нет. Старые
  архивы без этого раздела сохраняют текущие источники и расписания, очищая
  только несовпадающие привязки. Явный пустой раздел нового архива восстанавливает
  пустой список источников. Rollback включает исходный файл подписок; отдельное
  восстановление его метаданных не перезапускает transport-manager или nfqws.
  Автообновление и restore согласованы через существующее обслуживание и mutex,
  без нового фонового процесса или пользовательских подтверждений. Форма backup
  поясняет состав архива и последствия восстановления на RU/EN, не показывая
  приватные URL. Архивы новой версии не предназначены для старого бинарника.
  Проверки: backup/recovery 45/45 (619 assertions), subscription refresh 61/61
  (568); frontend 1229/1229 (4149), lint/i18n/literals/typecheck/UI build PASS.
  API codegen воспроизводим, регистрация targets 4/4; production-код backup
  и объект daemon_api скомпилированы. IPK, установка, commit и push не выполнялись.

- Локальный срез 06.09 (ещё не установлен): независимая автоочистка журналов
  keen-pbr и nfqws по размеру и/или возрасту. Размер файла — 64 КиБ–16 МиБ,
  возраст — 1–365 дней; по умолчанию ограничение размера 1 МиБ включено,
  возраст выключен. У keen-pbr остаётся одна предыдущая копия; у nfqws
  сохраняется активный inode. Фоновое обслуживание использует существующий
  минутный таймер и executor. Строки без распознаваемой даты не удаляются
  по возрасту; лимит размера действует независимо. Это пояснено в UI.
  Изменение настроек не перезапускает службы.
- Настраиваемое автообновление источников подписок на роутере, без открытой
  панели: выключено либо от 1 часа до 7 дней, по умолчанию 6 часов. Для
  URI/base64-списков изменённые параметры уже импортированных VPN применяются
  через существующий transport-manager с сохранением псевдонимов, интерфейсов,
  маршрутов и групп. Новые серверы предлагаются к выборочному импорту, а
  исчезнувшие не удаляются. Переименование узла провайдером при неизменном
  подключении не создаёт ложного предложения импорта. Полные JSON-конфигурации
  sing-box остаются отдельным форматом, автоматическое обновление их outbounds
  этим срезом не реализовано.
- Системные уведомления о новых серверах подписок с переходом непосредственно
  к нужной карточке. Предупреждения о трафике появляются при остатке 10% или
  меньше; о сроке — за 7, 3 и 1 день, затем после истечения. Следующий порог
  снова уведомляет после очистки предыдущего, одинаковая проверка не создаёт
  дубликатов. Неизвестные провайдерские лимиты не подменяются догадками.
  Существующий общий колокольчик и SSE используются без нового browser polling.
- Кнопка перезапуска в строке keen-pbr-sb карточки «Службы»: заменяет верхнюю
  «Перезапустить маршрутизацию» и перезапускает daemon вместе с transport-manager
  и управляемыми подключениями sing-box. nfqws не перезапускается. Подсказка
  предупреждает о перерыве маршрутизации; уведомление показывает прогресс,
  панель ждёт новый процесс и готовность маршрутизации/менеджера.

  Проверки объединённого локального среза: frontend 1211/1211 (4100 assertions),
  lint/i18n/typecheck и UI build PASS; адресные backend 95/95 (957 assertions),
  restart shell 5/5, shell syntax и включение новых targets в Makefile PASS.
  Изменённые production-объекты API и daemon скомпилированы; генерация API
  воспроизводима. IPK, установка, commit и push этого среза не выполнялись.

- В настройках nfqws — необязательное исключение проверок подключения Android
  из обработки nfqws: 12 указанных владельцем доменов в `exclude.list`, список
  адресов и пояснение RU/EN для ложного «Wi-Fi без интернета» за отдельной точкой
  доступа. Включение и выключение сохраняют ручные исключения; применяется
  обычной кнопкой Save, с restart только уже запущенного nfqws2. Ошибка restart
  не скрывает факт сохранённого списка; повторное применение не пишет файл
  повторно. Работает в стратегиях/режимах, использующих exclude.list; не меняет
  маршруты VPN. Локально готово 06.09, ещё не установлено на роутер.

- Общий колокольчик для всех устройств: очистка сохраняется на роутере и
  передаётся открытым панелям через существующий SSE. При повторном входе
  скрытые записи не возвращаются; новые ошибки во время очистки остаются.
  Диагностический журнал не удаляется, часы и localStorage браузера больше
  не определяют прочитанное. Хранится ограниченный набор ID, а не второй
  журнал. Отдельный пароль и перезапуск маршрутизации не требуются.
  Известные ошибки групп, применения конфигурации, VPN и списков получили
  понятные RU/EN сообщения; исходная строка доступна в «Подробностях».
  Локальные frontend и узкие backend-проверки прошли; изменение пока не
  входит в установленную `3.3.0-20260905193037` и требует следующей поставки.

- Обновление URL-списков: показ «было → получено» при резком сокращении,
  одно действие «Принять сокращение» и отдельная повторная загрузка.
  Порог настраивается для каждого источника в расширенном редакторе.
  Принятие относится только к показанной версии; последующие обновления
  используют прежнюю политику. Рабочее тело и ETag сохраняются при отказе,
  дополнительная большая копия списка не удерживается. Используются
  существующие загрузчик, обновление интерфейса и применение маршрутизации.
  Поставлено в `3.3.0-20260905193037`; контролируемая проверка сокращения
  тестового источника на Keenetic остаётся отдельной приёмкой.

- Поведение отдельного правила при отказе VPN: наследовать общую настройку,
  блокировать новые подключения или переключать их на явно выбранный резервный
  VPN/группу. Редактор правила и быстрая настройка списка используют одну форму;
  ранее сохранённые правила не меняют поведения. Используются существующие
  проверки интерфейсов, выбор участников групп и firewall worker, без нового
  процесса, опроса или контура допуска записи. Новая политика отдельного
  правила не срабатывает из-за ошибки одного проверочного сайта: такая ошибка
  даёт degraded, а не отказ всего VPN. Отсутствие интерфейса/маршрута остаётся
  причиной переключения; новый сетевой тест peer не добавлен. Kill-switch других
  правил не изменяются, глобального сброса соединений нет. Резерв учитывается
  в зависимостях конфигурации и разрешениях OpenConnect. Удаление резервного
  VPN сохраняет правило с блокировкой при отказе основного; это показано
  в прежнем подтверждении удаления, без нового диалога. TABLE-маршруты оставлены
  в прежнем режиме inherit: для них нет проверки с привязкой к VPN-интерфейсу.
  Поставлено в `3.3.0-20260905193037`; рабочие VPN ради проверки отказа не
  останавливались, поэтому controlled primary/fallback приёмка остаётся открытой.

- Продолжение настройки из каталога: выбор списков, поиск, категория и маршрут
  сохраняются при переходе к созданию VPN или настройке DNS, включая возврат
  кнопкой браузера. При отсутствии маршрута «только списки» выбирается явно;
  ошибки существующего планировщика предлагают выбрать VPN или настроить DNS.
  После создания VPN и импорта подписки уведомление предлагает «Выбрать сайты»
  либо «Вернуться в каталог». Навигация не создаёт списки или конфигурационный
  черновик; завершение WG/AWG и привязка маршрута остаются на прежнем пути.
  Срез прошёл frontend-проверки и установлен вместе с DNS-привязками
  в `3.3.0-20260905112308`; пользовательская приёмка каталога ещё предстоит.
- Прямые доменные привязки в редакторе DNS-сервера без создания списков:
  поле доменов с нормализованным предпросмотром, сохранение для основного и
  резервного сервера, поиск по доменам и показ удаляемых привязок в обычном
  подтверждении удаления DNS. Привязка включает поддомены и имеет приоритет
  над DNS-правилами списков и унаследованной доменной политикой Keenetic;
  среди прямых привязок выигрывает более точный суффикс. Общий fallback и
  правила маршрутизации не меняются. Используются существующие registry,
  detour и генератор dnsmasq; новые процессы и контуры применения не добавлены.
  Срез прошёл выбранные backend/frontend-проверки и включён в установленную
  `3.3.0-20260905112308`; отдельная пользовательская DNS-приёмка ещё предстоит.
- Вкладка «Подписки» в меню VPN: источники sing-box, срок действия,
  использованный и оставшийся трафик из `Subscription-Userinfo`, связанные
  VPN и последнее обновление. Отсутствие лимитов обозначается как неизвестное,
  а не безлимит. Источник нового URL-импорта сохраняется автоматически.
  Кнопка «Добавить подписку» открывает общий импорт с выбором VPN и созданием
  связанных маршрутов, а не только сохранение метаданных. Имя берётся из
  `Profile-Title`, если провайдер его передаёт; ручное переименование сохраняется.
  Ручное обновление, переименование и удаление записи не меняют VPN/маршруты
  и не требуют перезапуска. URL хранится в отдельном `subscriptions.json`
  с правами 0600 и не выдаётся в списке API; тела подписок не сохраняются.
- Импорт штатных WireGuard и AmneziaWG из URI и файлов `.conf` прямо в KeeneticOS. Поддерживаются актуальные AmneziaWG 2.0 и legacy-конфигурации 1.0–1.5; секретный текст передаётся один раз и не сохраняется в журнале панели.
- Автоматическое включение импортированного интерфейса, сохранение конфигурации KeeneticOS и создание связанного маршрута keen-pbr. После неоднозначного ответа панель сама сверяет результат и завершает настройку появившегося интерфейса без повторной отправки секрета.
- Управление импортированными WG/AWG в общем списке «VPN, прокси, группы»: пользовательский псевдоним и страна, редактирование обычным карандашом и удаление обычной корзиной. Техническое имя интерфейса остаётся только в подробностях.
- Включение, выключение и перезапуск существующих клиентских WireGuard/AmneziaWG прямо из обычного переключателя и кнопки перезапуска в списке.
- Удаление принадлежащего панели штатного туннеля вместе со связанным маршрутом, сохранением KeeneticOS и возможностью безопасно закончить прерванную операцию. Удалённый слот можно освободить для последующего импорта после явного отказа от сохранённой копии отката.
- Инвентарь штатных интерфейсов KeeneticOS с различением принадлежащих панели, чужих, удалённых и требующих завершения операций записей. Внешние ответы не содержат внутренних transaction ID, маркеров и имён файлов снимков.
- Единая карточка проверки сайта на дашборде: доступность из браузера и с роутера, фактический маршрут/туннель, покрытие nfqws2 и проверка по реестру. Проверка реестра расположена непосредственно под адресом сайта.
- Диагностика маршрутизации передаёт через API и управляющий Unix-сокет
  выбранное правило, опубликованный firewall outbound с точной меткой и
  read-only ответ FIB ядра с интерфейсом и таблицей. Техническая карточка
  «Проверка пути маршрутизации» убрана с дашборда; доступность сайта,
  результаты маршрутизации, nfqws2 и реестр сохранены. Временные
  firewall-объекты для диагностики не создаются.
- Установка закреплённой версии sing-box из панели с предварительной оценкой возможности, прогрессом загрузки, отменой, проверкой результата и сохранением заменённого бинарника для отката.
- Проверка фактического внешнего адреса и страны каждого транспорта, а также ручная проверка задержки штатных VPN-интерфейсов.
- Полное OpenAPI-описание API панели, включая аутентификацию, резервные копии, диагностику, обновления, транспортные операции и закрытые ручные native-мутации.
- Встроенный каталог вместо обязательного скачивания при первой установке, миграция источников с недоступного `repo.hoaxisr.ru` на SagerNet и возможность планировать отдельный список как всегда прямой.
- Настраиваемая ёмкость ipset для больших списков и долговечное, атомарно сохраняемое согласие на проверку сайта по реестру.
- Транзакционный lifecycle nfqws2 хранит точные пакеты до и после мутации,
  однозначно завершает прерванную операцию при следующем запуске и позволяет
  установить отсутствующий компонент прямо с его страницы. Пользовательская
  конфигурация не заменяется без заранее сохранённой точной копии.
- Локальный gate `make check-ndmc-env` воспроизводимо проверяет, что системный
  `ndmc` запускается без несовместимого Entware `LD_LIBRARY_PATH`.
- На карточке служб дашборда появилась явно подписанная кнопка
  «Перезапустить маршрутизацию». Для остановленного или сломанного runtime она
  запускает маршрутизацию, для работающего — выполняет штатный перезапуск, а
  затем ждёт готовности маршрутов и DNS.

### Изменено

- **P1 / nfqws rotator:** по live-наблюдению на `Dimkin-S26U` приложение
  YouTube через QUIC работало, но `m.youtube.com` по TCP зависал при исправном
  DNS. Между перезапусками nfqws сохраняются только подтверждённые успешные
  слоты `gv_tcp` / `yt_tcp` / `yt_quic`: снимок привязан к точному
  `kpbr_rev`, ограничен 64 записями и 30 днями, использует debounce 120
  секунд и не более одной записи за 6 часов; счётчики ошибок не сохраняются.
  Установленный первый срез вернул работу мобильного браузера, но live-файлы
  выученных слотов остались пустыми. Найдена функциональная причина: внешний
  nfqws2 передаёт первые 15 ответных пакетов, однако прежние пороги 18/24 КиБ
  были пограничными или недостижимыми, а `circular` стоял после payload-
  фильтра и с выключенным по умолчанию входным диапазоном. Встроенные профили
  теперь доказывают TCP-успех после 8 КиБ, дают только `circular` диапазон
  до 9652 байт либо два входящих QUIC-пакета и сразу возвращают
  `in-range=x` перед стратегиями. Скрипт запуска внешнего пакета nfqws2 не
  изменяется. Узкие проверки профилей 20/20, Lua-smoke и `diff --check`
  прошли. Исправление вошло в `bb652330` и установленную IPK
  `3.3.0-20260904163027`: learned-state сохранился после S51 restart и
  аппаратной перезагрузки, конфигурационные хэши не изменились, а пользователь
  подтвердил работу видео в третьем браузере.

- Запуск API больше не ожидает синхронный ответ
  `/rci/show/rc/user`: проверка изменения учётных данных Keenetic выполняет
  первый запрос сразу в существующем фоновом worker, а затем сохраняет прежний
  30-секундный интервал. При зависшем RCI listener запускается и обычные API-
  запросы продолжают отвечать; начатый в стартовом окне вход прозрачно ждёт
  завершения первой фоновой попытки, поэтому прежняя граница публикации сессии
  сохранена. Request path и control loop не получили нового RCI-reader или
  state machine. Точечные credential-regression тесты прошли, IPK и роутер для
  этого слоя ещё не проверялись.
- Определение версии KeeneticOS и сведения о модели для дашборда теперь
  используют один process-local single-flight snapshot
  `/rci/show/version` вместо двух независимых RCI-запросов. Версия прошивки
  и hardware facts остаются независимыми typed-проекциями одного точного
  поколения: строковый ответ не выдаётся за модель, неполный объект модели не
  скрывает корректную версию, а временный сбой сохраняет последнее корректное
  состояние. Прежние plain, quoted и UTF-8 ответы KeeneticOS поддерживаются;
  router-info принимает новое совмещённое поколение только из свежего
  authoritative version snapshot. UI и пользовательский сценарий не
  менялись; IPK и роутер для этого слоя ещё не проверялись.
- Два независимых чтения structured HTTP config KeeneticOS — для определения
  порта WebUI и политики блокировки входа — сведены к одному immutable
  `/rci/show/rc/ip/http` snapshot. Порт и lockout policy остаются независимыми
  typed-проекциями: ошибка одного поля не скрывает корректное другое, а
  сетевой или структурный сбой не затирает их LKG. Endpoint по-прежнему
  подтверждает адрес и порт настоящим Keenetic auth challenge и использует
  общий running-config fallback на старых прошивках; login budget принимает
  policy только из свежего снимка. UI и пользовательский сценарий не
  менялись; IPK и роутер для этого слоя ещё не проверялись.
- Пассивные потребители полного `/rci/show/interface` — runtime-каталог
  штатных VPN и поиск management-адреса для авторизации — теперь используют
  один immutable single-flight raw snapshot вместо двух независимых
  loopback-запросов. Typed-каталог сохраняет прежний API, поколения наблюдений
  и последнее корректное состояние; endpoint discovery разбирает management
  address из того же полного документа, не из урезанного каталога. Точные
  чтения результата native import/delete остались отдельными и свежими, чтобы
  кэш не влиял на работающие мутации. UI и пользовательский сценарий не
  менялись; IPK и роутер для этого слоя ещё не проверялись.
- Два независимых чтения полного
  `/rci/show/running-config` — для инвентаря VPN-серверов и резервного
  определения HTTP-настроек старых KeeneticOS — сведены к одному
  process-local single-flight snapshot. Ошибочный или структурно неверный
  ответ не затирает последнее корректное состояние, а семантически
  неизменившийся JSON не публикует новое поколение содержимого. Схема API,
  интерфейс панели и пользовательский сценарий не изменились; этот слой ещё
  не проходил проверку в IPK и на роутере.
- Рабочий runtime маршрутизации переведён с compatibility-вызовов на точные
  raw-rtnetlink транзакции. IPv4/IPv6 маршруты и policy rules сначала читаются
  полным `RTM_GETROUTE`/`RTM_GETRULE` снимком, затем создаются, заменяются и
  удаляются только по полной представимой identity под одной process-wide
  route/rule lease. Замена маршрута выполняется одним `RTM_NEWROUTE(REPLACE)`,
  а прежние широкие delete/flush пути owner больше не использует.
- Firewall и следующие runtime-фазы публикуются только после доказанного
  exact-результата маршрутизации; stale fence после уже выполненной мутации не
  выдаётся за неизменившийся preimage, а rollback признаётся завершённым только
  после точной сверки всего предыдущего route/rule поколения. Recovery может
  продолжиться из свежего raw inventory после неоднозначного поколения, а
  чужие и непредставимые объекты остаются нетронутыми. Выбранный пользователем
  TABLE outbound остаётся допустимым, когда его таблица временно пуста или
  содержит маршруты только одной address family. Внутрипроцессная lease не
  является kernel CAS с внешними writer, но составная identity каждого
  создаваемого policy rule (`family/table/fwmark/fwmask/priority`)
  зарезервирована за keen-pbr. Если после отсутствовавшего до записи правила
  `RTM_NEWRULE` не вернул надёжный receipt, а тот же transaction под общей
  lease увидел единственный точный объект, он считается созданным сервисом и
  откатывается exact-delete. Неоднозначный или непредставимый объект остаётся
  нетронутым; содержимое внешней TABLE сервису не присваивается.
- Фоновое завершение удаления native VPN больше не отменяется обновлениями
  статуса и задержки: после одного нажатия на корзину внутренняя запись отката
  удаляется без отдельного пользовательского сценария.
- Проект перешёл на базовую версию **3.3.0**; версия пакета и шапка панели используют `3.3.0` и дату/время конкретной сборки вместо старой метки `sb.12`.
- Изменения upstream перенесены в форк без дублирования его механизмов: обновлены каталог, API, жизненный цикл sing-box, конфигурационные проверки и инфраструктура сборки.
- Технические метки импорта вида `kpbr-ni-v1-*` больше не выдаются пользователю как названия. Для интерфейса используется заданный псевдоним; домен или IP предлагается только как редактируемый вариант псевдонима.
- Проверка сайта показывает nfqws2, реестр и доступность до блока маршрутизации, чтобы результат читался сверху вниз как один ответ, а не как набор разрозненных инструментов.
- Транспорт управляющего Unix-сокета — listener, accept-поток, framing, peer
  credentials и ограниченная очередь — вынесен из `Daemon` в
  `IpcControlService`. `Daemon` остаётся composition root, а status, list
  refresh, routing test и resolver stream пока выполняются на прежнем
  сериализованном control loop. API не собирает дорогие снимки состояния,
  когда ими никто не пользуется.
- Журнал браузера для native-операций получил межвкладочную блокировку и автоматическое согласование результата, но восстановление больше не требует от обычного пользователя отдельной скрытой кнопки.
- Проверки каталога, обновлений и привилегированных loopback-запросов ограничены по объёму и времени; редиректы для привилегированных запросов запрещены.
- IPv4/IPv6 firewall использует общий RAW PREROUTING-контур, а обычный runtime
  refresh переиспользует подтверждённые live sets, когда поколение содержимого
  не изменилось. Полное стриминговое применение остаётся проверяемым fallback.
- Остановка runtime из API и финальное завершение демона теперь передают ту же
  exact mutation lease единственному `RuntimeFirewallOperationOwner`. Маршруты,
  owned conntrack state, firewall и системный resolver очищаются одной
  ограниченной транзакцией до остановки owner/executor; тот же путь выполняется
  после ошибки event loop. `stopped` публикуется только после авторитетно
  пустого routing inventory, деактивации resolver и свежего доказательства
  отсутствия owned iptables/nftables state. Строгий backend sweep охватывает
  обе семьи и таблицы, NAT/validation chains, managed ipsets, TTL/PPE hooks,
  exact TCP reset и crash-residue прежнего backend; неполная очистка оставляет
  runtime неактивным в `broken`, а не выдаёт ложный успешный STOP.
- Компенсирующий rollback остановленного cold boot и неудавшегося START больше
  не считает firewall очищенным только потому, что команда не бросила
  исключение. Оба пути требуют свежего `verified_absent` по owned
  iptables/nftables state, а удаление маршрутов — авторитетного пустого
  inventory одновременно в фазе и исходе `cleared`; неполное доказательство
  сохраняет fail-closed состояние вместо ложного успеха.
- Первый cold boot теперь допускает точно сопоставленное private-поколение
  resolver до публикации общего committed-поколения. Это устраняет замкнутую
  проверку, из-за которой корректно созданный firewall неизбежно откатывался,
  resolver не активировался, а runtime исчерпывал три попытки запуска.
- Повторный terminal wake, возникший прямо внутри control-pass firewall-owner,
  больше не теряется за флагом уже опубликованного прохода. Cold boot после
  успешной resolver-активации получает обязательный второй проход и завершает
  публикацию runtime вместо бесконечного состояния `starting`.
- Аварийный rollback при исключении синхронного startup больше не закрывает
  firewall-owner/executor до очистки и не выполняет отдельные прямые
  route/firewall/resolver-записи. Он сначала завершает ту же exact typed
  STOP-транзакцию со строгим доказательством, что и обычное завершение
  процесса, и лишь затем закрывает admission, owner и executors; недоказанный
  исход остаётся `broken`.
- Короткоживущее exact TCP-reset правило для preventive WhatsApp recovery
  больше не устанавливается и не удаляется прямыми вызовами из control loop или
  timer callback. Установка правила, повторное read-only наблюдение того же
  IPv4/TCP 5-tuple с неизменившимися state/counters, точное удаление conntrack
  и компенсационное удаление правила выполняются одной non-coalescing
  point-транзакцией под той же physical mutation lease. Cleanup ledger
  резервируется до первой backend-команды; неоднозначная публикация сохраняет
  точную цель для cleanup, а generation change не обходит owner отдельным
  firewall writer.
- Остальные фоновые точечные изменения firewall и conntrack — публикация
  WhatsApp UDP peer-affinity, удаление точно подтверждённых idle-flow,
  ограниченная очистка Meta UDP/443 и повторная очистка owned marks — теперь
  выполняются только как non-coalescing типизированные транзакции единственного
  `RuntimeFirewallOperationOwner`. Каждая цель повторно сверяется уже внутри
  worker под той же physical mutation lease, а control loop получает
  структурированный частичный результат вместо самостоятельной записи.
  Неоднозначный исход `ipset`/`nft` больше не считается отсутствием
  изменения и запускает полный netfilter recovery.
- Единственный ответ на вопрос, активна ли маршрутизация, теперь принадлежит
  `RuntimeStateStore`; отдельное изменяемое поле `Daemon` удалено, а API/SSE
  строят проекцию из того же состояния.
- `ConfigStore` стал единственным владельцем неизменяемого активного поколения
  `Config + outbound marks`: дублирующий `Daemon::config_` и legacy daemon
  apply-путь удалены. Candidate остаётся приватным до точной публикации, а
  длительные читатели удерживают тот же shared snapshot и не смешивают данные
  двух последовательных apply.
- Production config-save, SIGHUP и backup restore переносят один exact mutation
  lease через candidate/rollback и публикуют ровно подготовленный handle только
  через exact-base `ConfigStore` CAS. При no-op publication store и reader
  получают один и тот же prepared handle; stale/base mismatch не переключает
  reader pin.
  Отдельный event-loop cursor `Daemon::outbound_marks_` удалён: синхронные
  потребители читают paired config/marks из одного active snapshot, а async
  list-refresh удерживает тот же handle до завершения worker. `UrltestManager`
  атомарно меняет immutable marks generation и владеет aliasing `shared_ptr`
  точного `ActiveConfigSnapshot`, поэтому старые probes не получают dangling
  reference и не смешивают поколения. `FirewallState` сохраняет отдельную
  realized-проекцию marks для диагностики candidate/rollback; это не authority
  конфигурации. Новый admission или пользовательский recovery-контур для этого
  не добавлялся.
- Добавлен узкий `keen-pbr-urltest-manager-tests`, чтобы проверка worker,
  generation и snapshot lifetime не пересобирала монолитный backend suite.
- Продолжено P0-1 отделение `Daemon`: новый `IpcControlService` единолично
  владеет Unix listener, путём и группой сокета, accept-потоком, ограниченным
  framing, `SO_PEERCRED`, очередью принятых запросов и stop/join/unlink.
  `Daemon` остался composition root и обрабатывает status, list refresh,
  routing test и resolver stream на прежнем сериализованном control loop.
  PID lock по-прежнему берётся до старта сокета, а ingress останавливается
  только после resolver/runtime drain и до закрытия control eventfd. Уже
  переданный dispatcher или ровно один async worker остаётся единственным
  владельцем client FD. Существующие admission, resolver/list coordinators и
  recovery-пути не заменялись.
- Добавлен быстрый `keen-pbr-ipc-control-service-tests`: production acceptor,
  peer credentials, request handoff, bounded rejection, wake-failure ownership
  и stop/join/unlink проверяются без линковки монолитного backend suite.
- Следующий ownership-срез P0-1 выделяет `InternalVpnResolutionCache` как
  единственного владельца опубликованной пары native-VPN server/service и её
  verified include-only LKG. Чистая интерпретация уже полученных NDMS/netlink
  снимков перенесена из `Daemon`, а RCI I/O, invalidation, single-flight gate,
  retry, scheduler и runtime generation сохранены в прежнем control loop.
  Публикация и rollback обменивают одну точную пару через существующий firewall
  fence; cold boot по-прежнему готовит fallible LKG до fence. Новый admission,
  recovery или пользовательский защитный сценарий не добавлялся.
- Узкая цель native-VPN переименована в
  `keen-pbr-internal-vpn-resolution-cache-tests` и теперь отдельно проверяет
  владельца active/LKG, legacy и empty-конфигурации, stale include-only
  retention, authoritative removal, IKE ingress/DNS bypass и точный
  publication/rollback без запуска монолитного backend suite.
- Следующий P0-1 слой выделяет `ConntrackCleanupCoordinator` как единственного
  владельца точного pending-остатка owned conntrack cleanup, его coalescing,
  ограниченного backoff, единственного timer, однократного предупреждения об
  отсутствии `conntrack` и опубликованного native-VPN SNAT cursor. Физическое
  удаление flows, mutation admission и generation fence по-прежнему выполняет
  существующий типизированный `RuntimeFirewallOperationOwner`; новый writer,
  recovery или пользовательский защитный сценарий не добавлялся. Мёртвый
  параллельный `OwnedConntrackCleanupOperation` вместе с watchdog удалён, а
  config-preapply и STOP получают тот же точный remainder через coordinator.
- Добавлен быстрый `keen-pbr-conntrack-cleanup-coordinator-tests`: отдельно
  проверяются фильтрация и порядок marks, coalescing поколений, bounded retry
  `2/4/8/16/30` секунд, точное потребление timer, повтор после ошибки scheduler,
  handoff/cancel, once-warning и обратимый SNAT publication cursor.
- Следующий service-extraction слой P0-1 выделяет `IdleStallSupervisor` как
  единственного владельца observer timer и его schedule identity, enabled /
  single-flight / coverage generation, обоих detector и полного observation
  scope. `Daemon` остаётся composition root: eligibility, netlink/conntrack
  observation, blocking executor/control commit, mutation admission,
  типизированные physical point mutations и exact-TCP cleanup ledger не
  дублировались и не получили нового writer/recovery/UI-контура. Устаревший
  callback после cancel/re-arm теперь отсекается serial fence, а исключение или
  отрицательный ID scheduler не оставляют включённый observer без timer.
- Добавлен быстрый `keen-pbr-idle-stall-supervisor-tests`: отдельно проверены
  timer replace/consume/inline/stale callback, scope identity, detector reset,
  single-flight, coverage invalidation и scheduler failure. Focused target
  прошёл `39/39`, `343/343`; production target полностью слинкован,
  изменённые `IdleStallSupervisor`, `daemon_core` и `daemon_runtime`
  проверены реальным Keenetic GCC 8.4, coverage gate прошёл `4/4`. Полный
  несвязанный suite, IPK, роутер и origin не менялись.
- Продолжено P0-1 отделение publication tail от `Daemon` без нового admission,
  recovery-журнала или пользовательских блокировок. Семь разрозненных
  публикаций rules/list/native-VPN/SNAT/Meta сведены в один небросающий core
  checkpoint с сохранением прежней семантики точного preimage для
  START/cold-boot и отдельных rollback-кандидатов для config, DNS и URLTEST.
  DNS/config теперь используют единый checkpoint для согласованного resolver
  generation/sync/retry/timestamp; десять флагов выполнения хвоста вынесены в
  отдельный value-only DAG progress. Mutex и оба verified include-only LKG
  native VPN больше не принадлежат `Daemon`: ими владеет самостоятельный
  `RuntimeInternalVpnLkgStore`, включая обратимый cold-boot exchange. Решение о
  post-SNAT conntrack retry вынесено в чистый план, а существующий scheduler
  остаётся единственным исполнителем. Существующие lifecycle/CAS admission
  callbacks и generation/lease fences сохранены.
- Следующий слой P0-1 убирает из `Daemon` policy-ветвление оставшегося
  publication tail: Meta candidate/failure matrix, START/cold-boot resolver
  proof, non-START resolver action, последний route-epoch fence и классификация
  START/background terminal теперь вычисляются отдельными value-only
  `noexcept` plans. Cold-boot core/LKG/resolver/runtime commit и обратный
  rollback выполняются одной тестируемой транзакцией; post-success conntrack
  использует точный borrowed plan. Существующие resolver stream, scheduler,
  incident, admission и bounded retry остаются единственными исполнителями —
  новый защитный или recovery-контур не создавался.
- Очередной слой P0-1 переносит process-wide владение попыткой resolver stream —
  IPC gate, exact attempt/generation, inactive START pointer и epochs — из
  `Daemon` в `ResolverStreamAttemptOwner`, а фазу и точное завершение
  foreground lifecycle — в value-only
  `RuntimeFirewallLifecycleResolverAttempt`. Порядок Meta, conntrack, START и
  background effects закреплён тонким stateless dispatcher без нового writer,
  admission, retry, incident или recovery-контура. Существующие coordinator и
  schedulers остаются единственными исполнителями побочных эффектов.
- API-операции runtime и SIGHUP rollback удерживают точное активное поколение
  конфигурации до завершения, а отложенный list/firewall staging использует
  закреплённый снимок кэша без ссылки на изменяемого владельца `CacheManager`.
- Сохранение активной конфигурации передаёт уже выданный exact mutation lease
  в типизированный `config_preapply` lifecycle единственного firewall-owner.
  Проверка и при необходимости восстановление owned SNAT текущего поколения
  выполняются вне control loop; кандидат применяется только продолжением того
  же terminal после `FinalizationProof`. Preapply не обновляет remote lists,
  resolver, маршруты или Meta-состояние, имеет конечный hot-retry и сохраняет
  точный остаток conntrack cleanup без повторного удаления уже обработанных
  marks. Чистый отказ до handoff возвращает тот же lease и закрывает WAL как
  неизменённый runtime; неоднозначный или уже committed terminal сохраняет
  recovery evidence без нового пользовательского сценария восстановления.
- При активном runtime production config-save теперь проводит `config_preapply`,
  candidate и при необходимости rollback в контуре единственного
  `RuntimeFirewallOperationOwner`, не освобождая и не получая заново тот же
  физический mutation lease. До первой мутации закрепляются exact committed
  resolver/list snapshot базового поколения; candidate использует собственные
  pinned list/resolver inputs и точный route checkpoint. Candidate `G -> G+1`
  остаётся приватным до подтверждённых route/firewall и resolver stream, после
  чего `ConfigStore` CAS и один небросающий checkpoint публикуют согласованные
  config, outbound marks, реализованный firewall core, resolver cursor и runtime
  generation. Typed rollback имеет terminal identity `G+1 -> G+2`, возвращает
  опубликованный runtime к базовому `G` и после вошедшего COMMIT использует
  сохранённый realized candidate core как firewall preimage; базовый preimage
  допустим только при доказанном отсутствии candidate-мутации, а неоднозначный
  исход fail closed. Candidate и подтверждённый rollback по одному разу
  выполняют соответствующие Meta, native-VPN и stale-flow tails. Пришедшие во
  время операции SNAT/catalog изменения не переигрывают старый candidate:
  после возврата lease запускается свежий NAT/full backend resnapshot. Точный
  файловый rollback теперь сам закрывает WAL; ошибка удаления или fsync active
  marker переводит операцию в `UNKNOWN`/`recovery_required`, не выдавая ложный
  rollback-success. Для Entware/Keenetic GCC 8 old ABI точная публикация
  Config использует доказуемый no-throw relocation-swap через move construction
  вместо недоказуемого для старого `std::string` aggregate swap.
- Сохранение конфигурации при остановленном runtime теперь удерживает тот же
  exact mutation lease и проходит типизированный
  `RuntimeFirewallOperationOwner` как отдельный bootstrap lifecycle. Кандидат
  остаётся приватным до подтверждённых worker/firewall и resolver-фаз, после
  чего публикуется точным `ConfigStore` CAS и переводит runtime в `running`.
  Чистый отказ до COMMIT возвращает runtime в `stopped` и сохраняет draft;
  неоднозначный post-COMMIT terminal или отказ exact CAS после подтверждённого
  кандидата fail closed. Legacy pre-generation путь больше не выполняет прямой
  missing-SNAT firewall repair перед переиспользованием marks.
- Forward-восстановление резервной копии и поздний компенсирующий rollback
  теперь проходят типизированного runtime firewall-owner с одной exact physical
  mutation lease. Handoff gate вооружается до первой долговечной файловой
  записи; чистый отказ до COMMIT откатывает файлы без повторной отправки тела,
  а неоднозначный либо post-COMMIT исход fail closed и не запускает
  автоматический повтор или rollback как будто операция отсутствовала.
  Локальный backup guard допускает второй handoff только после terminal
  completion первого owner и возврата той же exact lease, без фабрикации или
  получения нового claim. Это не завершает P0-1: хвосты DNS, STOP и cleanup
  остаются.
- Периодическое обновление Keenetic DNS больше не выполняет прямые firewall-
  записи из `daemon_resolver`: типизированный exact handoff передаёт одну
  physical mutation lease через private candidate и при необходимости exact
  rollback единственного `RuntimeFirewallOperationOwner`. Resolver stream
  начинается только после точного доказательства route checkpoint и
  однозначного firewall COMMIT; DNS view, реализованный firewall core и
  resolver cursor публикуются одним небросающим CAS/swap checkpoint. Ошибка
  resolver после подтверждённого candidate и точная route-мутация при
  неслучившемся firewall COMMIT восстанавливаются тем же token. Ambiguous
  COMMIT, generation/CAS mismatch и недоказанный preimage не повторяют тело и
  переходят к свежему recovery только после возврата lease. Неизменившийся
  снимок не забирает lease у coordinator. Этот срез не меняет cold boot, STOP
  и cleanup tails.
- Первичный cold boot больше не выполняет прямые route/firewall-записи,
  ручную публикацию resolver или retry через `sleep_for` из `Daemon::run()`.
  Новый типизированный lifecycle `cold_boot` передаёт закреплённые
  list/native-VPN/DNS inputs и одну exact mutation lease единственному
  `RuntimeFirewallOperationOwner`; route preimage снимается только после
  получения lease. Resolver запускается лишь после точного route
  worker/control checkpoint и однозначного firewall COMMIT. Реализованный
  firewall core, native-VPN LKG, resolver generation/cursor, routing-active и
  `running` публикуются одним обратимым no-throw checkpoint; ошибка resolver
  или публикации восстанавливает прежние core/LKG/resolver metadata и оставляет
  runtime остановленным. Точная route-мутация до COMMIT откатывается к preimage
  тем же token, clean pre-COMMIT сохраняет прежнюю crash-surviving kernel
  generation, а ambiguous исход не откатывается и не переигрывает тело.
  Retained и fresh contexts делят один bounded budget не более чем на три
  candidate body; свежий post-lease снимок разрешён только после возврата
  прежней lease. Shutdown до или во время завершения не открывает API и фоновые
  сервисы; исчерпание бюджета, потеря authority или отклонённый scheduler
  оставляют доступными control/diagnostics в `degraded`, а не публикуют ложный
  `running`. Финальные focused tests основной cold-boot цепочки прошли: policy
  16/16 сценариев и 80/80 assertions, owner regression 93/93 и 1765/1765.
  Найденный после первого target-gate разрыв очистки API закрыт strong exception
  guarantee: неудачный `remove_fd` очищает и epoll, и локальную `fd_entries`, а
  destruction освобождает server/context до epoll без двойного teardown.
  Второй shutdown gate после `setup_api()` синхронно снимает только что
  открытые server/context/conntrack, если admission закрылся во время запуска;
  в сборке без API тот же gate просто не публикует открытие сервисов. Focused
  API lifecycle прошёл 6/6 и 24/24. Production target `keen-pbr` после
  исправления слинкован на host с exit 0. Авторитетный
  `kpbr-keenetic-batch-gate` на live `/workspace-src` собрал target `keen-pbr`
  с `-j2` реальным OpenWrt AArch64 GCC 8.4: 8/8 compile/link шагов, включая
  `[8/8] Linking CXX executable keen-pbr`, exit 0. Немедленный повтор завершён
  с `ninja: no work to do`; единственное сообщение — ожидаемый `STAGING_DIR`
  warning.
- Внутренние изменения runtime/firewall из восстановления правил, URLTest,
  Keenetic DNS, отложенного старта и автообновления списков проходят через
  единого владельца мутации. Занятый владелец сохраняет один ограниченный
  поколением отложенный запрос без расходования бюджета повторов; принудительное
  применение обновлённого списка не теряется при совпадении с read-only/304.
- SIGHUP, синхронное API-обновление списков и фоновый list-refresh больше не
  используют legacy `apply_config_with_rollback`. Одна exact move-only mutation
  lease через типизированный одноразовый handoff проходит общий
  candidate/rollback owner; active `ConfigStore` публикуется только exact-base
  CAS и не читает и не меняет panel draft. После durable cache commit отказ,
  stale либо ambiguous terminal сначала возвращает lease, затем ставит один
  bounded forced reconcile: coalesced SIGHUP и deferred list refresh не создают
  второго writer и не теряют уже сохранённое обновление списка.
- Для следующего этапа выноса firewall I/O подготовлены неизменяемая
  backend-транзакция и один фазовый retry-handoff. Транзакция различает вход в
  commit и его возврат, проверяет Meta UDP/443 перед первоначальным и резервным
  commit и допускает только один переход `RulesOnly` -> `PreserveSets`.
  Отдельные snapshot-контракты выводят из закреплённых config/marks точную
  generation-fenced authority для последующей owned conntrack-очистки.
  Retry-coordinator сохраняет один claim через timer, worker и control,
  аутентифицирует terminal callback и не теряет более новую recovery revision
  либо один coalesced trailing run. Точное наблюдение native-VPN передаётся
  одним неизменяемым generation-fenced снимком интерфейсов и сервисов; только
  типизированная операция может принять его, а отмена, stale generation и
  отказ scheduler/queue имеют однозначного владельца результата.
- Для off-loop apply добавлен долговечный worker-envelope без второй
  state machine. Неподделываемый running claim, один mutation lease и
  caller-owned terminal mailbox переживают копирование или отмену задачи,
  отказ очереди, исключение worker и уничтожение операции до постановки в
  очередь. Результат либо точное требование переснять входы публикуется один
  раз до best-effort уведомления control loop.
- Локальный B1-контур добавляет долговечного control-владельца
  terminal outcome. Владелец сам выполняет и без второго fallible-шва сохраняет
  переходы `worker_running -> control_pending -> complete`, удерживает result,
  mutation lease и одноразовый noexcept-checkpoint публикации, а exact
  coordinator completion имеет приоритет над пришедшим раньше lease-free
  `lost_claim`. Тесты контракта покрывают обе очередности этой гонки, повтор
  после исключения и явный drain уже работающего terminal при shutdown.
- Отдельный worker-attempt выполняет одной попыткой SNAT-observation,
  stage/Meta-preflight/commit и post-COMMIT проверки Meta/FastNAT, сохраняя
  неоднозначный commit без автоматического повтора. Точный authority-снимок
  owned conntrack marks теперь захватывается до COMMIT и переносится в result:
  finalizer не должен строить ему замену после изменения firewall generation.
  Production-сигнатура native VPN уже передаёт интерфейсное и сервисное
  наблюдения одним immutable generation-fenced объектом; exact TCP cleanup
  остаётся dormant, пока retry-coordinator владеет firewall backend.
- Первый production-срез B1 локально подключает отложенные firewall
  `schedule/defer` к типизированному exact catalog/claim/terminal API и отдельной
  ограниченной очереди `1x1`. Один Daemon-owned active context удерживает
  immutable worker input, точную SNAT-recovery authority, mutation lease и
  одноразовый terminal owner; scheduler/queue rejection, отмена, stale claim и
  worker outcome завершаются через тот же durable drain. Best-effort wake
  подкреплён watchdog, а shutdown закрывает admission, отменяет timer/queued
  work, прокачивает terminal до и во время quiesce, затем останавливает worker и
  выполняет финальный drain. Control publication использует безаллокаторные
  swap-checkpoints и освобождает lease только после coordinator completion.
  Неоднозначный entered COMMIT не получает автоматического replay даже при уже
  накопленном trailing intent: продолжение возможно только через новый полный
  backend resnapshot. Exact native-direct source cleanup выполняется worker-side
  лишь после доказанного commit и не повторяется в control tail.
- Асинхронный transport/ownership-контур этого B1-среза вынесен из `Daemon` в
  отдельный `RuntimeFirewallOperationOwner` без обратного указателя на `Daemon`.
  Он единолично удерживает active context, typed schedule/defer, очередь `1x1`,
  terminal wake/drain, повторяющийся watchdog, shutdown fence и долговечный
  pending successor. Точный SNAT recovery и самый новый совместимый prepared
  catalog сохраняются до доказанного приёма successor новым context/coordinator;
  более новое catalog-less поколение не наследует устаревший catalog. Отказ
  control-post, регистрации watchdog, выделения нового context либо timer arm
  не теряет terminal outcome и не оставляет mutation lease без владельца.
- Это извлечение ownership, а не завершение firewall P1 и не полное разделение
  `Daemon`: построение worker input, static-route reconciliation, публикация
  rules/LKG/resolver/Meta/conntrack и политика ambiguous COMMIT пока остаются в
  daemon-specific domain state и control-tail callback. Актуальный production
  target слинкован полностью (253/253); отдельная owner/terminal/worker цель
  прошла 26/26 сценариев и 509/509 assertions, итоговое независимое review не
  нашло P0/P1-блокеров.
- Следующий локальный срез **B1-immediate** убирает старый синхронный первичный
  runtime refresh: attempt 0 без фиктивного таймера сразу передаётся тому же
  `RuntimeFirewallOperationOwner` и `RuntimeFirewallRetryCoordinator`.
  Move-only completion intent завершает periodic URLTEST, owned-SNAT repair и
  FULL netfilter probe только после точного terminal outcome; queue/scheduler
  rejection, stale claim и shutdown не оставляют operation без владельца.
  Backend failure attempt 0 переходит к attempt 1 через одну секунду, а
  исчерпавшийся SNAT pre-worker retry уходит в тихое обслуживание через 60
  секунд без same-attempt loop. Старый compatibility-путь удалён, число прямых
  production-вызовов `apply_firewall()` уменьшено с 12 до 11. Узкий suite
  прошёл 43/43 сценария и 666/666 assertions; blockers-only review дал
  P0/P1 = 0.
- Переключение выбранного child в URLTEST теперь передаёт candidate и точный
  rollback единому `RuntimeFirewallOperationOwner` с одной generation-fenced
  mutation lease. Выбор в URLTEST manager и соответствующий `FirewallState`
  публикуются только после подтверждённых route mutation и firewall COMMIT;
  чистый отказ до COMMIT не запускает лишний rollback, а уже изменённый route
  или неоднозначный COMMIT восстанавливаются по точному preimage. Recovery
  начинается только после возврата физической lease. Перед первым worker
  Meta UDP/443 cleanup и idle observer останавливаются и перевооружаются через
  точный epoch и barrier `udp_call_affinity_mutation_mutex_`; в обработчике
  URLTEST больше нет прямых routing/firewall writers. Существующие семантики
  priority quarantine `5/2` и direct-interface bind не изменены. Этот срез
  явно не переносит Keenetic DNS, cold boot, STOP и оставшиеся cleanup tails.
- Для следующего route-среза построение желаемого поколения выделено в чистые
  `PlannedRoutingState`/`plan_routing_state()`: planner принимает immutable
  reachability snapshot, не выполняет Netlink I/O и не изменяет входные данные.
  Совместимый `populate_routing_state()` сохраняет прежние callback caching и
  порядок route/rule mutation, cleanup и adoption. Routing suite прошёл 66/66
  сценариев и 261/261 assertions, включая parity, deterministic replay,
  nested URLTEST и IPv6-off; независимое review дало GO без P0/P1.
- Вторая половина R0 переносит блокирующее IPv6-наблюдение, снимки main routes
  и интерфейсов и построение желаемого route/rule generation первичной runtime
  attempt в тот же owned worker. Immutable `RuntimeRouteHealthPlan` несёт
  serial, runtime generation и route epoch; одноразовый typed checkpoint
  допускает firewall backend только после подтверждённой публикации того же
  поколения. Stale, route-unavailable, mutation failure и shutdown не запускают firewall.
  Watchdog повторно поднимает отпущенный control claim, shutdown будит worker,
  а main-table `RTM_NEWROUTE`/`RTM_DELROUTE` инвалидируют план и сохраняют один
  coalesced refresh даже после уже выданного ack. Ожидание checkpoint не держит
  affinity/backend lock. Kernel route/rule mutation пока остаётся короткой
  control-loop фазой до R1. Focused gates: route preparation 3/3 (32
  assertions), route health 5/5 (65), InterfaceMonitor 4/4 (31), отдельный
  owner suite 45/45 (675); blockers-only review дал GO без P0/P1.
- Первый prerequisite-срез R1 выделяет постоянного
  `RuntimeRoutingOperationOwner`, который единолично хранит mutable-ledger
  `RouteTable` и `PolicyRuleManager`. Запрос связан точным tuple operation,
  runtime generation, intent, inventory revision и route epoch; stale/replay
  отклоняются до Netlink. Читатели получают только неизменяемую копию с
  монотонной ревизией, а частичная ошибка сохраняет lifetime-owned typed
  journal и рабочий снимок для следующего exact resnapshot. Результат намеренно
  называется `compatibility_converged`, а не commit. Production/test targets собраны; узкий набор прошёл
  6/6 сценариев и 65/65 assertions, независимый blockers-only review дал GO,
  P0/P1 = 0.
- Второй prerequisite-срез R1b добавляет одну совмещённую route/rule
  транзакцию с заранее опубликованным lifetime-owned журналом, точными
  per-object receipts, immutable fence и commit-before-cleanup. Нормальная
  очистка policy rules использует только явный owner-ledger; kernel-shape
  heuristic разрешён лишь отдельному recovery-проходу. Foreign/unrepresentable
  route/rule, wildcard rule identity, неоднозначный kernel slot, неканонический
  default-route запрос и racer во время rollback/cleanup дают отказ или
  `cleanup_pending`, но не широкое удаление. Production backend намеренно
  fail-closed: до появления единого exclusive-writer lease, полного raw
  inventory и честных conditional replace/delete capability-preflight
  останавливает операцию до первого Netlink-вызова, поэтому R1b ещё не включён
  в `Daemon` и не объявлен production commit-authority. Оба target слинкованы;
  focused suite прошёл 40/40 сценариев и 340/340 assertions, полный gate —
  3266/3266 и 35 748/35 748; независимый blockers-only review дал GO без P0/P1.
- Следующий wiring-срез подключает `RuntimeRoutingOperationOwner` к `Daemon`:
  отдельные mutable `route_table_` и `policy_rules_` удалены, initial setup,
  reconcile, lifecycle/rollback cleanup, interface wake и runtime-снимки идут
  через одного владельца. Вложенные manager-деструкторы для него разоружены,
  поэтому teardown выполняется один раз под тем же owner; обычные standalone и
  dry-run manager сохраняют прежний cleanup. Завершённый inventory публикуется
  атомарным immutable `shared_ptr`, поэтому status/SSE не ждут worker-held
  routing mutex. Interface wake при занятом owner сохраняется в
  дедуплицирующем pending-наборе и также не блокирует control loop.
- В основном runtime refresh совместимый route/rule reconcile теперь
  выполняется в worker до firewall backend. Fence поколения и route epoch
  повторяется уже после захвата combined owner. Успешный immutable snapshot
  строится из фактических manager-ledger после reconcile, поэтому неудавшееся
  удаление obsolete route/rule больше не скрывается желаемым планом. До первого
  Netlink write заранее выделяется консервативная публикация с явным
  `inventory_complete=false`: даже если точную post-operation копию нельзя
  выделить, уже применённое kernel-поколение не превращается в ложный mutation
  failure, а снимок не выдаётся за полный. Неизвестный эффект удаления rule
  теперь оставляет logical/owned ledger и защищает route-anchor той же таблицы;
  shutdown/rollback соблюдают тот же порядок. Отдельный
  `compatibility_cleanup_pending` не называется convergence. Признаки точности
  ledger и известности kernel-эффектов проходят в runtime-state: health отвечает
  unknown вместо ложного mismatch, а worker выдаёт `stale` и не допускает
  firewall до свежего авторитетного reconcile. Покрыты статические тестовые
  сценарии отказа до/после delete-effect, allocation-fallback, pre-mutation
  dump/fence failure и разных route/rule backend-объектов. Дополнительное
  fail-closed усиление сохраняет route-anchor при tracked-unowned, foreign и
  restart-orphan policy rule, отличает нормальное desired rule той же таблицы
  от чужой зависимости и не принимает ошибку orphan dump за пустой inventory.
  Неоднозначный rollback одной dual-stack family и неудавшееся восстановление
  replaced route заранее получают консервативную запись owner-ledger. Повторный
  startup использует live-aware repair. Exact identity публикует consumed
  high-water marks даже при ошибке до первой записи, но journal и fallback
  выделяются до consume, поэтому ранняя allocation failure оставляет identity
  повторяемым.
- Единый `RuntimeRoutingInventoryAuthority` теперь используется worker и всеми
  синхронными firewall apply. Incomplete/unknown routing и известный
  live-missing desired route не допускают backend COMMIT; health/API сразу
  получают false-маркеры при сохранённом firewall LKG. Interface-probe больше
  не открывает параллельный routing writer, а ставит coalesced central refresh.
  Route-preparation после выполненной route mutation переносится allocation-free
  через placement-new move-конструкцию, поэтому старый ABI GCC 8.4 не зависит
  от небезопасного предположения о `noexcept std::string::swap`, а ошибка строки
  не превращается в ложный ambiguous firewall COMMIT.
  После освобождения owner control loop только повторно сверяет
  exact context, публикует metadata и выдаёт ack;
  netlink mutation там больше нет. Это всё ещё compatibility
  offload, а не включение exact R1b: изменение epoch/внешний writer во время
  legacy netlink-фазы остаётся причиной немедленного coalesced retry, пока raw
  inventory/owned-preimage backend не завершён. Прежний production gate на
  Entware/Keenetic GCC 8.4 прошёл 281/281 build-шагов. Замороженный production-
  снимок прошёл host gate: preflight 18/18, monolithic 3295/3295 и 36 063
  assertions, 12 narrow-целей 756/756 и 14 374 assertions, crash smoke и
  production link/version; суммарно 4051 doctest-сценарий и 50 437 assertions
  без ошибок. Текущий post-snapshot batch 26 августа отдельно прошёл общий
  compile-only target-gate реальным GCC 8.4 с `BUILD_TESTS=ON`: завершён граф
  из 739 compile/link шагов и слинкованы ARM64 `keen-pbr`, monolithic
  `keen-pbr-tests`, `keen-pbr-runtime-firewall-owner-tests` и
  `keen-pbr-runtime-firewall-lifecycle-completion-tests`. Все четыре ELF имеют
  interpreter `/opt/lib/ld-linux-aarch64.so.1`; без QEMU они не исполнялись.
  IPK и роутер не менялись.
- Эти новые локальные срезы ещё **не закрывают firewall P0-1/P1/B1**: первичный
  backend apply, блокирующие route/link observation/planning и совместимый
  kernel route/rule reconcile первичной runtime attempt уже вынесены из
  control loop, но exact raw backend, daemon-specific publication tail,
  прямые cleanup/backend writers и синхронные apply-группы остаются. Все sync apply теперь
  fail-closed проверяют authoritative routing, но сами ещё выполняются в
  control loop. После проверенного safety-снимка отдельный startup-срез передал
  его legacy recursive firewall retry тому же
  `RuntimeFirewallOperationOwner` и уменьшил прямые вызовы с 11 до 10.
  Следующий локальный P0-1 restart-срез подключил эти prerequisites к
  production API: request guard передаёт точный уже выданный mutation lease
  без release/reacquire, HTTP worker ждёт exact-once
  `RuntimeFirewallLifecycleTerminal`, а control loop только принимает операцию
  в owner и не блокируется ожиданием worker. Один retained lease и тот же
  lifecycle source проходят queue/pre-worker failure, bounded successor и
  shutdown drain; rejection возвращает исходный lease, final terminal
  различает verified success, shutdown, not-verified и ambiguous COMMIT.
  Restart использует `RouteReconcileMode::Strict`, сохраняющий действующее
  поколение `FirewallApplyMode::PreserveSets` и обязательную проверку resolver
  reload/stream. Уже совершённый firewall COMMIT без подтверждённого resolver
  не считается успешным restart и не вызывает replay исходной мутации. Старый
  прямой синхронный `restart_routing_runtime()` удалён, поэтому число прямых
  production-вызовов `apply_firewall()` уменьшилось с 10 до 9.
  Следующий локальный START-срез передаёт уже выданный lease тому же owner и
  удаляет прямой синхронный `start_routing_runtime()`. Cache-only DNS остаётся
  до первой мутации; routes/firewall/owned conntrack выполняются worker-side,
  resolver `activate` — отдельным stream-worker с exact attempt-id/epoch.
  Кандидат не публикуется до подтверждения resolver; bounded transient retries
  равны 100/200/400 мс. Любой окончательный отказ, потерянный resolver control
  handoff либо ошибка обязательной runtime-публикации удерживает тот же
  terminal/lease до off-loop rollback routes, firewall и resolver. Поэтому
  на этом checkpoint прямых `apply_firewall()` оставалось восемь в пяти
  функциональных группах: cold startup, Keenetic DNS candidate/rollback,
  pre-apply SNAT repair, URLTEST candidate/rollback и общий synchronous
  config wrapper. Текущий owner-switch убрал из последней группы активный API
  config-save; на том checkpoint stopped-runtime save, SIGHUP и list-refresh
  ещё использовали прежнюю обёртку. Текущий SIGHUP/list-refresh owner-switch
  удалил её и все legacy call sites; stopped-runtime путь остаётся частью
  отдельного inactive-runtime/cold-start среза.
  В том же несобранном batch устранены terminal/liveness-разрывы: firmware
  netfilter refresh не отменяет foreground lifecycle timer; trailing source
  после verified START/restart отделяется в background successor; последняя
  route-epoch fence не позволяет объявить START успешным по устаревшему route
  plan: позднее изменение топологии расходует конечный START-бюджет
  100/200/400 мс и после него запускает rollback вместо бесконечного
  same-attempt defer. Четыре последовательных scheduler/executor rejection до
  `begin_worker()` завершаются не внутри transport-owner, а типизированным
  terminal Daemon; START выполняет обычный off-loop rollback, после чего lease
  освобождается до `not_verified`. Coordinator и queue mailbox получили
  небросающие ownership-locks, поэтому pre-worker transfer не зависит от
  ошибки platform mutex, а stale claim не создаёт второго владельца. Поздний
  SNAT/catalog delta после сохранения completion переносится в successor; при
  исчерпании transport-бюджета restart exact payload отделяется в один
  background resnapshot, а foreground lease освобождается перед
  `not_verified`. START имеет отдельный конечный бюджет из четырёх отказов
  передачи rollback worker и после его исчерпания завершает fail-closed
  finalizer вместо зависания в `starting`. Сбой сохранения необязательного
  background tail не меняет уже подтверждённый foreground result. Promoted
  watchdog делает не более одного launch за tick. Shutdown переводит
  foreground timer в typed terminal, а timer-id observation failure идёт через
  exact rejection terminal вместо ложного `consumed`.
  Обязательные resolver `activate` для START и `reload` для foreground restart
  теперь используют общий асинхронный `ResolverStreamCoordinator`: hook и
  15-секундное ожидание exact dnsmasq stream не блокируют control loop, terminal
  паркуется до fenced completion, а потерянный control wake подбирается
  watchdog. Restart без необходимости DNS refresh больше не получает ложный
  resolver failure; неуспешный reload передаётся существующему background retry
  без повторного firewall apply. Production hook не может зависнуть навсегда:
  общий KeeneticOS `safe_exec` ограничивает его 30 секундами, завершает process
  group с 2-секундным grace и затем `SIGKILL`; после успешного hook отдельно
  действует 15-секундный stream timeout.
  START resolver generation также строится по точному подготовленному
  native-VPN DNS access policy этой же lifecycle-операции, а не по ещё старому
  глобальному inventory до публикации кандидата. Тот же список участвует в
  вычислении expected hash и затем передаётся dnsmasq; отдельная регрессия
  закрепляет приоритет подготовленного кандидата над stale fallback.
  Финальный объединённый gate обоих lifecycle wiring-срезов прошёл: monolithic
  `3341/3341` и `36 767/36 767`, owner `75/75` и `1251/1251`, lifecycle
  completion `4/4` и `28/28`, все обязательные native/rescue/DNS/router-info/
  resolver/journal/lock narrow-цели и crash smoke. Реальный Entware/Keenetic
  GCC 8.4 собрал 285 target-объектов; IPK прошёл identity validator и
  изолированный install/reinstall/remove/recovery lifecycle. На том checkpoint
  P0-1 оставался открытым для пяти sync apply-групп, exact raw routing backend
  и дальнейшего
  извлечения daemon-specific publication tail. Следующий pre-generation SNAT
  barrier нельзя делегировать простым ранним исключением: API и SIGHUP приняли
  бы его за изменившийся runtime и запустили ложный rollback. Для этого среза
  требуется typed pre-apply-deferred continuation с тем же exact mutation
  lease, без ручного пользовательского повтора.
  Установленный Keenetic IPK `3.3.0-20260826231313`
  (`05997c8f81ff-dirty`, SHA-256
  `b56f111d2447a0db28a1da176776b446f9fb101e841127f2462478b4a581bb1f`)
  прошёл router acceptance. Первая candidate-попытка была корректно откатана:
  `status` через 6 секунд увидел штатное незавершённое initial URLTest
  поколение. Исправленный установочный gate дождался строгого `Overall: OK`
  под PENDING/update-lock, выдержал ещё 30 секунд и завершился `DEPLOY-OK`.
  Независимый postflight подтвердил точную binary identity, живые S79/S80,
  routing `Overall: OK`, свободную update-lock, неизменные SHA конфигурации и
  rescue `ready=true`, `rollback_available=true`, `pending=false`,
  `unknown=false`; прежний IPK `3.3.0-20260825071624` сохранён как rollback
  previous с SHA-256
  `e95b8085586f7c72724e429cb2c80fb7ad041e7dafd898edfbc1a6b54f4b95b4`.
- Для следующего pre-generation SNAT-среза подготовлен типизированный
  `config_preapply` foundation без включения незавершённого пути в production.
  Worker различает обязательный exact cleanup remainder и полный снимок,
  разрешённый только после собственного наблюдения missing SNAT; healthy,
  unknown, malformed generation/mask/IPv6 scope, неподдерживаемый cleanup mode
  и неоднозначный COMMIT не получают расширенной destructive authority.
  `RuntimeFirewallOperationOwner` переносит тот же move-only mutation lease и
  одноразовый `noexcept` continuation через bounded retry/defer и возвращает их
  только после типизированного `TerminalOwner::FinalizationProof`; обычный
  shutdown возвращает точный token, а деструктор не вызывает внешний callback.
  API guard умеет один раз принять обратно тот же физический lease и проверяет
  одновременно token и принадлежность исходному `RuntimeMutationAdmission`,
  поэтому foreign lease с совпавшим номером отклоняется. Узкий owner/terminal
  suite прошёл 80/80 сценариев и 1382/1382 assertions; затронутые production и
  test-объекты собраны реальным Entware/Keenetic GCC 8.4, итоговое независимое
  review не нашло оставшихся P0/P1. Старый синхронный barrier пока сохранён;
  полный suite, IPK и роутер этим foundation-срезом не запускались.
- Production config-save теперь передаёт уже выданный точный
  `RuntimeMutationAdmission::Lease` в адаптер демона и обязательно возвращает
  его в тот же API guard до интерпретации результата, rollback либо WAL commit.
  Адаптер пока вызывает прежний синхронный apply, поэтому этот срез не выдаётся
  за off-loop переключение. Короткий lease-bound handoff gate закрывает окно
  второго writer только на время текущего запроса: после exact return он
  снимается немедленно, а при нарушении внутреннего контракта — после
  emergency quiesce и фиксации recovery WAL при unwind запроса. Постоянного
  `shutdown`/poison, отдельного пользовательского восстановления и ручного
  повтора этот контракт не добавляет; неполная production-связка отклоняется
  до prepare, записи файлов и открытия WAL. Узкий gate прошёл 9/9 admission
  сценариев и 71/71 assertions; реальный Entware/Keenetic GCC 8.4 собрал шесть
  затронутых объектов с API и три без API. Независимое blockers-only review
  дало GO без P0/P1; полный suite, IPK и роутер не запускались.
- Router-info обновляется single-flight без RCI I/O под cache mutex. Принятый
  снимок живёт 30 секунд, конкурентные читатели получают stale LKG, а полный
  RCI failure повторяется не чаще раза в 30 секунд и не затирает LKG.
- После выхода URLTest child его принадлежащие потоки очищаются по умолчанию;
  временный отказ cleanup получает ограниченную повторную попытку.

### Исправлено

- nfqws → «Настройки»: Save не активен до реальных изменений; возврат
  исходных значений очищает dirty. Сохранение одного параметра не переписывает
  неизменённые строки стратегии. Ошибки загрузки/сохранения показаны в форме.
- Размеры «Добавить резервный маршрут» и «Подробность» согласованы со
  стандартными полями настроек; описания вкладки «Журнал» больше не сужены.
- Пустые карточки трафика при первом входе: единственный SSE-поток запускается
  после авторизации, а не получает 401 до неё. Сохранены reconnect, уведомления
  и пауза скрытой страницы; дополнительный polling не добавлялся.
- «Соединения»: завершённые TCP-сессии не считаются активными; недавняя
  история не вытесняется целиком активными записями. Предел кэша остаётся 1500,
  чтение большого /proc также ограничено по памяти. Порядок сортировки и
  пагинация не менялись. Локальные проверки объединённого среза 06.09:
  frontend 1183/1183, узкий backend 10/10; lint/i18n/literals/typecheck/WebUI
  build и компиляция production-объекта PASS. IPK ещё не собран и не установлен.
- Оставшийся технический текст ошибки проверки VPN использует существующий
  ключ RU/EN вместо английского пользовательского литерала.

- Локальный P1-срез перепроверки адресов автоматического `tunnel_probe`:
  существующий worker проверяет уже добавленные адреса даже без новых записей
  nfqws, в пределах общего лимита запросов. Обе ноги пробы привязаны к прежним
  интерфейсам; сама перепроверка не меняет список или маршрутизацию.
  Результаты и история возвратов сохраняются в ограниченном `<list_file>.review`
  (до 512 записей / 384 КиБ). После нескольких прямых успехов панель предлагает
  «Попробовать без туннеля» через прежнее явное удаление. При повторном возврате
  адреса порог повышается; исключения учитывают поддомены. Смена ISP/туннеля
  сбрасывает старые подтверждения, но не историю возвратов.
  API редактирует действующий список, а не путь из черновика. Coverage/очередь
  обновляются без перезапуска и повторного чтения старого лога; параллельные
  ручные изменения не затираются снимком, взятым перед пробой. Ошибка истории
  не блокирует обычное управление списком. Новых admission/recovery-слоёв,
  авторизации, таймеров или фонового UI polling нет. IPK и router acceptance
  этого среза ещё не выполнялись.
- Локальный P1-срез диагностики DNS-пути: карточка использует существующий
  одноразовый запрос и общий поток событий; отсутствие запроса не выдаётся за
  доказанный обход DNS или поломку. Добавлены RU/EN-подсказки для Android
  Private DNS, браузерного DoH и Apple Private Relay, ограничения существующего
  принудительного DNS и переход с фокусом на его настройку. Черновик и отсутствие
  данных не показываются как действующая настройка. Обрыв событий сразу
  завершает проверку; истёкший ручной тест можно явно запустить заново.
  Файервол, маршрутизация, авторизация и число запросов не изменены.
  Срез проверен локально; IPK не собран, на роутер не установлен.
- Локальный P1-срез ошибок обновлений и резервных копий: keen-pbr, sing-box,
  nfqws2, создание/чтение/восстановление копий и откат используют общую RU/EN
  подачу со свёрнутой технической причиной. Ошибка проверки доступности отката
  больше не скрывается. Подтверждённые результаты установки и предупреждение
  о возможном выполнении сохраняются; неизвестная ошибка не обещает откат.
  Диагностика nfqws сохраняет прежние ограничения объёма. Авторизация, запросы,
  повторы и порядок операций не изменены. Срез проверен локально, не установлен.
- В `make test` добавлены четыре уже существующие, но пропущенные NDMS resource
  test-цели; проверка полноты списка целей теперь проходит.
- Продолжение локального P1-среза ошибок: сохранение конфигурации и импорт
  VPN/подписок возвращают необязательные машинные коды известных причин.
  Панель выбирает RU/EN-текст по коду, сохраняя исходную причину в подробностях;
  старые ответы без кода поддерживаются. Неизвестный код не превращается в
  ложное сообщение об откате или занятости сервиса. При частичном импорте
  подписки код относится только к неудачному VPN. HTTP-статусы, запросы,
  повторы, блокировки и порядок применения не изменены. Изменения пока
  локальные; IPK не собирался и на роутер не устанавливался.
- Локальный P1-срез подачи ошибок сохранения и импорта: основной текст
  ConfigSaveErrorAlert, баннера Apply/discard, редактора VPN, переключения
  режима sing-box и импорта подписок переведён на RU/EN. Исходные причины,
  включая apply_error/recovery_error/rollback_error и ошибки полей, доступны
  в свёрнутых «Подробностях» без сериализации произвольного тела ответа.
  Восстановление конфигурации заявляется только по явному полному результату
  сервера; неизвестная причина получает нейтральный текст. Неизвестный отказ
  URL подписки больше не превращается в непереведённый ключ; действие для
  устаревшего preview сохранено. Backend, запросы, повторы и блокировки не
  менялись. Срез пока только локальный, WebUI/IPK на роутере не обновлялись.
- Локальный follow-up OpenConnect: NDM hook теперь обрабатывает перестройку
  таблицы `filter` для iptables/ip6tables через прежний `reapply-firewall`.
  Ранее hook игнорировал эту таблицу, хотя в ней находится `KeenPbrNativeFwd`:
  поздняя перестройка firmware могла удалить разрешения после link-события
  без уведомления сервиса. Новых процессов, таймеров и механизмов повтора нет;
  остановка сервиса сохраняет прежнее подавление teardown-событий.
  Regression воспроизвёл пропуск на старом hook; исправленный shell fixture
  и BusyBox syntax-check прошли. Fixture остановки также учитывает уже
  добавленную очистку NativeFwd. По разрешению владельца 05.09.2026 в
  17:10:38 MSK атомарно заменён только `/opt/etc/ndm/netfilter.d/50-keen-pbr-routing.sh`;
  прежний hook сохранён в backup, SHA-256 установленного совпадает с локальным.
  Restart/apply и сборка не выполнялись, PID демона остался `12340`;
  пакет прежний — `3.3.0-20260905112308` плюс этот hook-hotfix.
  Один reconnect после hotfix прошёл: при включённой галочке владелец
  подтвердил открытие 2ip.io и YouTube с первой попытки; новый ifindex oc0,
  FORWARD hook и DNS ingress подтверждены read-only. Функциональная приёмка
  OpenConnect на текущем Keenetic закрыта: ON/OFF, idle→connect и один reconnect
  подтверждены. Причина прежней долгой задержки не доказана; общая протокольная
  матрица и включение hotfix в следующую IPK остаются отдельно. Promotion,
  commit и push не выполнялись.
- В `3.3.0-20260905112308` исправлена конкуренция сохранения с фоновым
  firewall/URLTEST: ожидающий пользовательский запрос получает приоритет
  перед новым фоновым проходом в существующем механизме сериализации.
  Ожидание текущего фонового прохода увеличено с 5 до 30 секунд: штатный
  iptables-restore сам может ждать блокировку ядра до 10 секунд. Запрос формы
  не отправляется повторно; применение конфигурации остаётся последовательным.
  Тесты приоритета прошли 19/19; реальное сохранение при занятом worker
  ещё требуется подтвердить в панели.
- В `3.3.0-20260905112308` исправлено обновление OpenConnect idle→connect/reconnect:
  перед построением DNS/firewall обновляются текущие peer-интерфейсы из
  Netlink, даже если каталог службы подготовлен до подключения. Пулы службы
  и другие native-протоколы, включая IKEv2, не меняются.
  На Keenetic/iptables разрешается пересылка клиентов OpenConnect через все
  VPN-интерфейсы из включённых правил, включая вложенные группы и резервные
  участники; разрешение связано с исходным пулом, ingress, egress и меткой.
  Оно не ограничено одним сайтом или VPN и действует при включённой
  галочке. При выключенной галочке все назначения обходят маршрутизацию
  keen-pbr и идут напрямую через nfqws; при включённой direct-сайты также
  сохраняют обработку nfqws. Используются прежние apply/cleanup и S80
  fallback cleanup; новые процессы, владельцы операций и повторы не добавлены.
  Выбранные backend-тесты прошли 760/760; отдельный DNS/cache target — 16/16.
  После штатной установки runtime и группы восстановились без второго
  рестарта, DNS содержит активный oc0, FORWARD охватывает все VPN из правил.
  Владелец подтвердил 2ip.io через VPN и YouTube/direct через nfqws с галочкой.
  Затем подтверждён OFF: оба сайта напрямую с IP домашнего провайдера.
  После reconnect 2ip.io сначала не открылся, затем оба сайта восстановились
  автоматически без service restart. DNS ingress и маршрут присутствовали;
  поздний снимок подтвердил ESTABLISHED/ASSURED через AWG. Причина задержки
  и одного timeout IPC пока не установлена; OpenConnect P0 остаётся открытым
  именно по задержке восстановления, не по уже исправленным режимам ON/OFF.
- Исправлена совместимость существующего Meta-cleanup с mawk/BusyBox awk:
  выражения присваивания больше не разрываются сразу после `=`. Общий shell
  regression, включая очистку новых OpenConnect-правил, проходит.
- Создание рекомендованного DNS из списка передаёт пустые доменные привязки
  явно; форма списка не падает после добавления нового поля редактора DNS.

- Прямое создание VPN вместе с маршрутом больше не требует отдельного
  пользовательского черновика: существующий commit принимает отсутствие
  staged-конфигурации и по-прежнему сохраняет параллельный пользовательский
  черновик. Это устраняет отказ `staged configuration changed before candidate
  admission` при импорте подписки.
- Импорт ожидает завершения уже выполняющегося фонового переключения URLTEST
  в рамках имеющегося ожидания writer, вместо немедленного отказа с
  `urltest-selection-change`. Тело импорта повторно не отправляется.
- S79/S80 очищают одноразовые флаги установки и восстановления перед запуском
  долгоживущих процессов. Унаследованный `KEEN_PBR_PACKAGE_POSTINST` больше
  не блокирует обычный перезапуск transport-manager при откате после установки.

- Список нативных WG/AWG больше не выдаёт включённый интерфейс ядра за
  рабочее подключение: учитывается отсутствие соединения KeeneticOS и
  оценка связанного маршрута с дашборда. Подтверждённый отказ отображается
  как «Не работает»; отсутствие замера само по себе не считается отказом.
  Переключатель включения и периодичность автоматических проверок не менялись.

- Остановка transport-manager при откате создания VPN передаёт общий дедлайн
  также в ожидание firewall-мьютекса, проверку xtables и очистку правил.
  Отдельные 12-секундные команды больше не продлевают остановку за лимит init-
  сценария и не оставляют restart без запуска нового процесса. Первоначальная
  ошибка Apply сохраняется отдельно от последующей ошибки восстановления
  в журнале и API (`apply_error` / `recovery_error`).
- Кнопки редактирования и удаления подписок и VPN используют один компонент:
  одинаковые иконки, размеры, цвета, реакция на наведение и клики без открытия
  строки. На сенсорных экранах действия остаются видимыми. Подтверждение
  удаления подписки использует общий диалог удаления VPN.

- Обычное обновление nfqws2 больше не начинает экспорт резервного архива,
  который вызывал повторный запрос пароля. Дополнительное скачивание копии
  выключено по умолчанию; встроенная локальная точка восстановления остаётся.
- Ручной замер задержки выбранного VPN показывает новое измерение сразу по
  готовности, не ожидая общего обновления списка. Повторение того же значения
  в миллисекундах завершает индикатор; отказ запуска и неудачный замер видны
  сразу, старое успешное значение не выдаётся за новый результат. Одновременные
  ручные проверки разных строк не перезаписывают друг друга; уход со страницы
  прекращает ожидание. Без связанного маршрута кнопка объясняет причину,
  а не запускает проверку всех VPN. Автоматический cadence не изменён;
  короткое дополнительное чтение результатов работает только во время клика.
- После успешного применения конфигурации четыре независимых запроса
  обновления интерфейса запускаются параллельно, без последовательного
  ожидания каждого. Порядок применения на роутере не меняется.
- Карточка проверки сайта отдельно показывает результат с текущего устройства
  и с роутера, публикуя каждый по мере готовности. Ошибки DNS, TLS, соединения
  и timeout больше не объявляют весь сайт или VPN недоступным; HTTP-ответ
  сайта отличается от отказа API панели, подробная причина доступна раскрытием.
  Состояние транспорта `degraded` подписано «Есть проблемы» вместо «Не работает».
- Остановка демона отменяет загрузки URL-списков, начатые SIGHUP, через
  существующий HTTP cancellation token. Shutdown больше не ждёт таймаутов
  остальных источников; уже записанный кеш сохраняется, а отменённая
  конфигурация не применяется.
- Диагностика marked-FIB на KeeneticOS 4.9 теперь запрашивает у ядра
  фактически выбранную policy-routing table через `RTM_F_LOOKUP_TABLE`.
  Раньше интерфейс и метка определялись правильно, но результат мог ошибочно
  показывать main table `254` вместо фактической table `164`; сама
  маршрутизация при этом не менялась.
- Периодические проверки интерфейсов больше не принимают измеряемую
  ротационную пару за полный снимок конфигурации и не запускают лишний полный
  проход при каждом неизменившемся тике. Допустимый возраст результата
  учитывает 20-секундный cadence, размер ротации и время полного цикла всех
  настроенных интерфейсных выходов.
- Выбранный URLTEST-child больше не становится зелёным только из-за metric-0
  маршрута: свежая успешная проверка даёт `ACTIVE/HEALTHY`, ошибка —
  `DEGRADED`, отсутствие результата — `UNKNOWN`.
- Успешная проверка интерфейсов с целыми route/rule теперь остаётся read-only
  и не конкурирует с пользовательским сохранением через безусловный
  `runtime-firewall-worker`. Существующее центральное самовосстановление
  запускается лишь при точно пустом route/rule slot; конфликтующий,
  непредставимый или изменившийся снимок ничего не мутирует.
- Обычная загрузка Keenetic больше не исчерпывает короткий интерактивный
  START-бюджет до готовности netfilter. Cold boot сохраняет три быстрых
  наблюдения и дополнительно повторяет свежую публикацию через 30 и 60 секунд;
  API START остаётся быстрым. Регистрация URLTEST отделена от необязательного
  фонового обслуживания. Вместе со следующими двумя исправлениями срез вошёл
  в `33a3aaad`; аппаратная cold-boot приёмка первичного выбора пройдена.
- URLTEST-группы при START/cold boot больше не публикуются с пустым
  `selected_outbound`: валидный текущий child сохраняется, а при пустом или
  устаревшем выборе назначается первый валидный child по weight/order. Поэтому
  таблица группы с первой публикации содержит маршрут выбранного child вместо
  пустого selector с одним terminal `unreachable`, а последующие health probes
  могут переключить её на измеренного победителя. Новый
  recovery/retry-контур и пользовательское действие не добавлены. Focused
  regression прошёл `3/3` (7 assertions), production `daemon_runtime.o`
  собран. Исправление вошло в IPK `3.3.0-20260904083916`; после аппаратной
  перезагрузки группы без ручного Apply получили выбранные рабочие children и
  валидные default routes.
- Доменные назначения DNS-серверов из системной политики KeeneticOS больше не
  теряют часть активных scoped candidates: plain и encrypted candidates
  сохраняются одновременно, а наличие encrypted endpoint больше не удаляет
  рабочий plain candidate. Явное DNS-правило keen-pbr сохраняет приоритет над
  пересекающейся системной привязкой. Focused DNS tests прошли: parser — 87
  assertions, generator — 10, scoped group — 15; проверка diff чистая.
  Исправление вошло в ту же IPK и прошло настоящую reboot-приёмку:
  `nslookup youtube.com` успешен, системная политика не вернула `8.8.8.8`.
- Появление нового TUN-интерфейса во время импорта sing-box-подписки больше не
  запускает конкурирующий `runtime-firewall-worker`, пока исходное сохранение
  уже владеет runtime-мутацией. Событие остаётся в существующей очереди и
  присоединяется к тому же foreground lifecycle после передачи firewall-owner,
  поэтому импорт не откатывается из-за конфликта панели с собственной фоновой
  работой.
- Остановка transport-manager теперь соблюдает заданный срок, даже если
  переход общего sing-box-процесса ещё удерживает внутреннюю блокировку.
  Внутренний восьмисекундный бюджет оставляет запас до лимита Entware
  `rc.func`.
  Неудачный apply/rollback больше не должен оставлять transport-manager
  остановленным из-за зависшего `restart` и тем самым обрывать маршрутизацию
  через sing-box.
- Импорт sing-box-подписки снова является одним пользовательским действием:
  выбранные подключения и связанные маршруты создаются одной транзакцией,
  подключения сразу запускаются, а после публикации маршрутизации начинается
  их первый точечный замер. Внешняя форма больше не пытается повторно сохранить
  URL как отдельный транспорт, поэтому не остаётся лишнего черновика и не
  требуется вручную создавать маршрут или нажимать «Проверить».
- Предложение импортировать обнаруженную подписку получает фокус, а при
  отсутствующем или конфликтующем имени фокус переходит к подсвеченному полю.
  После полного успеха оба окна закрываются сами; внутренний счётчик попыток
  восстановления больше не показывается пользователю. По умолчанию выбирается
  одно новое подключение, а явный массовый выбор ограничен восемью, чтобы
  каталог провайдера не запускал сотни отдельных sing-box-процессов на роутере.
- Обычная пользовательская мутация может один раз в пределах пяти секунд
  дождаться завершения уже выполняющегося фонового firewall-worker. Это убирает
  ложную ошибку `Another runtime mutation is already in progress` без повтора
  запроса, нового recovery-контура или ожидания за другой foreground-операцией.
- Сохранение маршрутизации больше не отклоняется с ложной причиной
  `Configuration pre-apply owner is busy`, когда собственный фоновый
  firewall-owner уже закончил работу, но его terminal ещё ожидал следующего
  прохода control loop. Готовый background-terminal завершается в текущем
  проходе, его заменяемый таймер снимается, и тот же exact lease сразу
  передаётся в Apply без повторной записи конфигурации и без нового цикла
  восстановления.
- Каскадное удаление импортированного WG/AWG больше не откатывается до
  native-delete, если сохранение связанного маршрута встретило ещё
  выполняющийся или уже отделённый background firewall-successor. Тот же
  пользовательский запрос ограниченно ждёт текущий terminal, переносит
  актуальные recovery/catalog данные в foreground pre-apply и продолжает с
  той же exact lease; повторно нажимать корзину или восстанавливать операцию
  не требуется. Successor, появившийся во время terminal-drain, остаётся
  владельцем данных до следующего прохода; если foreground-start ещё не
  принят, exact recovery/catalog возвращаются в штатный background-defer.
  Устаревшие conntrack selectors и native-каталог другого runtime-поколения
  не переносятся.
- Перед публикацией нового поколения конфигурации foreground-операция снимает
  уже запланированный фоновый firewall-retry штатным owner-механизмом. Если
  активный SIGHUP всё же встретил выполняющийся background-owner, exact lease
  возвращается до одной свежей подготовки; лимит этого повтора сохраняется
  через writer-wait и смену поколения и не может превратиться в цикл.
- GitHub firewall-integration снова линкует raw-rtnetlink codec, а IPC-тесты
  используют отдельного test-only владельца текущего процесса; production-
  сокет остаётся строго root:root или root:keen-pbr.
- Короткая гонка KeeneticOS между публикацией RAW-цепочек и добавлением
  builtin-hook больше не откатывает применение конфигурации. Если в этом
  точном окне исчезает `KeenPbrRawCt`, iptables-backend ограниченно повторно
  публикует то же закреплённое IPv4/IPv6-поколение под тем же writer, не
  повторяя конфигурационную транзакцию и не оставляя ложную command failure
  после успешного восстановления.
- Корзина импортированного native VPN заранее называет связанные группы,
  правила и настройки, но не требует удалять ссылки вручную: после одного
  подтверждения панель каскадно убирает зависимости, маршрут и принадлежащий
  ей туннель.
- При неудачном «Применить и перезапустить» панель явно сообщает, что рабочая
  маршрутизация не изменилась, а выбранные значения остались в черновике.
  Точная причина сохраняется в нижней lifecycle-панели, даже если в
  колокольчике нет отдельной записи. Срез прошёл `3/3` focused backend cases
  (`15/15` assertions), `12/12` frontend tests, TypeScript typecheck, ESLint и
  проверку переводов.
- Для внутренней службы OpenConnect подтверждённые интерфейс и пул сохраняются
  при кратковременной недоступности NDMS; другие внутренние VPN-службы не
  изменены. Ранний вариант «OFF меняет только DNS» заменён в
  `3.3.0-20260905112308`: OFF пропускает все сайты напрямую через nfqws,
  ON использует VPN-правила и сохраняет nfqws для direct-сайтов.
- Сборка Keenetic IPK теперь передаёт заданный лимит `KEEN_PBR_JOBS` также
  вложенному Ninja. Раньше Ninja игнорировал jobserver внешнего `make`,
  запускал компиляцию на всех ядрах и мог завершить backend cross-build по OOM
  даже при `KEEN_PBR_JOBS=1` или `2`. Лимит передаётся через отдельный
  исполняемый wrapper без аргументов в имени команды: поэтому чистая
  конфигурация зависимостей Meson видит Ninja, а не ошибочный путь вида
  `ninja -jN`.
- Прямой push в `alpha` проверяет Clang thread-safety на всём production-
  бинарнике и отдельном negative smoke, не собирая повторно полный тестовый
  монолит. PR, `next`, теги и ручной запуск сохраняют полный `clang-check`;
  его timeout увеличен до 120 минут, поскольку текущая матрица уже превышает
  прежний 75-минутный предел.
- Keenetic IPK собирается параллельно с длинной backend-матрицей после готовых
  frontend и transport-manager. Финальная публикация `alpha`, `next` и release
  по-прежнему явно ожидает успешные backend, crash, Clang и firewall gates,
  поэтому ускорение не позволяет опубликовать непроверенный пакет.

- Явное `recover-pending` теперь может завершить уже записанное пакетное
  восстановление с обычным маркером `UNKNOWN`, но только внутри той же
  подтверждённой update-lock транзакции. Успешное восстановление удаляет
  `UNKNOWN` лишь после проверки runtime и долговечной синхронизации; при ошибке
  `PENDING` и `UNKNOWN` сохраняются. Обычный запуск, небезопасный тип маркера и
  persistent recovery по-прежнему блокируются без нового пользовательского
  сценария. Rescue suite прошёл `88/88` сценариев и `3458/3458` assertions;
  отдельный lock/guard script и BusyBox shell syntax также зелёные.
- Журнал DNS-наблюдений dnsmasq в `/tmp` теперь ограничивается демоном раз в
  минуту, даже если страницу подключений никто не открывает. Переросший 2 MiB
  файл не читается целиком API и обнуляется в том же inode, поэтому открытый
  append-дескриптор dnsmasq остаётся действующим, а журнал больше не может
  незаметно занять весь tmpfs роутера. Узкая exact-inode цель прошла `4/4`
  сценария и `21/21` assertions; production daemon полностью слинкован.
- Ошибка применения конфигурации больше не прячет действия над сохранённым
  черновиком за lifecycle-плашкой. Если черновик остался после отказа, в той же
  нижней панели доступны обычные «Применить и перезапустить» и «Отменить» без
  дополнительного восстановления или перезапуска сервиса. Таблица состояний
  прошла `8/8` focused frontend tests, TypeScript typecheck и узкий ESLint.
- Редкий сбой ответа после фактически выполненного `RTM_NEWRULE` больше не
  оставляет созданное policy rule и его route anchor как безусловный
  `partial_unknown`. Повторно наблюдённое точное правило получает durable
  ownership receipt и проходит штатный exact rollback. Если identity успела
  измениться, правило и маршрут сохраняются для следующей точной owner-очистки;
  broad delete не используется.
- Обязательный `make test` снова перечисляет все объявленные узкие CMake-цели.
  В список добавлены новые IPC и URLTEST targets, а также ранее пропущенные
  `keen-pbr-runtime-firewall-publication-tests` и
  `keen-pbr-runtime-internal-vpn-lkg-tests`; coverage gate больше не роняет
  backend CI до начала самих тестов.
- URLTEST после штатного `opkg`/START больше не зацикливается на
  `admitted=false`: при занятом writer или отказе typed handoff менеджер
  возвращает курсор к уже применённому child, а один отложенный проход
  передаётся существующим generation gate и центральному firewall reconcile.
  После освобождения lifecycle lease этот проход возобновляется через уже
  существующую durable admission-очередь owner: при одновременных initial
  selectors терминал первого надёжно передаёт ожидающий проход без ручного
  «Применить», нового recovery-контура, нового круга external-health probes и
  лишнего второго central pass.
- Удалены временные установочные обходы этого дефекта: `postinst` больше не
  ждёт 20 секунд и не делает условный второй restart, а `install.sh --update`
  не выполняет отдельный дополнительный restart. Установка и обновление снова
  используют штатный одиночный запуск `S80keen-pbr`.
- Диагностика `iptables` больше не показывает однопроходную ложную деградацию
  при переключении физического A/B-поколения статических `ipset`: verifier
  канонизирует logical/A/B-имена с обеих сторон. В русской локали переведены
  статусы, действия, области проверки и известное сообщение об отсутствующем
  правиле; технические идентификаторы, `fwmark` и имена таблиц остаются без
  изменений.
- Прямой interface-child URLTEST теперь проверяется с привязкой сокета к его
  интерфейсу; nested selector сохраняет прежнюю проверку только по fwmark.
  После первого отказа выбранный direct-child в режиме приоритета возвращается
  только после `success_threshold` последовательных привязанных успешных проб.
  Это убирает ложный успех и ранний failback, но не объявляет устранённым
  первичный AWG data-plane отказ, возникающий под направленным трафиком.
- Редактор URLTest-групп больше не предлагает сохранить заведомо недопустимые
  ступени, ссылки, URL, числовые значения и режимы conntrack. Отсутствующее
  значение режима остаётся отдельным автоматическим выбором и не подменяет
  явно заданный `preserve`; ошибки backend привязываются к полю или секции.
- Два последовательных GitHub Actions failure разделены по причинам. Run
  `33203448079` выявил coverage guard, в котором отсутствовали два уже
  существующих обязательных test target; CI-only коммит `0b6b39b6` добавил их
  в матрицу. В run `33204658857` все 15 записанных падений относились к
  устаревшему test harness, а не к production build/runtime: восемь reload-
  тестов не подключали `try_acquire_runtime_mutation_handoff_gate_fn`, а семь
  iptables-fakes считали read-only capability probe `-w 0` реальной мутацией.
  Отдельно при разборе найден реальный production-дефект: API START/restart
  забирал production mutation lease без предварительного exact handoff gate.
  Теперь недоступный lifecycle-owner даёт 503 до мутации вместо внутреннего
  `logic_error`. Локальный production binary и monolithic test target
  перелинкованы с кодом 0; точные фильтры прошли `8/8` (75/75 assertions) и
  `7/7` (15/15 assertions). Этот batch пока не отправлен в GitHub и не входит
  в установленный на роутере baseline `31c12a58`.
- Колокольчик больше не показывает десятки одинаковых записей о кратких сбоях
  Meta/WhatsApp UDP/443 и PPE de-offload: внутри одного процесса сохраняется
  только самый новый инцидент каждого вида, а записи предыдущего процесса
  снимаются лишь после найденного в том же журнале запуска совпадающих
  build/commit. Состояние текущего процесса не объявляется исправленным без
  доказательства; сравнение по порядку строк не зависит от часового пояса или
  коррекции часов роутера.
- Управляющие вызовы `iptables` и `ip6tables` теперь ограниченно ждут общий
  xtables lock, учитывая оба реально встречающихся диалекта: современный
  `-w <секунды>` и KeeneticOS `iptables 1.4.21` с одним `-w`. Capability
  определяется отдельно для IPv4/IPv6 и не кэшируется по временному Busy.
  Внешний deadline остаётся обязательным; если завершение probe или самой
  команды нельзя доказать, фактическая операция не повторяется, а частичный
  вывод очищается до всех parser'ов и не может ложно доказать отсутствие
  цепочки. Живая read-only проверка подтвердила bare `-w` на установленном
  Keenetic; изменённые production/test-объекты собраны GCC 8.4, без IPK и
  установки на роутер.
- Панель стратегий nfqws больше не назначает первую стратегию каталога, когда
  backend честно сообщает, что активный `nfqws2.conf` не совпал ни с одной из
  них. Разбор показывается только для применённой или явно открытой стратегии,
  заголовок называет просматриваемый разбор, а не состояние системы. Кнопка
  «Сохранить» не создаёт byte-identical override встроенного профиля и не
  замораживает автоматическую подстановку WAN; «Применить» недоступна для ещё
  не сохранённого нового черновика. Кнопка «Сохранить текущую как стратегию»
  доступна только для загруженной собственной конфигурации; у уже сохранённого
  встроенного или пользовательского профиля она выключена и сообщает имя
  существующей стратегии. Четыре фронтенд-коммита Claude интегрированы с
  сохранением исходной истории.
- Английская backend-причина `disabled by configuration` в русской карточке
  диагностики теперь отображается как «Отключено в настройках»; неизвестные
  диагностические подробности сохраняются без подмены. Ошибка сохранения
  привязки уже созданного native VPN также локализована и явно запрещает
  повторный импорт; восемь внутренних delete-диагностик, которые никогда не
  выводятся пользователю, документированы в allowlist.
- Версия установленной сборки теперь имеет единый вид
  `v3.3.0-ГГГГММДДччммсс` в daemon API, шапке, package control и frontend
  marker. Строгий IPK-validator сверяет timestamp, binary identity, активный
  JavaScript и реально отдаваемые gzip-копии `index.html`/JS; старый `sb.12`,
  stale entrypoint и несовпадающие package/frontend данные отклоняются. Source
  identity учитывает untracked-файлы, которые упаковщик копирует в build tree,
  поэтому пакет больше не может выглядеть собранным из clean commit при наличии
  неопубликованного исходника.
- Текущий предсборочный gate после этих изменений прошёл: frontend `904/904`
  (2633 assertions), identity/IPK-validator `29/29`, monolithic C++
  `3342/3342` (36 773 assertions), а также все обязательные native, rescue,
  DNS, resolver, lock и runtime-firewall standalone-наборы. Новый IPK после
  этого объединённого checkpoint на момент записи ещё не собирался.
- Восстановление встроенной стратегии nfqws больше не записывает tombstone,
  который сразу снова скрывал её после reload. Для неактивной стратегии
  заголовок разбора берётся из builtin-каталога. Изменение перенесено из
  Claude-коммита `4a7212e3` локальным интеграционным коммитом `05997c8f`;
  добавлены пять backend-регрессий, frontend и transport-manager gates прошли.
- Завершение `RuntimeFirewallOperationOwner` больше не оставляет mutation lease
  во внешне удержанном context и не вызывает внешний terminal callback после
  разрушения owner. После остановки worker/timer очищаются lease, operation,
  terminal owner и callbacks, а lifecycle source переводится в однозначный
  `not_verified`; owner-suite закрепляет отсутствие callback re-entry и
  исправляет точную START/restart failure injection.
- В новом lifecycle completion удалено повторное неверное предположение о
  `noexcept std::string::swap` на Entware GCC 8.4. Заранее подготовленный
  terminal теперь публикуется placement-new move-конструкцией; после входа в
  `settle()` нет строкового allocation/swap-шва.
- Для GCC 8.4 со старым ABI libstdc++ устранено неверное предположение, что
  move-assignment результата со `std::string` всегда `noexcept`. Точный
  worker-result теперь конструируется через заранее выделенный holder и
  `optional::emplace`, а потенциально бросающее сохранение terminal detail
  выполняется до checkpoint `prepared`; после entered COMMIT не добавлен новый
  fallible publication seam и неоднозначная операция не переигрывается.
- Ранний отказ неавторизованного native/API-запроса теперь всегда получает
  `Cache-Control: no-store` ещё до чтения тела. Удаление native VPN и очистка
  tombstone явно проверяются с обычной действующей сессией без повторного ввода
  пароля; устаревшие ожидания step-up удалены из интеграционных тестов.
- Обязательный `make test` больше не пропускает отдельный
  `keen-pbr-native-direct-observation-tests`. Инъекция смены типа интерфейса в
  cooperative-delete тесте теперь действует в обеих областях точного guard и
  не зависит от исторического номера чтения каталога.
- Обязательный gate теперь включает отдельные
  `keen-pbr-runtime-firewall-owner-tests` и
  `keen-pbr-runtime-firewall-lifecycle-completion-tests`; статическая сверка
  Makefile/CMake прошла 4/4. Assertions над `shared_ptr` в owner-тесте явно
  приводятся к `bool`, поэтому doctest 2.5.1 больше не пытается форматировать
  сам указатель и не останавливает host-компиляцию.
- После удаления native WG/AWG вместе с интерфейсом и маршрутом удаляется его
  локальная карточка псевдонима/страны. Осиротевшие карточки больше не создают
  отдельную вкладку KeeneticOS с уже отсутствующими `WireguardN`.
- Удаление импортированного WG/AWG с пользовательским псевдонимом больше не
  блокируется как `snapshot_unreadable`: зашифрованный снимок восстанавливает
  исходную каноническую подпись вместе с псевдонимом.
- Фоновая финализация удаления корректно учитывает различие runtime-каталога и
  running-config KeeneticOS. Другие существующие WG/AWG больше не блокируют
  удаление локального снимка и tombstone уже исчезнувшего интерфейса.
- Импорт WG/AWG больше не останавливается на валидном Keenetic running-config, не зависает в `executor_blocked` после фактически созданного интерфейса и корректно принимает интерфейс, который появился после неоднозначного ответа.
- После успешного или восстановленного импорта конфигурация KeeneticOS включается и сохраняется, конфигурация keen-pbr обновляется, а новый маршрут появляется без ручного обновления страницы.
- Завершение native-импорта больше не теряет введённые псевдоним, выбор
  связанного маршрута и разрешённое автоопределение страны, если ответ Keenetic
  пришёл после закрытия модального окна или обновления этой вкладки. Сохраняется
  только несекретный план оформления; URI и ключи в него не попадают. Ответ
  восстановления `no_work` теперь сначала сопоставляется с уже созданным
  интерфейсом по принятому псевдониму, а не стирает план маршрута и страны.
- Псевдоним импортируемого WG/AWG теперь передаётся в фактически используемое
  KeeneticOS имя импортируемого файла, а не только в игнорируемый прошивкой
  комментарий `# Name`. После подтверждённого импорта описание интерфейса в
  самом Keenetic становится ровно выбранным человеком именем; служебная метка
  переносится в невидимое поле peer и больше не добавляется к названию. Обычные
  редактирование, восстановление и удаление через корзину при этом сохраняются.
- Для native WG/AWG результат разрешённого автоопределения страны сохраняется в
  карточке после создания и используется как флаг даже при отсутствии серверного
  адреса в несекретном native-tracker. Для ранее добавленного native-туннеля
  режим «Автоматически» определяет страну по адресу, фактически видимому через
  его точный интерфейс.
- Импортированный интерфейс можно переименовать, указать ему страну, редактировать тем же карандашом и удалять той же корзиной, что и остальные записи. Флаг выбранной страны теперь показывается непосредственно в строке native-интерфейса.
- Разбор `.conf` принимает legacy-параметры AmneziaWG 1.0–1.5 и корректно различает допустимые необязательные значения вместо общей ошибки «параметр имеет недопустимое значение».
- Удаление сначала применяет удаление связанного маршрута, а при известном отказе Keenetic восстанавливает его. Отсутствующий диагностический снимок зависимостей без единой найденной ссылки больше не блокирует навсегда удаление принадлежащего панели туннеля; реально найденные ссылки по-прежнему показываются и блокируют операцию.
- Корзина native VPN стала единственным пользовательским действием удаления: один клик запускает удаление самого VPN и связанного маршрута без второго диалога, ввода имени, галочек, отдельного восстановления и повторного пароля в уже авторизованной сессии.
- Если Keenetic уже удалил native-интерфейс, панель автоматически завершает и удаляет служебную запись, сбрасывает кэш и убирает исчезнувшую строку. Служебные записи отката и их проверки больше не показываются пользователю отдельным разделом и не запускают скрытую повторную авторизацию при открытии страницы.
- Удаление отключённого WG/AWG теперь завершается и тогда, когда исчезнувший интерфейс уже нельзя найти в списке устройств Linux: панель восстанавливает штатное имя `nwgN`, автоматически удаляет служебный снимок завершённого удаления и сразу освобождает импорт из файла или URI.
- Исправлены ложные «результат импорта неизвестен», бесконечные межвкладочные блокировки, подмена семейства восстановления и утечка внутренних идентификаторов через подпись интерфейса.
- Проверка доступности сайта больше не полагается только на один HTTP-ответ роутера и различает доступный пользовательский путь, маршрут через туннель и обработку nfqws2.
- Восстановлено плавное перемещение индикатора вкладок в стиле KeeneticOS: глобальная настройка сокращения движения больше не превращает его в мгновенный прыжок, а само подчёркивание перемещается композитной анимацией и не срывается при изменении раскладки страницы.
- Усилена аутентификация после смены пароля роутера; устаревшие сессии не продолжают работать, а растяжение ключа ограничено на уровне всего роутера.
- Обновление sing-box не перезаписывает чужой или недоступный для проверки бинарник, корректно отменяется и не оставляет ложного доступного отката.
- Исправлены мобильная компоновка строк и обычная корзина удаления native WG/AWG, кнопки обновлений и проверки, сообщения уведомлений и привязка результата проверки транспорта к точному интерфейсу. Из раскрытой строки убраны внутренние диагностические пояснения о готовности управления; неизвестная роль больше не отключает управление клиентским WG/AWG.
- После передачи WG/AWG в Keenetic окно добавления больше не считается формой с
  несохранёнными изменениями; быстрое автоматическое завершение закрывает его
  после создания карточки и маршрута. Из превью удалено внутреннее пояснение о
  локальном структурном разборе, не помогающее пользователю выполнить импорт.
- Если Keenetic принял одноразовый WG/AWG import и потребовал фоновое завершение,
  окно один раз показывает состояние завершения и автоматически закрывается без
  вопроса о несохранённых изменениях. Привязка маршрута, применение конфигурации
  и автоопределение страны продолжаются на странице списка и больше не ждут
  ручного закрытия модального окна.
- После автоматического закрытия окна импорта в правом нижнем углу показывается
  короткий индикатор «VPN импортируется». Он сменяется подтверждением и исчезает
  только после создания записи, привязки маршрута и завершившегося перезапуска
  transport-manager.
- После распознавания native URI поле с секретной ссылкой блокируется, а фокус
  переходит к безопасному превью. Попытка продолжить без имени возвращает фокус
  к полю псевдонима и заметно выделяет предложенный вариант. Пока импорт и
  привязка маршрута не завершены, нельзя открыть ещё одну форму добавления, а
  импортированный интерфейс не показывается как внешний туннель KeeneticOS.
- `RulesOnly` refresh сохраняет точные static generations предыдущего apply и
  не сообщает designed self-heal внешнего dispatcher как системную аварию.
- nfqws2/package mutation больше не теряет собственный журнал при boot recovery,
  не запускает maintainer scripts вне владельца транзакции, не зависит от
  порядка старта Entware SSH listener и пишет шаги в журнал именно той
  транзакции, где они произошли. Обновление и packet capture не требуют
  повторного пароля в уже авторизованной сессии.
- Frontend API client повторно сгенерирован из текущей OpenAPI-схемы, поэтому
  модели панели и backend-контракт снова совпадают byte-for-byte по генератору.

## [3.1.1-sb.12] — подготовительный журнал до перехода на 3.3.0

### Добавлено

- Мастер первого запуска проводит через создание связки «туннель + маршрут» одной транзакцией, выбор сервисов из каталога и проверяемое применение результата. Можно использовать уже существующий маршрут; промежуточный черновик и повторная отправка всей конфигурации не создаются.
- Снимок последней неудачной привилегированной команды в диагностике. Запись ограничена по размеру, атомарна, очищена от секретов и некорректного UTF-8 и не создаёт отдельного постоянно опрашиваемого API.
- Интеграционные сценарии восстановления firewall после частично удалённых или повреждённых цепочек, повторного применения конфигурации и сохранения чужих правил и битов меток.
- Опциональный экспериментальный режим `daemon.meta_udp443_policy: messages_first` отклоняет только UDP/443 к адресам штатного IP-компаньона Meta/WhatsApp с меткой его маршрута. Клиент может перейти на TCP, что иногда ускоряет первую доставку, но живой тест Android также показал последующее зависание сессии WhatsApp без отправки сообщения. Поэтому рекомендуемым остаётся `balanced`; интерфейс прямо предлагает вернуться к нему при сообщениях без галочек. Режим не блокирует UDP/3478, UDP/5349 и P2P-медиа на высоких портах; предупреждение также описывает влияние на Instagram и другие сервисы Meta, возможную дополнительную задержку звука звонка на 10–20 секунд и необходимость отключённой IPv6-маршрутизации до появления проверенного IPv6-покрытия. Owned-правило проверяется и удаляется также после аварийного завершения демона и при удалении пакета.

### Изменено

- Новая установка sing-box зафиксирована на уже установленной и проверенной версии `1.13.14`: установщик больше не запрашивает и не предлагает непроверенный последний выпуск. Уже выбранный локальный бинарь не заменяется автоматически.
- Бэкенд маршрутизации синхронизирован с архитектурными изменениями keen-pbr 3.1.1: состояние iptables применяется через две сменяемые генерации, а стабильные dispatcher-цепочки переключаются только после успешной подготовки и проверки новой конфигурации.
- Содержимое ipset читается через строгий XML-разбор с ограничением объёма до изменения рабочего состояния. Сети `/0` безопасно разворачиваются в две половины адресного пространства.
- Правила `multiport` автоматически делятся с учётом лимита xtables в 15 слотов. Имена интерфейсов проходят единую строгую проверку до формирования команд firewall.
- Интерфейс списков, DNS, резервных копий, настроек, маршрутов и туннелей стал компактнее: связанные настройки собраны вместе, таблицы используют единые строки и действия, а в формах и группах приоритетны понятные пользователю имена вместо технических идентификаторов.
- Конфигурации sing-box в общем и изолированном режимах явно задают DNS-resolver для доменных адресов и совместимы с требованиями sing-box 1.12 без включения устаревшего режима.

### Исправлено

- Для штатного списка IP-адресов WhatsApp зависшее соединение подтверждается отдельной одноразовой проверкой через одну секунду. Остальные списки сохраняют общий осторожный интервал, а действующий звонок, ответивший поток и чужие биты метки по-прежнему защищены от очистки.
- Восстановление отсутствующих dispatcher-цепочек и builtin-hook больше не требует полного удаления управляемого firewall-состояния и не затрагивает чужие цепочки.
- Повреждение только одной A/B-генерации теперь исправляется из подтверждённой рабочей генерации; если достоверного состояния нет, применение завершается безопасно до изменения ipset.
- Очистка NAT не выполняется раньше проверки, что целевая генерация действительно неактивна.
- Диагностический снимок не перезаписывается подавленными probe-командами и корректно обрабатывает таймаут, сигнал, ошибку `fork`/`pipe` и ненулевой код завершения.
- Маршруты и policy rules с протоколом владения keen-pbr сходятся после исчезновения или конкурирующей замены: сервис заменяет только собственное конфликтующее состояние, сохраняет чужие записи и откатывает незавершённое изменение.
- `ndmc` и системные сценарии Keenetic запускаются без унаследованного Entware `LD_LIBRARY_PATH`, поэтому системная утилита больше не подхватывает несовместимую `libc`; диагностический вывод при этом сохраняется.
- Транспорт считается исправным только после свежей успешной проверки, результат которой можно однозначно связать с его интерфейсом. Отсутствующий, устаревший или неатрибутируемый результат больше не даёт ложный зелёный статус.

## [3.0.7-sb.11] — 2026-07-22

### Добавлено

- **Резервное копирование и восстановление.** В настройках можно собрать выборочную копию общих настроек, транспортов, исходящих соединений, DNS, списков, правил и nfqws2. Перед обновлением и восстановлением создаётся rollback-копия, которую можно скачать на компьютер.
- **Анализ зависимостей до удаления.** У списков и исходящих соединений заранее показано, какие правила, DNS-серверы и группы от них зависят; связанные объекты открываются по ссылке.
- **Имена интерфейсов из NDMS.** На дашборде используется имя, заданное в прошивке, в настройках показываются пользовательское и техническое имена, а в соединениях системное имя перенесено в подсказку.
- **Расширенная информация о транспортах.** В карточке видны протокол, защита, SNI, сетевой транспорт и страна сервера. Страну можно выбрать вручную, определить через внешний сервис с месячным кэшем или не показывать вовсе.
- Выключатели sing-box и nfqws2 в карточке служб и ручная проверка задержки для нативных и управляемых туннелей.
- **Защищённый внешний доступ к панели.** Порт открывается только по явному выбору пользователя и при включённой авторизации; разрешённый доступ не создаёт ложное системное предупреждение.
- Импорт и экспорт исходящих соединений, транспортов и файлов nfqws2. Полный экспорт транспортов выполняется отдельным авторизованным запросом и содержит необходимые для восстановления секреты с явным предупреждением перед скачиванием.
- Безопасное обновление nfqws2: изменённый пакетный `nfqws2.conf` сохраняется как стратегия `default (ГГГГ.ММ.ДД)`, а изменение только `IPV6_ENABLED=0` не создаёт лишнюю копию. Встроенные стратегии автоматически получают активные WAN-интерфейсы.

### Изменено

- Интерфейс переработан в знакомой для пользователей KeeneticOS/NDMS логике: единая типографика и синяя палитра, компактные формы и карточки, светлая и тёмная темы, фиксированные панели действий и новый логотип. В пакет включены только необходимые кириллические и латинские WOFF2-наборы Roboto.
- Мобильные списки и правила получили отдельную компактную компоновку, работающий выбор нескольких строк и перетаскивание на pointer-событиях. Массовые действия перенесены в непрозрачную нижнюю панель, а мобильная шапка остаётся закреплённой.
- Раздел последовательно переименован из «Интерфейсов» и «Точек выхода» в **«Исходящие соединения»**. Пользовательское слово `outbound` убрано из форм и подсказок; технические теги и формат конфигурации не изменились. nfqws2 перенесён рядом с транспортами.
- Карточки транспортов и дашборд стали компактнее и показывают полезное состояние: активный маршрут, резерв, протокол, задержку и зависимости вместо внутренних PID и повторяющихся технических статусов.
- Kill-switch в форме исходящего соединения получил понятное описание каждого режима, а выбор интерфейса — метку протокола (`AWG/WG`, VLESS, OpenVPN, IKEv2 и другие).
- Проверка соединений и транспорта использует общий кратко кэшируемый снимок здоровья. FORWARD-правила проверяются раз в 30 секунд, conntrack разбирается не чаще раза в две секунды, имена устройств кэшируются на минуту, история ограничена 1500 соединениями, а невидимые строки таблицы не отрисовываются.
- HTTP-запросы, внешние команды и их вывод получили ограничения времени и объёма. Статические файлы отдаются потоково, версионированные ассеты кэшируются браузером.
- Сохранение конфигурации и восстановление резервной копии выполняются транзакционно с атомарной записью и возвратом предыдущего runtime-состояния при ошибке.

### Исправлено

- Проверка маршрута принимает кириллические домены и URL: имя переводится в punycode штатным браузерным разбором.
- Кратковременное расхождение во время замены маршрутов больше не вызывает ложное `unexpected route present in table`: ошибка подтверждается повторным снимком через 200 мс.
- PID-файл обновления проверяется через `kill(0)` и `/proc/<pid>/cmdline`; устаревший файл удаляется. Прогресс обновления читается из локального атомарного состояния без повторного обращения к GitHub каждые три секунды.
- Выключенный пользователем IPv6 больше не создаёт предупреждение. Разбор `iptables-restore` понимает сообщение `line N failed` без двоеточия и показывает проблемное правило.
- Пустой ответ NDMS об именах интерфейсов теперь кэшируется, а неожиданный тип полей `connected` и `link` не роняет весь запрос.
- Ссылка TUIC без учётных данных больше не приводит к ошибке разбора.
- Статус исходящего соединения не помечается ошибочным «транспорт не отвечает», если фактический ping через него успешен.
- Бинарные `.lua.gz` в резервной копии кодируются в Base64 и не вызывают `invalid UTF-8 byte`. Ограничены размер, количество и пути файлов; при ошибке восстановления nfqws2 выполняется автоматический rollback.
- Исправлены выбор правил на телефоне, расписание cron, прокрутка редакторов nfqws2 и каталога, а также удаление списка из правила с другими оставшимися условиями.
- Кнопки проверки обновлений keen-pbr-sb и nfqws2 всегда сообщают результат, в том числе когда новых версий нет.
- Флаг страны формируется из ISO-кода в браузере, а ручной ввод заменён локализованным списком стран с теми же эмодзи.
- Экспорт транспортов больше не теряет share-link, outbound JSON, UUID и остальные параметры подключения. Повседневный endpoint остаётся обезличенным; секреты возвращает только отдельный авторизованный экспорт.

## [3.0.7-sb.10] — 2026-07-20

### Исправлено

- **Резервные DNS-серверы не перетаскивались.** Строка получала вид «места, откуда её забрали», уже в момент нажатия на ручку, а этот вид прячет её содержимое. Указатель оказывался над невидимым элементом раньше, чем браузер успевал начать перетаскивание, и жест отменялся, не начавшись. Нажатие теперь только разрешает строке двигаться, а серую пунктирную заготовку она принимает, когда перетаскивание действительно началось.

### Изменено

- Доступ к панели снаружи убран из интерфейса: на части прошивок правило межсетевого экрана не переживало перенастройку сети, а полурабочая точка входа в панель управления — не то, что стоит показывать. Вернётся, когда будет надёжно работать.
- README короче: раздел разработки и инструкция сопровождения форка убраны.

## [3.0.7-sb.9] — 2026-07-20

### Исправлено

- **Обновление из веб-интерфейса оставляло dnsmasq на старой конфигурации, и доменная маршрутизация переставала работать.** Помощник перезапускал dnsmasq через init-скрипт и считал успехом сам факт вызова. На Keenetic PID-файл регулярно расходится с реальным процессом: остановка сообщала `failed`, запуск — `already running`, dnsmasq продолжал работать со старым `conf-script`, а вместе с ним пропадали и директивы ipset, и TXT-запись с хешем. Внешне это выглядело как вечный баннер «требуется перезапуск», и лечилось только перезагрузкой роутера. Теперь процесс ищется по имени, а не по PID-файлу, остановка ждёт завершения и при необходимости применяет KILL, устаревший PID-файл удаляется, а результат перезапуска проверяется. Само обновление дополнительно переподключает конфигурацию и пишет в журнал, удалось ли это.

### Добавлено

- **Раздел каталога в интерфейсе** — 87 заготовок по категориям, с поиском, отметкой уже добавленных и выбором, куда направлять их трафик. Отдельно выбирается интерфейс, через который скачивается сам каталог: он лежит на GitHub, то есть ровно там, куда многие и не могут дотянуться напрямую. Выбранный маршрут запоминается и используется еженедельной проверкой.
- **Каталог готовых списков.** Проект [awg-manager](https://github.com/hoaxisr/awg-manager) держит все свои заготовки в одном файле и постоянно его правит, поэтому копия, собранная руками, устаревала за считанные дни. Теперь этот файл скачивается раз в неделю, а в пакет вшита копия на 87 записей — на свежей установке без интернета выбор всё равно есть. Скачанное принимается, только если разбирается как непустой каталог: страница-заглушка провайдера не сможет затереть рабочую копию.

### Изменено

- Интерфейс стал ближе к привычной навигации KeeneticOS/NDMS: переработаны шапка, заголовки страниц, боковое меню, состояния наведения и расположение основных действий.
- «Обзор системы» переименован в «Дашборд».
- Перетаскивание строк показывает место вставки и больше не заставляет список прыгать обменом соседних элементов.
- README короче: подробности оформления заменены одной фразой, инструкция публикации убрана.

## [3.0.7-sb.8] — 2026-07-20

### Исправлено

- **Сервис мог не подняться после перезагрузки роутера.** Применение правил вызывает `iptables-restore` и `ipset` как внешние процессы, а ожидание завершения не имело предельного срока; сам `iptables-restore` запускался без `-w` и бесконечно ждал освобождения блокировки xtables. Прошивка при загрузке перенастраивает iptables десятками раз в секунду, поэтому демон вставал намертво, не дойдя до цикла событий, — маршрутизации не было вовсе. Теперь ожидание внешней команды ограничено 30 секундами с эскалацией TERM → KILL, `iptables-restore` вызывается с `-w` и откатом на вызов без ключа для сборок, которые его не знают, а неудача применения не обрывает запуск: сервис поднимается и повторяет попытку с нарастающей задержкой.
- **Клиенты VPN-сервера роутера не попадали в туннели.** Прошивка подменяет адрес источника только для сетей, которые сама направила в туннель, поэтому трафик клиентов входящего WireGuard уходил с внутренним адресом и оставался без ответа. keen-pbr-sb теперь маскарадит выход каждого интерфейса, задействованного в outbound, — это покрывает и sing-box, и нативные WireGuard с AmneziaWG, а заодно гостевые сегменты.

### Добавлено

- **Журнал в файл.** `syslog(3)` в Entware никто не слушает, а стандартный поток ошибок при запуске из init-скрипта теряется, поэтому падения при загрузке не оставляли следов. Появился `/opt/var/log/keen-pbr.log` с отметками времени, сбросом на диск после каждой строки и ротацией на мегабайте, `GET /api/logs` для чтения хвоста и раздел настроек с выключателем записи и выбором подробности — настройка применяется на лету.
- **Задержка для всех интерфейсов.** Раньше измерялись только участники failover-групп; теперь каждый интерфейсный outbound, включая нативные туннели прошивки, опрашивается тем же запросом к `generate_204` раз в 20 секунд. Рядом с цифрой показан возраст замера и кнопка немедленной проверки.
- **Уведомления в шапке.** Колокольчик собирает предупреждения и ошибки из журнала и сообщение о доступном обновлении. Кнопка очистки помечает прочитанным, не трогая сам журнал.
- **Карточка роутера в обзоре системы** — модель, процессор с температурой, память, Wi-Fi, адрес WAN, клиенты, версия прошивки, время работы и средняя нагрузка. Управление маршрутизацией переехало в карточку служб.
- **Вход учётной записью роутера** переключается в настройках; обновление перенесено в конец страницы.

### Изменено

- Обновлены оформление и формулировки интерфейса: единая шапка, более ясная визуальная иерархия, понятные подписи вместо внутренних терминов и человекочитаемое время состояний соединения.

## [3.0.7-sb.6] — 2026-07-19

### Исправлено

- **DNS-detour не работал на практике, из-за чего пропадал доступ к сайтам из списков.** Метка, добавленная в sb.5, перекидывала запросы dnsmasq в туннель уже после того, как ядро выбрало адрес источника: пакет уходил в туннель с адресом WAN, пир его не принимал, ответ не возвращался. Резолв молча падал, IP-адреса не попадали в ipset, и трафик до сайтов не доходил вовсе. Router-originated detour-трафик теперь помечается отдельным битом `0x01000000` (вне пользовательской маски fwmark) и маскарадится на выходе цепочкой `KeenPbrSnat` в `nat POSTROUTING` — источником становится адрес туннеля. Для nftables добавлена цепочка `router_origin_snat` в hook postrouting.

### Изменено

- Интерфейс получил знакомую структуру KeeneticOS/NDMS, компактное меню, плоские таблицы и более предсказуемые формы. Состояние служб и задержка транспортов вынесены на дашборд.
- Управление транспортами переведено на тумблеры, а порядок правил — на перетаскивание за ручку. Панели состояния и массовых действий больше не перекрывают содержимое.

## [3.0.7-sb.5] — 2026-07-19

### Добавлено

- **Принудительный DNS клиентов (Client DNS enforcement).** Новая опция `dns.client_dns_enforcement` и переключатель в общих настройках веб-интерфейса: обычный DNS (порт 53) от клиентов прозрачно перенаправляется на резолвер роутера (nat REDIRECT), а DNS-over-TLS (порт 853) блокируется. Это закрывает главный источник «утечек» доменной маршрутизации — Secure DNS/DoH-клиенты, резолвящие домены мимо dnsmasq, из-за чего IP-адреса не попадали в ipset и трафик шёл напрямую. DoH на порту 443 универсально заблокировать нельзя — рекомендация отключить Secure DNS в браузерах сохраняется в подсказке интерфейса.
- Первая полная переработка интерфейса: синяя палитра, единая навигация, формы, диалоги, таблицы, адаптивная компоновка и фактический индикатор состояния nfqws2.
- Фактическая задержка urltest в карточках транспортов и interface-outbound.
- Отдельная кнопка штатного перезапуска каждого управляемого транспорта.

### Изменено

- Восстановлен подробный основной README с описанием архитектуры, сценариев использования, установки и быстрого старта; новые сведения добавлены без сокращения прежнего текста.
- Периодический опрос nfqws2 теперь полностью пассивен и определяет здоровье по процессу и активной NFQUEUE 300, а не по ошибочной строке init-скрипта.
- Улучшены элементы управления и визуальная обратная связь панели.
- **Kill-switch по умолчанию для туннельных outbound.** Для interface-outbound без шлюза (sing-box TUN, WireGuard/AmneziaWG) `strict_enforcement` теперь по умолчанию включён: при падении или перезапуске туннеля помеченный трафик блокируется unreachable-маршрутом, а не «проваливается» в прямое подключение с настоящим IP. Явные значения `strict_enforcement` на уровне daemon или outbound по-прежнему имеют приоритет — обратите внимание, что явно записанное `"strict_enforcement": false` в `daemon` отключает новое поведение.

### Исправлено

- Ложное состояние «nfqws2 остановлен» при живом процессе и пустом `/opt/var/run/nfqws2.pid`.
- Ошибка restart nfqws2, при которой новый процесс запускался до освобождения очереди 300 и завершался с `nfq_create_queue(): Operation not permitted`.
- При явно запрошенном restart/stop PID-файл безопасно восстанавливается только для единственного найденного процесса; restart ожидает освобождения процесса и NFQUEUE перед новым запуском.
- **DNS detour не работал для запросов самого роутера.** Цепочка маркировки существовала только в `mangle PREROUTING`, поэтому запросы dnsmasq к upstream-серверам с `detour` (например, `8.8.8.8` через AWG или `1.1.1.1` через VLESS) уходили в WAN открытым текстом и могли перехватываться провайдером. Добавлена цепочка `KeenPbrOutput` в `mangle OUTPUT` (nftables: chain `output` типа `route`), которая маркирует локально порождённый DNS-трафик, после чего он корректно перенаправляется через detour-туннель.
- **Перезапуск транспорта мог оставить его выключенным.** Действия транспорта выполнялись с контекстом HTTP-запроса, а `SingBox.Up()` по отмене контекста убивает только что запущенный процесс: если браузер обрывал запрос за время рестарта (до ~15 с), туннель оставался down. Действия переведены на независимый контекст с таймаутом 90 с.
- **Кнопка перезапуска транспорта вела себя непонятно.** Во время рестарта она исчезала (транспорт кратковременно уходит в down), не показывала прогресс, а сама операция блокировала кнопки всех остальных карточек. Теперь кнопка остаётся на месте, крутит иконку и блокирует только свою карточку.
- **Номер очереди nfqws2 был зашит в код.** Проверка здоровья и подтверждение start/restart всегда смотрели на очередь 300, поэтому при изменении `NFQUEUE_NUM` через веб-интерфейс служба показывалась остановленной, а успешный запуск — ошибкой. Значение читается из `nfqws2.conf`.
- **Проверка обновления из веб-интерфейса падала с «GitHub вернул некорректный тег выпуска».** Тег и ссылки на ассеты извлекались из ответа GitHub построчно (`grep | cut -d '\"' -f 4`), что работает только для форматированного JSON; при компактном ответе разбирался посторонний фрагмент. Разбор `self-update.sh` и `install.sh` теперь не зависит от форматирования ответа.
- **Вендоренные CMake-модули не попадали в репозиторий.** Из-за правила `*.cmake` в `.gitignore` файлы вроде `third_party/fmt/support/cmake/JoinPaths.cmake` и `third_party/cpptrace/cmake/*.cmake` не коммитились, и сборка из свежего клона падала на этапе configure.

## [3.0.7-sb.4] — 2026-07-18

### Добавлено

- Отдельный transport-manager для управляемых sing-box TUN.
- Импорт share-link и произвольного sing-box outbound JSON.
- Поддержка VLESS, VMess, Trojan, Shadowsocks, Hysteria2, TUIC, AnyTLS, SOCKS и HTTP(S)-прокси через sing-box.
- Нативные интерфейсы Keenetic, WireGuard и AmneziaWG.
- `urltest`/failover-цепочки между несколькими интерфейсами.
- Детерминированный уникальный `/30` для каждого sing-box TUN и ручное поле `tun_address`.
- Health-check транспорта по фактическому состоянию urltest keen-pbr, а не только по наличию процесса.
- Кнопка исключения адреса VPN-сервера из туннеля для защиты от маршрутной петли.
- Настраиваемые bootstrap DNS.
- Текстовые URL-списки и бинарные sing-box rule-set `.srs`.
- Настраиваемое автоматическое обновление URL-списков.
- Мастер быстрого создания правил маршрутизации и DNS при добавлении списка.
- Импорт и экспорт списков и правил маршрутизации.
- Страница активных соединений: адреса, порты, состояния, устройства Keenetic, домены при наличии DNS-сопоставления, маршрут и автообновление. История ограничена 1500 соединениями.
- Русская авторизация веб-интерфейса с HttpOnly cookie-сессией.
- Интерактивные русские установщик и деинсталлятор.
- Установка проверенной версии sing-box `1.13.14` и предупреждение о непроверенных новых версиях.
- Опциональная установка `nfqws2-keenetic`.
- Встроенная страница nfqws2: служба, конфигурация, стратегии, списки, Lua-скрипты, журналы, обновление пакета и проверка сайтов.
- Редактирование штатных сжатых Lua-скриптов `*.lua.gz` с прозрачной распаковкой и обратным сжатием.
- Проверка и установка обновлений keen-pbr-sb из веб-интерфейса.
- Показ заметок GitHub Release и ссылки на полный `CHANGELOG.md` перед веб-обновлением.

### Изменено

- Брендинг веб-интерфейса заменён на `keen-pbr-sb`.
- FORWARD-правила управляемых TUN добавляются в конец цепочки, а не перед пользовательскими правилами.
- README стал русским основным описанием форка.
- Удалён тяжёлый локальный видеоролик upstream-документации; документация использует внешнюю ссылку на оригинал.
- GitHub Actions исключены из поставки: AArch64 IPK публикуется вручную в GitHub Release.

### Исправлено

- Конфликт одинакового TUN-адреса `172.19.0.1/30` у нескольких одновременных sing-box-транспортов.
- Зомби-состояние «процесс жив, туннель не работает».
- Накопление осиротевших процессов и FORWARD-правил после аварийного завершения transport-manager.
- Распознавание `sing-box.real`, запускаемого через загрузчик Entware `ld-2.27.so`.
- Возможная маршрутная петля через адрес прокси-сервера.
- Ошибка sing-box `unknown field dns_mode`.
- Поиск sing-box только по `/opt/bin/sing-box` без учёта выбранного пользовательского пути.
- Повторный запуск установщика, необходимый в ранних сборках.
- Установка nfqws2 через HTTPS при наличии `wget-nossl` вместо `wget-ssl`.
- Права списков nfqws2 после редактирования из веб-интерфейса.
- Пустой список Lua-файлов nfqws2 из-за расширения `.lua.gz`.
- Выбор и индикация активной стратегии nfqws2.
- Исчезающие уведомления после применения стратегии, перезапуска и обновления nfqws2.
- Обрезание значений и кнопок в карточке транспорта.
- Потеря пользовательской конфигурации при переустановке IPK.
- Ложное предложение отката, когда установленная версия новее последнего GitHub Release.

## [3.0.7-sb.3] — 2026-07-18

Первый полный публичный выпуск форка для Entware `aarch64-3.10`.

### Добавлено

- Управляемые sing-box-транспорты.
- VLESS, VMess, Trojan, Shadowsocks и другие протоколы sing-box.
- Нативные WireGuard/AmneziaWG-интерфейсы.
- URLTest failover.
- Keenetic DNS Override и bootstrap DNS.
- Бинарные SRS-списки.
- Страница активных соединений.
- Импорт и экспорт списков и правил.
- Встроенное управление nfqws2 и набор стратегий.
- Проверенная версия sing-box `1.13.14`.

### Ограничения

- Ряд функций присутствовал в первой реализации, но ещё требовал исправлений, перечисленных в `3.0.7-sb.4`.

## 3.0.7-sb.2 — 2026-07-18

Промежуточная локальная сборка без отдельного публичного GitHub Release.

### Изменено

- Очищена структура исходников и исключены ненужные сборочные файлы.
- Подготовлена ручная публикация IPK и `SHA256SUMS` без GitHub Actions.
- Уточнены имена пакета и тегов форка.

## [3.0.7-sb.1] — 2026-07-18

Первый публичный тестовый пакет форка для Entware `aarch64-3.10`.

### Добавлено

- Первичная интеграция дополнений keen-pbr-sb в Keenetic IPK.
- Первый вариант однострочного установщика.
- Публикация AArch64-пакета и файла `SHA256SUMS`.

### Ограничения

- Выпуск имел экспериментальный статус и не рекомендуется для новой установки.
- Заметки GitHub Release для этой версии не заполнялись.

[3.0.7-sb.4]: https://github.com/blindtechnique/keen-pbr-sb/releases/tag/v3.0.7-sb.4
[3.0.7-sb.3]: https://github.com/blindtechnique/keen-pbr-sb/releases/tag/v3.0.7-sb.3
[3.0.7-sb.1]: https://github.com/blindtechnique/keen-pbr-sb/releases/tag/3.0.7-sb1
